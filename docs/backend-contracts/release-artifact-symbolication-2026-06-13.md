# Backend Contract Report: Release Artifact Symbolication - 2026-06-13

## Status

This is an SDK-originated backend contract request. No SDK currently advertises source-map or native debug-symbol support as release-ready. SDK-side validation now has `scripts/create_js_release_artifact_manifest.py`, which creates a dry-run JavaScript source-map manifest without uploading or storing artifacts, `scripts/prepare_js_release_artifact_debug_ids.py`, which dry-runs or explicitly writes matching Debug IDs into built JavaScript/source-map pairs and can strip embedded `sourcesContent` plus absolute source prefixes, `scripts/upload_js_release_artifacts.py`, which proves JavaScript artifact upload transport only against loopback fake intakes, `scripts/create_native_release_artifact_manifest.py`, which creates a dry-run native/mobile artifact manifest for iOS dSYM bundles, Android ProGuard/R8 mappings, Android native `.so` symbols, Unity symbols, Breakpad `.sym` files, and PE/PDB pairs, and real-user smoke scripts that prove web `.js`, real Vite 8 build output, real Next 16 build output, real React Native 0.86 Metro bundle output, React Native-style `.jsbundle`, JavaScript fake-intake upload success/auth-failure/validation-failure/retry/tamper handling, dSYM UUID extraction, ProGuard-mapping, Android ELF-symbol validation, Unity `build_id`/native `.so`/IL2CPP mapping validation, Android native symbol size/count limits, duplicate Android build-ID/richer-symbol handling, Breakpad MODULE metadata validation and duplicate/richer-symbol handling, and PE CodeView/PDB metadata validation and duplicate debug-ID handling without public backend upload. Backend handoff is pending because no backend automation/thread target is exposed in this session.

## Priority

P1 - This blocks production-grade frontend, mobile, native, and Unity debugging confidence against Sentry/Datadog-class expectations. Runtime SDKs can still ship safely, but LogBrew should not claim release-artifact symbolication support until backend upload, validation, lookup, and symbolicated error proof exist.

## User Impact

Developers comparing LogBrew with Sentry or Datadog will see a major production triage gap: minified JavaScript errors and native/mobile crashes remain hard to read unless LogBrew can match runtime stack frames to uploaded build artifacts. This weakens trust for frontend, React Native, mobile, Unity, and native SDK adoption even though runtime error capture works.

## Expected Backend Capability

LogBrew should support release-artifact ingestion separately from telemetry ingestion.

Required artifact identity:

- `release`: required, same product meaning as first-party telemetry ingest.
- `environment`: required for first-party parity and deployment isolation.
- `service`: required for multi-service apps and source-map matching.
- `artifactType`: one of `javascript_source_map`, `javascript_minified_source`, `ios_dsym`, `android_proguard_mapping`, `android_native_symbols`, `dotnet_pdb`, `breakpad_symbols`, `elf_debug`, `unity_symbols`.
- `debugId` or `artifactId`: optional at first, but required for debug-ID-based matching once SDK/tooling emits it.
- `minifiedPathPrefix` and `minifiedUrl`: required for JavaScript URL/path matching when no debug ID is present.
- `git.repositoryUrl` and `git.commitSha`: optional, app-owned, never inferred server-side.
- `artifactSha256`, `contentType`, `byteSize`, `createdAt`, and uploader SDK/tool version.

Suggested APIs:

- `POST /api/release-artifacts` for multipart upload of artifact file plus JSON metadata.
- `POST /api/release-artifacts/manifest` for dry-run validation without storing files.
- `GET /api/release-artifacts?release=...&environment=...&service=...` for deployment verification.
- Artifact retention endpoint for privacy requests and release hygiene.
- Internal symbolication lookup keyed by `release`, `environment`, `service`, `debugId` when present, otherwise normalized minified frame URL/path.

Validation and privacy behavior:

- Reject missing release, environment, service, artifact type, empty files, malformed source maps, and unsupported symbol formats.
- Enforce file size and artifact count limits per release.
- Normalize JavaScript frame URLs by removing query strings and fragments before matching.
- Treat source-map `sourcesContent` as sensitive. Accept it only when the uploader explicitly opts in; otherwise prefer maps without embedded source content and Git-link metadata.
- Store Git metadata only when provided by app-owned CI or tooling.
- Return structured validation errors with stable codes so SDK/CLI tests can assert behavior.

Runtime telemetry matching fields:

- Issue/error events should keep current release/environment requirements.
- Optional runtime fields should include `service`, `debugId`, `artifactId`, `bundleId`, `platform`, `runtime`, and stack frame `filename`/`lineno`/`colno`.
- SDKs must keep raw stack text opt-in. Symbolication should work with structured frames when available and avoid requiring raw unbounded stack strings as the only input.

## SDK Gap Observed

Current SDKs have no public release artifact uploader, no framework build plugin, and no installed-app proof for source-map or debug-symbol lookup. The repo now has dry-run JavaScript artifact validation, Debug ID preparation with optional `sourcesContent` and source-prefix stripping, Vite, Next.js, and React Native build-output proof, loopback-only JavaScript upload transport proof, native/mobile dSYM UUID extraction, ProGuard/R8, Android native `.so` validation with explicit file-count/byte limits and duplicate identity handling, Unity `build_id`/native `.so`/IL2CPP mapping validation, Breakpad `.sym` validation with duplicate/richer-symbol handling, and PE/PDB manifest validation with duplicate debug-ID handling, and real-user build-output/upload smokes in CI/release-readiness/public-verifier/checklist gates, but it is not a public backend upload or symbolication workflow. Runtime timeline helpers are strong, but they do not solve backend minified stack trace readability.

## Suggested SDK Work After Backend Contract

- Promote the local JavaScript manifest, Debug ID preparation, and upload transport verifier into a public artifact tool only after the backend contract exists; keep upload endpoints loopback-only until backend intake validation, auth behavior, storage, lookup, and symbolication are proven.
- Keep Vite, Next, and React Native build-output proof separate from runtime SDK install; React Native currently needs explicit CLI and Metro config packages for real bundle generation.
- Keep runtime packages dependency-light; do not add source-map parsing or upload dependencies to `@logbrew/sdk`.
- Add docs that teach release artifact setup separately from normal SDK install.
- Add real-user verifier apps that build minified JS, upload source maps to a local fake intake, emit a minified error with release/environment/service/debug ID, and assert a symbolicated result.
- After backend intake and lookup exist, add optional build-plugin proof for Vite/Next-style output so users can adopt artifact upload without adding dependencies to runtime SDK packages.
- Extend native/mobile contracts beyond the current dSYM UUID, ProGuard/R8, Android native ELF, Unity symbol, Breakpad `.sym`, and PE/PDB dry-run manifests to richer UUID/build-id extraction, upload, lookup, and symbolicated-error proof.

## Competitor Evidence

- Sentry JavaScript docs describe source-map upload as a separate build/deploy workflow.
- Sentry public source at `getsentry/sentry-javascript@4e12c7a9013daa6b14e6b7e6106304e3eba42724` includes artifact bundle test utilities that pair minified source and source maps by `debug-id`, plus a SvelteKit Vite plugin that controls build source-map settings and upload timing.
- Datadog source-map docs and `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` require service, release version, and minified path prefix for JavaScript source-map upload, validate source-map/minified files, attach optional Git metadata, and include React Native debug-ID injection.
- 2026-06-15 official-doc drift check: Sentry still recommends Debug IDs for source-map matching, while Datadog now documents an optional Source Maps build plugin that discovers `.js`/`.map` pairs and uploads them with Git metadata during builds.
- 2026-06-17 native/mobile source read: Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` has dry-run/no-upload ProGuard and debug-file commands in `src/commands/proguard/upload.rs`, `src/utils/proguard/upload.rs`, `src/utils/proguard/mapping.rs`, `src/commands/debug_files/upload.rs`, and `src/utils/dif_upload/mod.rs`. Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@a5a55f8448cddd9e0282a89c436b339704b30ab3` wires ProGuard UUID generation/upload and native-symbol upload/no-upload tasks through `SentryGenerateProguardUuidTask.kt`, `SentryUploadProguardMappingsTask.kt`, `SentryUploadNativeSymbolsTask.kt`, and `SentryTasksProvider.kt`. Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` exposes dSYM/ELF/Unity symbol dry-runs in `packages/base/src/commands/dsyms/upload.ts`, `dsyms/utils.ts`, `elf-symbols/upload.ts`, and `unity-symbols/upload.ts`.
- 2026-06-17 Android native symbol source read: Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` `packages/base/src/commands/elf-symbols/elf.ts`, `upload.ts`, `interfaces.ts`, and `elf-constants.ts` parse ELF headers, GNU/Go build IDs, executable/shared-library type, architecture, `.debug_info`, `.symtab`, `.dynsym`, `.text`, code hash fallback, and symbol-source metadata before upload. LogBrew now mirrors the validation shape in a dependency-free dry-run manifest, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 dSYM UUID source read: Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` `packages/base/src/commands/dsyms/upload.ts`, `dsyms/utils.ts`, and `dsyms/interfaces.ts` use `dwarfdump --uuid`, parse UUID/arch rows, thin multi-arch bundles with `lipo`, and upload UUID metadata. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` parses debug files through `symbolic` and records `Object::debug_id()`. LogBrew now mirrors the identifier-first shape for dry-run manifests by extracting Mach-O `LC_UUID` values from dSYM DWARF files without Apple tooling, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 Breakpad source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/breakpad.ts`, `upload.ts`, and `__tests__/pe.test.ts` parse ASCII Breakpad `MODULE` headers, normalize GUID/age identifiers, infer `debug_info` from early `FILE` records, and upload `.sym` files as PE symbol sources. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif.rs` treats Breakpad as a debug-info format through `symbolic` and records debug IDs/features. LogBrew now mirrors the identifier-first validation shape for dry-run manifests without serializing FILE/FUNC/PUBLIC records, source paths, local module directories, function names, or symbol payload bytes, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 PDB/PE source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/pe.ts`, `pe-constants.ts`, `upload.ts`, and `__tests__/pe.test.ts` parse PE headers, CodeView `RSDS` GUID/age/PDB filename metadata, architecture, and associated sibling `.pdb` files. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif.rs` and `src/utils/dif_upload/mod.rs` treat PDB/Portable PDB as debug-info formats and fix PDB age from PE metadata. LogBrew now mirrors the PE-side identity validation shape for `dotnet_pdb` dry-run manifests without serializing PDB payload bytes or embedded local PDB paths, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 native artifact limits source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/elf-symbols/upload.ts` and `packages/base/src/commands/pe-symbols/upload.ts` expose dry-run/no-upload, max concurrency, and duplicate build-ID handling that prefers richer symbols. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs`, `src/utils/dif_upload/error.rs`, and `src/constants.rs` enforce maximum debug-file size and treat oversized DIFs as hard validation errors. Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@f62e071eea2e6db2b0e646fcbd4f0dddd17fffb5` keeps native source upload opt-in through `SentryUploadNativeSymbolsTask.kt` and `SentryPluginExtension.kt`. LogBrew now mirrors the dry-run safety subset with explicit manifest `limits`, CLI overrides, Android native symbol count/size blocking, Android duplicate identity richer-symbol selection, and PE/Breakpad duplicate GUID-age selection, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 Unity symbol source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/unity-symbols/upload.ts`, `helpers.ts`, `interfaces.ts`, `README.md`, and `__tests__/upload.test.ts` require platform, symbols location, `build_id`, Android `.so` files, and optional `LineNumberMappings.json` with dry-run/no-upload behavior. Sentry Unity `getsentry/sentry-unity@4cda4392328f489a0b22490f0f0a2cc401cfc622` `src/Sentry.Unity.Editor/Android/DebugSymbolUpload.cs` and `test/Sentry.Unity.Editor.Tests/Android/DebugSymbolUploadTests.cs` discover Unity 2021.2+ `Library/Bee` and older `Temp` symbol paths, copy `.so` files into exported Gradle symbols, and wire `sentry-cli debug-files upload --il2cpp-mapping`. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` searches folders/ZIPs, computes IL2CPP mappings, and size-validates DIFs. LogBrew now mirrors the safe dry-run subset with `unity_symbols` manifests that require a safe `build_id`, validate native `.so` files through shared ELF rules, summarize `LineNumberMappings.json` without serializing source paths or methods, and warn when managed mappings are absent, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 PE/Breakpad duplicate source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/upload.ts`, `breakpad.ts`, and `pe.ts` parse PE and Breakpad GUID/age identities, then `PESymbolsUploadCommand.removeBuildIdDuplicates` keeps the richer symbol source. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` and `src/utils/dif.rs` key debug files by debug IDs and include PE/PDB/PortablePDB/Breakpad support through `symbolic`. LogBrew now mirrors the dry-run identity-selection subset for Breakpad and PE/PDB manifests without serializing source paths, function names, PDB payload bytes, or symbol bytes, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 Vite/source-map source read: Sentry JavaScript `getsentry/sentry-javascript@ccf5571c78c1116ba15fa7186234c86dcf57db5a` `packages/sveltekit/src/vite/sourceMaps.ts` and `dev-packages/test-utils/src/sourcemap-upload-utils.ts` enable or preserve Vite source maps, defer upload until build output is complete, pair minified files and maps by Debug ID, and can delete maps after upload. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/upload.ts`, `validation.ts`, `interfaces.ts`, and `helpers/git/format-git-sourcemaps-data.ts` require release/service/minified path prefix, discover JS/map pairs, validate files, attach optional Git metadata, support dry-run, and retry upload. LogBrew now proves the local build-artifact half for `vite@8.0.16`: Debug ID prep can strip `sourcesContent`, the manifest is ready without `--allow-sources-content`, query/hash text is removed from minified URLs, and a generated source-map position resolves back to `src/main.js`, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 Next/source-map source read: Sentry JavaScript `getsentry/sentry-javascript@839391fecb10be6765f55c418197cad541a1784b` `packages/nextjs/src/config/getBuildPluginOptions.ts`, `handleRunAfterProductionCompile.ts`, `clientNormalizationIntegration.ts`, `webpack.ts`, and `dev-packages/test-utils/src/sourcemap-upload-utils.ts` scope Next upload paths, inject Debug IDs after production compile, upload after framework output exists, optionally delete maps, strip stale sourceMappingURL comments, and normalize frame filenames to `app:///_next/...`. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` keeps the generic source-map CLI model with explicit release/service/minified path prefix, validation, optional Git metadata, dry-run, and upload retries. LogBrew now proves the local build-artifact half for real `next@16.2.9` App Router output: Debug ID prep strips Next `sourcesContent`, manifests are ready without `--allow-sources-content`, minified URLs use `app:///_next/static/chunks` without query/hash text, and a generated source-map position resolves back to `components/CheckoutProbe.jsx`, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 React Native/source-map source read: Sentry React Native `getsentry/sentry-react-native@78878471dc46a38eb72268f452a8abac8c1813a2` `packages/core/sentry.gradle.kts`, `sentry-xcode.sh`, `copy-debugid.js`, `has-sourcemap-debugid.js`, and Expo upload CLI logic force/find Metro source maps, copy Debug IDs to Hermes maps, and delegate upload to Sentry CLI. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/react-native/injectDebugId.ts`, `upload.ts`, `interfaces.ts`, `utils.ts`, and `validation.ts` target `.bundle`/`.jsbundle` plus sibling `.map`, require release/service/platform/build metadata, support dry-run/debug-ID injection, and can remove `sourcesContent`. LogBrew now proves the local build-artifact half for a real `react-native@0.86.0` Android Metro bundle with explicit `@react-native-community/cli@20.1.3`, `@react-native/metro-config@0.86.0`, `react@19.2.7`, Metro 0.84.4, and `@jridgewell/trace-mapping@0.3.31`: Debug ID prep strips `sourcesContent`, strips absolute app source prefixes from Metro `sources`, manifests are ready without `--allow-sources-content`, minified URLs use `app:///react-native` without query/hash text, and a generated source-map position resolves back to `index.js`, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 JavaScript upload-transport source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/helpers/upload.ts`, `helpers/retry.ts`, `commands/sourcemaps/upload.ts`, `interfaces.ts`, and `validation.ts` validate local files, build multipart metadata/file payloads, and retry server/network failures without retrying auth or validation failures. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/sourcemaps/upload.rs`, `src/utils/dif_upload/mod.rs`, `src/utils/chunks/upload.rs`, `src/api/mod.rs`, and `src/api/errors/api_error.rs` separate auth, chunked upload, no-upload/dry-run, missing-auth errors, and assemble polling. Sentry JavaScript `getsentry/sentry-javascript@4a94311d708efffc407535ddcb5d206cda95eee9` `packages/sveltekit/src/vite/sourceMaps.ts`, `packages/gatsby/gatsby-node.js`, and `packages/tanstackstart-react/test/vite/sourceMaps.test.ts` keep framework upload after build output and surface missing auth/org/project setup errors. LogBrew now mirrors the local transport safety subset with a loopback-only fake-intake uploader that revalidates manifest readiness, artifact hashes/sizes, root confinement, multipart shape, redacted endpoints, auth failure, validation failure, retryable 503-to-202 success, and tamper rejection without token/query/temp-path/raw-source leakage, but still has no public backend upload, storage, lookup, or symbolicated-error proof.

## Verification Needed

- Backend unit tests for metadata validation, upload storage, duplicate artifact handling, and delete/list behavior.
- Symbolication tests for debug-ID match, release/environment/service mismatch, path-prefix match, query/hash stripping, and missing artifact fallback.
- Local fake-intake SDK tests now prove JavaScript successful upload, auth failure, validation failure, retryable 503-to-202 recovery, and local tamper rejection, but backend upload/storage/lookup/symbolication proof is still required. Dry-run manifest validation now has focused local tests for ready artifacts, React Native bundle artifacts, missing source maps, sensitive `sourcesContent`, debug-ID mismatch, dSYM UUID extraction and missing-UUID warnings, ProGuard/R8 class mapping validation, Android native ELF build ID/symbol-source validation, Unity `build_id`/native ELF/IL2CPP mapping validation, Android native symbol file-count and byte-limit blocking, Android duplicate build-ID richer-symbol handling, Breakpad MODULE validation/source inference/duplicate richness, PE CodeView/PDB metadata validation/duplicate debug-ID handling, artifact-root confinement, and CLI nonzero exit on blocked manifests. Debug ID preparation now has focused local tests for dry-run-only behavior, explicit `--write` mutation, idempotency, React Native `.jsbundle` sibling-map fallback, map-only Debug ID propagation, `sourcesContent` stripping, source-prefix stripping, mismatch blocking, and CLI nonzero exit on blocked plans. `scripts/real_user_js_release_artifact_smoke.sh` proves the local built-artifact flow end-to-end without backend upload: dry-run, explicit Debug ID write, idempotency, React Native-style bundle/map pairing, `sourcesContent` blocking, explicit allow, query/hash stripping, and no raw source content in the manifest. `scripts/real_user_js_release_artifact_upload_smoke.sh` proves the loopback-only upload transport without backend support: manifest readiness revalidation, multipart manifest/minified/source-map parts, auth failure, validation failure, retryable server failure recovery, artifact tamper blocking before network, and no fake token, query text, temp path, or raw source content leakage in reports or multipart metadata. `scripts/real_user_vite_release_artifact_smoke.sh` proves a real `vite@8.0.16` app build with isolated npm cache, explicit `esbuild@0.28.1` minifier dependency, Debug ID write, Vite `sourcesContent` stripping, manifest readiness without source-content opt-in, query/hash stripping, and generated source-map resolution back to `src/main.js`. `scripts/real_user_next_release_artifact_smoke.sh` proves a real `next@16.2.9` App Router build with isolated npm cache, production browser source maps, Debug ID write, Next `sourcesContent` stripping, manifest readiness without source-content opt-in, `app:///_next/static/chunks` URL normalization, query/hash stripping, and generated source-map resolution back to `components/CheckoutProbe.jsx`. `scripts/real_user_react_native_release_artifact_smoke.sh` proves a real `react-native@0.86.0` Metro Android bundle with explicit CLI/Metro config dependencies, Debug ID write, `sourcesContent` stripping, absolute source-prefix stripping, `app:///react-native` URL normalization, query/hash stripping, and generated source-map resolution back to `index.js`. `scripts/real_user_native_release_artifact_smoke.sh` proves local dSYM UUID, ProGuard/R8, Android native ELF, Unity native `.so` plus IL2CPP mapping, Breakpad `.sym` duplicate/richer-symbol selection, and PE/PDB duplicate debug-ID handling without backend upload, absolute path leakage, symbol bytes, raw Mach-O payload bytes, raw ELF debug-section bytes, Unity source paths/method names, Breakpad source/function records, PDB payload bytes, embedded local PDB paths, or mapping class names in the manifest. These smokes are named in CI, release-readiness, the public verifier, and the SDK readiness checklist.
- Vite, Next, and React Native local build-output proof now exists, but backend upload/lookup/symbolicated-error proof is still required before public support claims.
