# Backend Contract Report: Release Artifact Symbolication - 2026-06-13

## Status

This is an SDK-originated backend contract request. No SDK currently advertises source-map or native debug-symbol support as release-ready. SDK-side validation now has `scripts/create_js_release_artifact_manifest.py`, which creates a dry-run JavaScript source-map manifest without uploading or storing artifacts, `scripts/prepare_js_release_artifact_debug_ids.py`, which dry-runs or explicitly writes matching Debug IDs into built JavaScript/source-map pairs and can strip embedded `sourcesContent` plus absolute source prefixes, `scripts/verify_js_release_artifact_symbolication.py`, which locally resolves one V8-style minified frame through a ready JavaScript manifest without raw source content, `scripts/upload_js_release_artifacts.py`, which proves JavaScript artifact upload transport only against loopback fake intakes, installed `@logbrew/sdk` bin `logbrew-release-artifacts`, which exposes dependency-free local `prepare-js`, `manifest-js`, `symbolicate-js`, and loopback-only `upload-js` commands without LogBrew backend upload, support-ticket creation, or backend symbolication claims, installed `@logbrew/sdk/vite-release-artifacts`, which lets Vite builds run that same local prepare/manifest flow from app-owned build config without upload or backend claims, installed `@logbrew/next/release-artifacts`, which lets Next builds run that local prepare/manifest flow from `next.config.*` after production compile without upload or backend claims, `scripts/create_native_release_artifact_manifest.py`, which creates a dry-run native/mobile artifact manifest for iOS dSYM bundles or ZIP archives, Android ProGuard/R8 mappings, Android native `.so` symbols, Unity symbols, Breakpad `.sym` files, and PE/PDB pairs, and `scripts/upload_native_release_artifacts.py`, which proves native/mobile artifact upload transport only against loopback fake intakes. Real-user smoke scripts prove web `.js`, installed package CLI use from `node_modules/.bin` including local frame resolution and loopback upload dry-run, real Vite 8 build output through the installed Vite plugin with an actual thrown minified frame resolved by installed `symbolicate-js`, real Next 16 build output through installed `@logbrew/next/release-artifacts` with a local minified-frame source-map proof, real React Native 0.86 Metro bundle output with a local minified-frame source-map proof, React Native-style `.jsbundle`, React Native-shaped Android mapping/native `.so` and iOS `.xcarchive/dSYMs` artifact paths, JavaScript installed-CLI and native fake-intake upload success/auth-failure/validation-failure/retry/tamper handling, dSYM UUID extraction from bundles and ZIP archives, ProGuard-mapping, Android ELF-symbol validation, Unity ZIP `build_id`/native `.so`/IL2CPP mapping validation, Android native symbol size/count limits, duplicate Android build-ID/richer-symbol handling, Breakpad MODULE metadata validation and duplicate/richer-symbol handling, and PE CodeView/PDB metadata validation with duplicate debug-ID handling plus external Portable PDB, embedded Portable PDB, and classic Windows PDB payload identity cross-checking without public backend upload. This report is now sent to backend coordination; backend coordination reports auth-gated release-artifact upload, lookup, byte retention, and one-frame JavaScript symbolication are deployed, but scoped live upload/lookup/retention/symbolication verification and symbolicated-error proof are still incomplete. SDK hosted-upload claims and non-loopback upload endpoints remain gated until backend reports live verification for the full path.

## Priority

P1 - This blocks production-grade frontend, mobile, native, and Unity debugging confidence against Sentry/Datadog-class expectations. Runtime SDKs can still ship safely, but LogBrew should not claim release-artifact symbolication support until backend upload, validation, lookup, retained-artifact symbolication, and symbolicated error proof are live-verified.

## User Impact

Developers comparing LogBrew with Sentry or Datadog will see a major production triage gap: minified JavaScript errors and native/mobile crashes remain hard to read unless LogBrew can match runtime stack frames to uploaded build artifacts. This weakens trust for frontend, React Native, mobile, Unity, and native SDK adoption even though runtime error capture works.

## Expected Backend Capability

LogBrew should support release-artifact ingestion separately from telemetry ingestion.

Required artifact identity:

- `release`: required, same product meaning as first-party telemetry ingest.
- `environment`: required for first-party parity and deployment isolation.
- `service`: required for multi-service apps and source-map matching.
- `artifactType`: one of `javascript_source_map`, `javascript_minified_source`, `ios_dsym`, `android_proguard_mapping`, `android_native_symbols`, `dotnet_pdb`, `breakpad_symbols`, `elf_debug`, `unity_symbols`.
- `debugId` or `artifactId`: optional at first, but required for debug-ID-based matching once SDK/tooling emits it.
- `minifiedPathPrefix` and `minifiedUrl`: required for JavaScript URL/path matching when no debug ID is present.
- `git.repositoryUrl` and `git.commitSha`: optional, app-owned, never inferred server-side.
- `artifactSha256`, `contentType`, `byteSize`, `createdAt`, and uploader SDK/tool version.

Suggested APIs:

- `POST /api/release-artifacts` for multipart upload of artifact file plus JSON metadata.
- `POST /api/release-artifacts/manifest` for dry-run validation without storing files.
- `GET /api/release-artifacts?release=...&environment=...&service=...` for deployment verification.
- Artifact retention endpoint for privacy requests and release hygiene.
- Internal symbolication lookup keyed by `release`, `environment`, `service`, `debugId` when present, otherwise normalized minified frame URL/path.

Validation and privacy behavior:

- Reject missing release, environment, service, artifact type, empty files, malformed source maps, and unsupported symbol formats.
- Enforce file size and artifact count limits per release.
- Normalize JavaScript frame URLs by removing query strings and fragments before matching.
- Treat source-map `sourcesContent` as sensitive. Accept it only when the uploader explicitly opts in; otherwise prefer maps without embedded source content and Git-link metadata.
- Store Git metadata only when provided by app-owned CI or tooling.
- Return structured validation errors with stable codes so SDK/CLI tests can assert behavior.

Runtime telemetry matching fields:

- Issue/error events should keep current release/environment requirements.
- Optional runtime fields should include `service`, `debugId`, `artifactId`, `bundleId`, `platform`, `runtime`, and stack frame `filename`/`lineno`/`colno`.
- SDKs must keep raw stack text opt-in. Symbolication should work with structured frames when available and avoid requiring raw unbounded stack strings as the only input.

## SDK Gap Observed

Current SDKs have no public release artifact uploader enabled for hosted LogBrew endpoints and no installed-app proof for live backend source-map or debug-symbol lookup. The repo now has dry-run JavaScript artifact validation, Debug ID preparation with optional `sourcesContent` and source-prefix stripping, an installed `@logbrew/sdk` local release-artifact CLI with local frame resolution plus loopback-only JavaScript upload proof, installed Vite/Next/React Native local release-artifact helpers, local minified JavaScript frame proof for Vite/Next.js/React Native build outputs, loopback-only JavaScript and native upload transport proof, native/mobile dSYM UUID extraction from bundles and ZIP archives, React Native-shaped Android mapping/native `.so` and iOS `.xcarchive/dSYMs` path validation, ProGuard/R8, Android native `.so` validation with explicit file-count/byte limits and duplicate identity handling, Unity ZIP `build_id`/native `.so`/IL2CPP mapping validation, Breakpad `.sym` validation with duplicate/richer-symbol handling, and PE/PDB manifest validation with duplicate debug-ID handling plus external Portable PDB, embedded Portable PDB, and classic Windows PDB payload identity cross-checking, and real-user build-output/upload/installed-package smokes in CI/release-readiness/public-verifier/checklist gates, but it is not a public hosted backend upload or symbolication workflow. Runtime timeline helpers and local source-map resolution proof are strong, but they do not solve backend minified stack trace readability for users until live verification passes.

## Suggested SDK Work After Backend Contract

- Keep the installed JavaScript CLI limited to local preparation, manifest, frame-resolution, and loopback fake-intake upload checks until backend live verification clears the full upload/lookup/retention/symbolication path; keep upload endpoints loopback-only until backend intake validation, auth behavior, storage, lookup, and symbolication are proven from deployed behavior.
- Promote native manifests, Debug ID preparation, and upload transport verifiers into broader public artifact tooling only after backend live upload/storage/lookup verification passes.
- Keep Vite, Next, and React Native build-output proof separate from runtime SDK install; React Native still needs app-owned CLI and Metro config packages for real bundle generation, while `@logbrew/react-native/release-artifacts` only prepares bundle/map output after those tools run.
- Keep runtime packages dependency-light; do not add source-map parsing or upload dependencies to `@logbrew/sdk`.
- Add docs that teach release artifact setup separately from normal SDK install.
- Keep local minified-frame verification available from the repo verifier and installed CLI until backend intake exists; then add real-user verifier apps that upload source maps to a backend-compatible local intake, emit a release/environment/service/debug-ID-bearing minified error, and assert a backend-symbolicated result.
- After backend intake, lookup, and symbolication are live-verified, add optional build-plugin proof for Vite/Next-style output so users can adopt artifact upload without adding dependencies to runtime SDK packages.
- Extend native/mobile contracts beyond the current dSYM bundle/ZIP UUID, ProGuard/R8, Android native ELF, Unity symbol, Breakpad `.sym`, and PE/PDB dry-run manifests with external Portable PDB, embedded Portable PDB, and classic Windows PDB identity checks to richer UUID/build-id extraction, upload, lookup, and symbolicated-error proof.

## Competitor Evidence

- Sentry JavaScript docs describe source-map upload as a separate build/deploy workflow.
- Sentry public source at `getsentry/sentry-javascript@4e12c7a9013daa6b14e6b7e6106304e3eba42724` includes artifact bundle test utilities that pair minified source and source maps by `debug-id`, plus a SvelteKit Vite plugin that controls build source-map settings and upload timing.
- Datadog source-map docs and `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` require service, release version, and minified path prefix for JavaScript source-map upload, validate source-map/minified files, attach optional Git metadata, and include React Native debug-ID injection.
- 2026-06-15 official-doc drift check: Sentry still recommends Debug IDs for source-map matching, while Datadog now documents an optional Source Maps build plugin that discovers `.js`/`.map` pairs and uploads them with Git metadata during builds.
- 2026-06-17 native/mobile source read: Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` has dry-run/no-upload ProGuard and debug-file commands in `src/commands/proguard/upload.rs`, `src/utils/proguard/upload.rs`, `src/utils/proguard/mapping.rs`, `src/commands/debug_files/upload.rs`, and `src/utils/dif_upload/mod.rs`. Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@a5a55f8448cddd9e0282a89c436b339704b30ab3` wires ProGuard UUID generation/upload and native-symbol upload/no-upload tasks through `SentryGenerateProguardUuidTask.kt`, `SentryUploadProguardMappingsTask.kt`, `SentryUploadNativeSymbolsTask.kt`, and `SentryTasksProvider.kt`. Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` exposes dSYM/ELF/Unity symbol dry-runs in `packages/base/src/commands/dsyms/upload.ts`, `dsyms/utils.ts`, `elf-symbols/upload.ts`, and `unity-symbols/upload.ts`.
- 2026-06-17 Android native symbol source read: Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` `packages/base/src/commands/elf-symbols/elf.ts`, `upload.ts`, `interfaces.ts`, and `elf-constants.ts` parse ELF headers, GNU/Go build IDs, executable/shared-library type, architecture, `.debug_info`, `.symtab`, `.dynsym`, `.text`, code hash fallback, and symbol-source metadata before upload. LogBrew now mirrors the validation shape in a dependency-free dry-run manifest, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17/19 dSYM UUID/archive source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/dsyms/upload.ts`, `dsyms/utils.ts`, and `dsyms/interfaces.ts` use `dwarfdump --uuid`, unzip ZIP input, parse UUID/arch rows, thin multi-arch bundles with `lipo`, compress dSYMs by UUID, and upload UUID metadata. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/debug_files/upload.rs`, `src/utils/dif_upload/mod.rs`, and `src/utils/dif.rs` walk directories and ZIPs unless disabled, parse debug files through `symbolic`, and record `Object::debug_id()`. LogBrew now mirrors the identifier-first shape for dry-run manifests by extracting Mach-O `LC_UUID` values from dSYM DWARF files in bundles or safe ZIP archives without Apple tooling, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 Breakpad source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/breakpad.ts`, `upload.ts`, and `__tests__/pe.test.ts` parse ASCII Breakpad `MODULE` headers, normalize GUID/age identifiers, infer `debug_info` from early `FILE` records, and upload `.sym` files as PE symbol sources. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif.rs` treats Breakpad as a debug-info format through `symbolic` and records debug IDs/features. LogBrew now mirrors the identifier-first validation shape for dry-run manifests without serializing FILE/FUNC/PUBLIC records, source paths, local module directories, function names, or symbol payload bytes, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 PDB/PE source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/pe.ts`, `pe-constants.ts`, `upload.ts`, and `__tests__/pe.test.ts` parse PE headers, CodeView `RSDS` GUID/age/PDB filename metadata, architecture, and associated sibling `.pdb` files. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif.rs` and `src/utils/dif_upload/mod.rs` treat PDB/Portable PDB as debug-info formats and fix PDB age from PE metadata. LogBrew now mirrors the PE-side identity validation shape for `dotnet_pdb` dry-run manifests without serializing PDB payload bytes or embedded local PDB paths, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 native artifact limits source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/elf-symbols/upload.ts` and `packages/base/src/commands/pe-symbols/upload.ts` expose dry-run/no-upload, max concurrency, and duplicate build-ID handling that prefers richer symbols. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs`, `src/utils/dif_upload/error.rs`, and `src/constants.rs` enforce maximum debug-file size and treat oversized DIFs as hard validation errors. Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@f62e071eea2e6db2b0e646fcbd4f0dddd17fffb5` keeps native source upload opt-in through `SentryUploadNativeSymbolsTask.kt` and `SentryPluginExtension.kt`. LogBrew now mirrors the dry-run safety subset with explicit manifest `limits`, CLI overrides, Android native symbol count/size blocking, Android duplicate identity richer-symbol selection, and PE/Breakpad duplicate GUID-age selection, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-17 Unity symbol source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/unity-symbols/upload.ts`, `helpers.ts`, `interfaces.ts`, `README.md`, and `__tests__/upload.test.ts` require platform, symbols location, `build_id`, Android `.so` files, and optional `LineNumberMappings.json` with dry-run/no-upload behavior. Sentry Unity `getsentry/sentry-unity@4cda4392328f489a0b22490f0f0a2cc401cfc622` `src/Sentry.Unity.Editor/Android/DebugSymbolUpload.cs` and `test/Sentry.Unity.Editor.Tests/Android/DebugSymbolUploadTests.cs` discover Unity 2021.2+ `Library/Bee` and older `Temp` symbol paths, copy `.so` files into exported Gradle symbols, and wire `sentry-cli debug-files upload --il2cpp-mapping`. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` searches folders/ZIPs, computes IL2CPP mappings, and size-validates DIFs. LogBrew now mirrors the safe dry-run subset with `unity_symbols` manifests that require a safe `build_id`, validate native `.so` files through shared ELF rules, summarize `LineNumberMappings.json` without serializing source paths or methods, and warn when managed mappings are absent, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 Unity archive source read: Sentry Unity `getsentry/sentry-unity@a9bebf56a8808b866ff11330bf42030685701cf9` `src/Sentry.Unity.Editor/Android/DebugSymbolUpload.cs` and `src/Sentry.Unity.Editor/Native/BuildPostProcess.cs` copy/discover Unity symbols and construct `debug-files upload --il2cpp-mapping` commands from build tooling. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/unity-symbols/upload.ts` and `interfaces.ts` separately upload `.so` files and IL2CPP mapping metadata with explicit build identity. LogBrew now accepts `unity_symbols` ZIP archives for local dry-run validation, blocks unsafe archive paths without echoing them, reports `archive.zip!entry` paths, validates `.so` files through ELF rules, and summarizes `LineNumberMappings.json` without serializing source paths, method names, or symbol bytes, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 PE/Breakpad duplicate source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/upload.ts`, `breakpad.ts`, and `pe.ts` parse PE and Breakpad GUID/age identities, then `PESymbolsUploadCommand.removeBuildIdDuplicates` keeps the richer symbol source. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` and `src/utils/dif.rs` key debug files by debug IDs and include PE/PDB/PortablePDB/Breakpad support through `symbolic`. LogBrew now mirrors the dry-run identity-selection subset for Breakpad and PE/PDB manifests without serializing source paths, function names, PDB payload bytes, or symbol bytes, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 Portable PDB payload source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/pe.ts`, `upload.ts`, and `__tests__/pe.test.ts` validate PE CodeView identity and associated PDB inputs. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` plus Sentry symbolic `getsentry/symbolic@3581ad0d67a074e83ba10e612bce34a83e5916a8` `symbolic-debuginfo/src/pe.rs`, `ppdb.rs`, and `symbolic-ppdb/src/format/{mod.rs,streams.rs,raw.rs}` parse Portable PDB payload IDs and embedded Portable PDBs. LogBrew now mirrors the safe external-payload subset for dry-run manifests by reading only the Portable PDB `#Pdb` stream identity, blocking malformed or mismatched Portable PDBs, warning on unrecognized PDB formats, and omitting PDB bytes, source paths, symbols, and source-link data, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 embedded Portable PDB source read: Sentry symbolic `getsentry/symbolic@15e05ed4827c11ed410763de879757ef0246e4b8` `symbolic-debuginfo/src/pe.rs` and `symbolic-ppdb/src/format/{mod.rs,streams.rs,raw.rs}` parse PE debug-directory type 17 `MPDB` records, deflate-decompress embedded Portable PDB bytes, and read the Portable PDB `#Pdb` identity. LogBrew now mirrors the local validation subset for `dotnet_pdb`: PE files with valid embedded Portable PDBs can pass without a sibling `.pdb`, malformed/mismatched embedded payloads block manifests, and compressed bytes, embedded sources, source links, symbols, and payload contents stay out of manifests. This still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 classic Windows PDB source read: Sentry symbolic `getsentry/symbolic@15e05ed4827c11ed410763de879757ef0246e4b8` `symbolic-debuginfo/src/pdb/mod.rs` recognizes MSF 7.00 PDBs and computes debug IDs from PDB GUID plus DBI age when present. Upstream Rust `pdb@0.8.0` paths `src/msf/mod.rs`, `src/pdb.rs`, `src/pdbi.rs`, and `src/dbi.rs` define stream-directory traversal, stream 1 PDB information GUID/age parsing, stream 3 DBI header parsing, and `DebugInformation::age`. LogBrew now mirrors the safe local subset for `dotnet_pdb`: classic Windows PDBs are parsed only for bounded MSF stream directory identity, malformed/mismatched PDBs block manifests, and symbols, source records, module records, file paths, and PDB payload bytes stay out of manifests. This still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 Vite/source-map source read: Sentry JavaScript `getsentry/sentry-javascript@ccf5571c78c1116ba15fa7186234c86dcf57db5a` `packages/sveltekit/src/vite/sourceMaps.ts` and `dev-packages/test-utils/src/sourcemap-upload-utils.ts` enable or preserve Vite source maps, defer upload until build output is complete, pair minified files and maps by Debug ID, and can delete maps after upload. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/upload.ts`, `validation.ts`, `interfaces.ts`, and `helpers/git/format-git-sourcemaps-data.ts` require release/service/minified path prefix, discover JS/map pairs, validate files, attach optional Git metadata, support dry-run, and retry upload. LogBrew now proves the local build-artifact half for `vite@8.0.16`: Debug ID prep can strip `sourcesContent`, the manifest is ready without `--allow-sources-content`, query/hash text is removed from minified URLs, and a generated source-map position resolves back to `src/main.js`, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 Next/source-map source read: Sentry JavaScript `getsentry/sentry-javascript@839391fecb10be6765f55c418197cad541a1784b` `packages/nextjs/src/config/getBuildPluginOptions.ts`, `handleRunAfterProductionCompile.ts`, `clientNormalizationIntegration.ts`, `webpack.ts`, and `dev-packages/test-utils/src/sourcemap-upload-utils.ts` scope Next upload paths, inject Debug IDs after production compile, upload after framework output exists, optionally delete maps, strip stale sourceMappingURL comments, and normalize frame filenames to `app:///_next/...`. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` keeps the generic source-map CLI model with explicit release/service/minified path prefix, validation, optional Git metadata, dry-run, and upload retries. LogBrew now proves the local build-artifact half for real `next@16.2.9` App Router output: Debug ID prep strips Next `sourcesContent`, manifests are ready without `--allow-sources-content`, minified URLs use `app:///_next/static/chunks` without query/hash text, and a generated source-map position resolves back to `components/CheckoutProbe.jsx`, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 React Native/source-map source read: Sentry React Native `getsentry/sentry-react-native@78878471dc46a38eb72268f452a8abac8c1813a2` `packages/core/sentry.gradle.kts`, `sentry-xcode.sh`, `copy-debugid.js`, `has-sourcemap-debugid.js`, and Expo upload CLI logic force/find Metro source maps, copy Debug IDs to Hermes maps, and delegate upload to Sentry CLI. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/react-native/injectDebugId.ts`, `upload.ts`, `interfaces.ts`, `utils.ts`, and `validation.ts` target `.bundle`/`.jsbundle` plus sibling `.map`, require release/service/platform/build metadata, support dry-run/debug-ID injection, and can remove `sourcesContent`. LogBrew now proves the local build-artifact half for a real `react-native@0.86.0` Android Metro bundle with explicit `@react-native-community/cli@20.1.3`, `@react-native/metro-config@0.86.0`, `react@19.2.7`, and Metro 0.84.4: Debug ID prep strips `sourcesContent`, strips absolute app source prefixes from Metro `sources`, manifests are ready without `--allow-sources-content`, minified URLs use `app:///react-native` without query/hash text, and a generated source-map position resolves back to `index.js`, but still has no backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 minified-frame proof source read: Sentry JavaScript `getsentry/sentry-javascript@e856eb4acb67057861b96f924347af6c8b86edfb` `packages/core/src/utils/stacktrace.ts`, `packages/core/src/types/debugMeta.ts`, `dev-packages/test-utils/src/sourcemap-upload-utils.ts`, Next build source-map handlers, and SvelteKit Vite plugin paths normalize stack frames and connect minified files/source maps through Debug IDs for backend symbolication. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/validation.ts` and `upload.ts` validate local minified/source-map payloads before upload. LogBrew now mirrors the local proof subset with `scripts/verify_js_release_artifact_symbolication.py`, which resolves one sanitized V8-style minified frame through a ready manifest without `sourcesContent`, absolute local source paths, raw source text, query/hash text, or temp paths, but still has no backend upload, storage, lookup, or production symbolication.
- 2026-06-18 JavaScript upload-transport source read: Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/helpers/upload.ts`, `helpers/retry.ts`, `commands/sourcemaps/upload.ts`, `interfaces.ts`, and `validation.ts` validate local files, build multipart metadata/file payloads, and retry server/network failures without retrying auth or validation failures. Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/sourcemaps/upload.rs`, `src/utils/dif_upload/mod.rs`, `src/utils/chunks/upload.rs`, `src/api/mod.rs`, and `src/api/errors/api_error.rs` separate auth, chunked upload, no-upload/dry-run, missing-auth errors, and assemble polling. Sentry JavaScript `getsentry/sentry-javascript@4a94311d708efffc407535ddcb5d206cda95eee9` `packages/sveltekit/src/vite/sourceMaps.ts`, `packages/gatsby/gatsby-node.js`, and `packages/tanstackstart-react/test/vite/sourceMaps.test.ts` keep framework upload after build output and surface missing auth/org/project setup errors. LogBrew now mirrors the local transport safety subset with a loopback-only fake-intake uploader that revalidates manifest readiness, artifact hashes/sizes, root confinement, multipart shape, redacted endpoints, auth failure, validation failure, retryable 503-to-202 success, and tamper rejection without token/query/temp-path/raw-source leakage, but still has no public backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 native artifact upload-transport source read: Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/debug_files/upload.rs` and `src/utils/dif_upload/mod.rs` discover, prepare, dry-run/no-upload, and chunk-upload debug files. Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@167c79e7b0a70d37af9ee82c67b9ed764f2b735e` `plugin-build/src/main/kotlin/io/sentry/android/gradle/tasks/SentryUploadNativeSymbolsTask.kt` delegates native symbols to `sentry-cli debug-files upload`, and Sentry Unity `getsentry/sentry-unity@a9bebf56a8808b866ff11330bf42030685701cf9` `src/Sentry.Unity.Editor/Android/DebugSymbolUpload.cs` wires Unity symbol/IL2CPP paths into Gradle upload tasks. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/helpers/upload.ts`, `helpers/retry.ts`, `commands/dsyms/upload.ts`, `commands/elf-symbols/upload.ts`, `commands/pe-symbols/upload.ts`, and `commands/unity-symbols/upload.ts` perform multipart uploads with explicit uploader auth, no retry for 400/401/403/413, and per-format local validation. LogBrew now mirrors the local native transport safety subset with a loopback-only fake-intake uploader that revalidates ready native manifests, root confinement, symlink rejection, directory tree file counts, byte totals, SHA-256 tree hashes, multipart basename file headers, redacted endpoints, auth failure, validation failure, retryable 503-to-202 success, dry-run, and tamper rejection without token/query/temp-path leakage, but still has no public backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18/21 installed CLI source read: Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `package.json`, `src/commands/sourcemaps/mod.rs`, `src/commands/sourcemaps/upload.rs`, `src/utils/chunks/upload.rs`, `src/api/mod.rs`, and `src/api/errors/api_error.rs` expose source-map workflows, auth handling, chunk upload, and dry-run/no-upload behavior from an installed `sentry-cli` binary. Sentry JavaScript `getsentry/sentry-javascript@e856eb4acb67057861b96f924347af6c8b86edfb` `packages/nextjs/src/config/handleRunAfterProductionCompile.ts` runs source-map work after framework production output exists. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/package.json`, `src/cli.ts`, `commands/sourcemaps/cli.ts`, `commands/sourcemaps/upload.ts`, `commands/sourcemaps/validation.ts`, and `packages/base/src/helpers/upload.ts` expose installable commands with local validation, release/service/path-prefix requirements, dry-run behavior, multipart upload, and bounded retry kept outside runtime SDK config. LogBrew now mirrors the local-safe part with an installed `@logbrew/sdk` `logbrew-release-artifacts` bin for `prepare-js`, `manifest-js`, `symbolicate-js`, and loopback-only `upload-js`, but still has no public backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 installed local resolver source read: Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/sourcemaps/resolve.rs` and `mod.rs` resolve one generated line/column through a source map from the installed CLI. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `validation.ts` and `upload.ts` validate minified/source-map pairs and path-prefix configuration before upload. LogBrew now adds installed `@logbrew/sdk` `symbolicate-js`, which resolves one stack frame through a ready manifest while blocking embedded source content and local source paths, but still has no public backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 installed Vite plugin source read: Sentry bundler plugins `getsentry/sentry-javascript-bundler-plugins@988efd30691e08c059eb577e499d0b4346434f3c` `packages/bundler-plugins/src/rollup/index.ts` and `packages/bundler-plugins/src/vite/index.ts` run source-map work from build hooks after output exists, while Sentry JavaScript `getsentry/sentry-javascript@c9730ce3a3635f83ab629559bddbd7345fae9917` `packages/sveltekit/src/vite/sourceMaps.ts`, `packages/nuxt/src/vite/sourceMaps.ts`, and `packages/nextjs/src/config/handleRunAfterProductionCompile.ts` enable or preserve framework source maps and defer upload. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `commands/sourcemaps/upload.ts` and `validation.ts` require release/service/path-prefix metadata and validate local file pairs before upload. LogBrew now adds installed `@logbrew/sdk/vite-release-artifacts` for local Vite prepare/manifest proof only, but still has no public backend upload, storage, lookup, or symbolicated-error proof.
- 2026-06-18 installed Next helper source read: Sentry JavaScript `getsentry/sentry-javascript@c9730ce3a3635f83ab629559bddbd7345fae9917` `packages/nextjs/src/config/withSentryConfig/index.ts`, `getFinalConfigObject.ts`, `getFinalConfigObjectBundlerUtils.ts`, `handleRunAfterProductionCompile.ts`, `getBuildPluginOptions.ts`, and `webpack.ts` wrap Next config, preserve source-map settings, chain production-compile hooks, and run source-map work after framework output exists. Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `commands/sourcemaps/upload.ts` requires release/service/path-prefix metadata and validates local file pairs before upload. LogBrew now adds installed `@logbrew/next/release-artifacts` for local Next prepare/manifest proof only, but still has no public backend upload, storage, lookup, or symbolicated-error proof.

## Verification Needed

- Backend unit tests for metadata validation, upload storage, duplicate artifact handling, and delete/list behavior.
- Symbolication tests for debug-ID match, release/environment/service mismatch, path-prefix match, query/hash stripping, and missing artifact fallback.
- Local fake-intake SDK tests now prove JavaScript and native successful upload, auth failure, validation failure, retryable 503-to-202 recovery, and local tamper rejection, but live backend upload/storage/lookup/symbolication proof is still required. Dry-run manifest validation now has focused local tests for ready artifacts, React Native bundle artifacts, missing source maps, sensitive `sourcesContent`, debug-ID mismatch, dSYM bundle/ZIP UUID extraction and missing-UUID warnings, ProGuard/R8 class mapping validation, Android native ELF build ID/symbol-source validation, Unity directory and ZIP `build_id`/native ELF/IL2CPP mapping validation, Android native symbol file-count and byte-limit blocking, Android duplicate build-ID richer-symbol handling, Breakpad MODULE validation/source inference/duplicate richness, PE CodeView/PDB metadata validation/duplicate debug-ID handling, external Portable PDB, embedded Portable PDB, and classic Windows PDB payload identity cross-check/mismatch/malformed blocking, artifact-root confinement, and CLI nonzero exit on blocked manifests. Debug ID preparation now has focused local tests for dry-run-only behavior, explicit `--write` mutation, idempotency, React Native `.jsbundle` sibling-map fallback, map-only Debug ID propagation, `sourcesContent` stripping, source-prefix stripping, mismatch blocking, and CLI nonzero exit on blocked plans. `scripts/verify_js_release_artifact_symbolication.py` proves one sanitized V8-style generated frame resolves through a ready JavaScript manifest without echoing raw source content or temp paths. `scripts/real_user_js_release_artifact_smoke.sh` proves the local built-artifact flow end-to-end without backend upload: dry-run, explicit Debug ID write, idempotency, React Native-style bundle/map pairing, `sourcesContent` blocking, explicit allow, query/hash stripping, and no raw source content in the manifest. `scripts/real_user_js_release_artifact_cli_smoke.sh` proves the installed `@logbrew/sdk` CLI path from `node_modules/.bin`: npm pack metadata, package bin, CLI help, `prepare-js --write`, `manifest-js`, `symbolicate-js`, loopback-only `upload-js --dry-run`, missing-build-dir failure, source-content stripping, source-prefix stripping, query/hash stripping, local frame resolution, and no private source marker or URL token leakage in CLI reports; focused package tests cover local source-path rejection. `scripts/real_user_js_release_artifact_upload_smoke.sh` proves the installed `@logbrew/sdk` CLI `upload-js` loopback transport without backend support: manifest readiness revalidation, multipart manifest/minified/source-map parts, auth failure, validation failure, retryable server failure recovery, artifact tamper blocking before network, and no fake token, query text, or raw source content leakage in reports or multipart metadata. `scripts/real_user_vite_release_artifact_smoke.sh` proves a real `vite@8.0.16` app build with isolated npm cache, explicit `esbuild@0.28.1` minifier dependency, packed `@logbrew/sdk/vite-release-artifacts` ESM/CommonJS imports, plugin-enabled hidden source maps, plugin Debug ID write, Vite `sourcesContent` stripping, manifest readiness without source-content opt-in, query/hash stripping, and actual thrown minified-frame resolution through installed `symbolicate-js` back to `src/main.js`. `scripts/real_user_next_release_artifact_smoke.sh` proves a real `next@16.2.9` App Router build with isolated npm cache, production browser source maps, Debug ID write, Next `sourcesContent` stripping, manifest readiness without source-content opt-in, `app:///_next/static/chunks` URL normalization, query/hash stripping, and local minified-frame resolution back to `components/CheckoutProbe.jsx`. `scripts/real_user_react_native_release_artifact_smoke.sh` proves a real `react-native@0.86.0` Metro Android bundle with explicit CLI/Metro config dependencies, Debug ID write, `sourcesContent` stripping, absolute source-prefix stripping, `app:///react-native` URL normalization, query/hash stripping, and local minified-frame resolution back to `index.js`. `scripts/real_user_native_release_artifact_smoke.sh` proves local dSYM ZIP UUID, ProGuard/R8, Android native ELF, Unity ZIP native `.so` plus IL2CPP mapping, Breakpad `.sym` duplicate/richer-symbol selection, PE/PDB duplicate debug-ID handling, and external Portable PDB, embedded Portable PDB, and classic Windows PDB payload identity validation without backend upload, absolute path leakage, symbol bytes, raw Mach-O payload bytes, raw ELF debug-section bytes, Unity source paths/method names, Breakpad source/function records, PDB payload bytes, embedded local PDB paths, or mapping class names in the manifest. `scripts/real_user_native_release_artifact_upload_smoke.sh` proves the loopback-only native upload transport for dSYM ZIP, ProGuard/R8 mapping, Android native `.so`, and Unity `symbols.zip` artifacts: ready-manifest revalidation, tree/file count and hash checks, symlink-safe path handling, multipart basename file parts, auth failure, validation failure, retryable server failure recovery, dry-run, tamper blocking before network, and no fake token, query text, or temp path leakage in reports or multipart metadata. These smokes are named in CI, release-readiness, the public verifier, and the SDK readiness checklist.
- Vite, Next, and React Native local minified-frame proof now exists; Vite and Next now have installed build-helper proof, but backend upload/lookup/symbolicated-error proof is still required before public support claims.
