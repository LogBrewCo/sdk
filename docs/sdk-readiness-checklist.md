# SDK Readiness Checklist

This checklist is public-facing and intentionally excludes private systems and operational details.

## Product requirements

- Keep the SDK surface small, dependency-light, and stable.
- Use fake/public placeholders in examples such as `LOGBREW_API_KEY`, and prefer `LOGBREW_CLIENT_KEY` plus `clientKey` naming for browser, frontend, and mobile examples where the value is expected to ship in an app bundle.
- Document event contracts, validation rules, retries, flush behavior, shutdown behavior, and error shapes.
- Ensure all emitted payloads are machine-parseable JSON.
- Treat support-ticket creation as a backend-gated capability. Local SDK
  diagnostics drafts are allowed, but SDKs must not call support-ticket routes
  or claim hosted ticket creation until [the support-ticket backend contract](backend-contracts/support-ticket-routes-2026-06-24.md)
  has deploy/live verification.

## Public documentation requirements

Public READMEs should explain what LogBrew is for, which package to install, how users send logs/errors/traces/metrics, how framework or logger integrations are wired, what privacy defaults protect users, and how automation agents can validate examples. Recommend installing only the runtime package and framework integrations the user's own app needs; never imply that all published packages should be installed together. Keep maintainer-only verifier detail, CI internals, registry mechanics, run IDs, package artifact inspection prose, and "prove this smoke path" language in scripts, checklists, release notes, or commit evidence instead of user-facing README pages.

## Signal readiness

Treat logs, issues/errors, traces/spans, and metrics as distinct observability signals with different safety and proof needs.

- Keep public severity names aligned with the [LogBrew severity contract](severity-contract.md): `info`, `warning`, `error`, and `critical` are product categories; `trace`, `debug`, `warn`, and `fatal` are compatibility aliases that SDKs may accept and normalize before delivery.
- Do not describe metrics support as release-ready until the SDK has an explicit metric event or helper contract with name validation, numeric value validation, unit guidance, aggregation kind guidance such as counter, gauge, or histogram, temporality, timestamp behavior, and low-cardinality attribute rules.
- Keep automatic metric capture opt-in until the source, frequency, and attribute set are bounded. Avoid query strings, raw stack text, document titles, user agents, and unbounded labels by default.
- When metrics helpers exist, prove them from installed artifacts with counters, gauges, histogram-like values, invalid values, omitted optional fields, batching, retry behavior, flush/shutdown behavior, and machine-parseable JSON separate from trace/log parity fixtures.
- Treat profiling, native debug-symbol, and source-map workflows as separate release capabilities, not implied by error or trace capture. Document setup and installed-artifact proof before advertising them as supported.
- Treat release artifact symbolication as a backend-plus-tooling capability. The current source-map/debug-symbol gap is tracked in [the 2026-06-13 competitor review](competitor-research/source-maps-debug-symbols-2026-06-13.md) and [backend contract report](backend-contracts/release-artifact-symbolication-2026-06-13.md); repo-local JavaScript dry-run manifest, installed `logbrew-release-artifacts` CLI prep/manifest/frame-resolution proof, explicit hosted JavaScript upload opt-in, installed `@logbrew/sdk/vite-release-artifacts` build-plugin proof, installed `@logbrew/next/release-artifacts` build-helper proof, installed `@logbrew/react-native/release-artifacts` bundle/upload-helper proof, Debug ID preparation, Vite/Next.js/React Native build-output proof, local minified-frame source-map proof, native/mobile dry-run manifest including external and embedded Portable PDB payload identity checks, JavaScript/Vite/Next.js/React Native loopback plus hosted-dry-run upload proof, native/mobile loopback upload proof, and real-user build-output smoke proof exist, but do not claim full symbolication support until hosted upload, validation, lookup, retained-artifact symbolication, and real minified/native symbolicated-error proof are live verified for that workflow.

## Real-user QA

Each SDK should include a runnable example that installs like a real user and exercises:

- Auth configuration with a clearly fake placeholder API key
- Release event creation
- Environment event creation
- Issue event creation
- Log event creation
- Trace or span event creation
- Action event creation when the language SDK supports it

Each SDK should also verify:

- Successful send
- Empty flush or no-op flush behavior
- Local validation failure
- Unauthenticated response handling
- Network failure handling
- Retry recovery after a transient failure
- Retry-budget exhaustion handling
- Non-retryable transport-status handling
- Flush behavior
- Graceful shutdown
- Post-shutdown rejection of new work
- JSON serialization
- Stable error reporting

Prefer an automated smoke path that creates a fresh throwaway app, installs the SDK through the ecosystem's normal tool, runs a happy-path scenario, exercises the supported failure/lifecycle paths, and asserts parseable output.

Prefer a machine-readable top-level verifier that also records the exact toolchain snapshot used for the run, including language runtimes, compilers, formatters, linters, and package managers, so future regressions can be tied back to ecosystem drift instead of being treated as anonymous SDK failures first.

When an ecosystem has a first-class package artifact, prefer installing that artifact instead of the source directory so packaging mistakes are caught before release.

Keep local Markdown links in package READMEs and shared docs under an automated checker such as `python3 scripts/check_markdown_links.py`; SDK UX includes the path users click before they install anything, not only the code they run afterward.

When smoke and package checks are implemented as shell scripts, run pinned ShellCheck from a temp-owned verifier such as `bash scripts/check_shell_static.sh`; verify the downloaded release digest, lint at style severity where practical, and keep any exclusions narrow and tied to intentional cross-language snippets or dynamic virtualenv activation.

Keep release-facing package metadata under a shared checker such as `python3 scripts/check_release_metadata.py`; require consistent package names, public version values where the ecosystem stores them, MIT license metadata, repository URLs, README presence, runtime requirements, and ecosystem-specific publish fields, while respecting ecosystems like Composer and SwiftPM where versions are expected to come from VCS tags or package registries instead of the manifest.

For JavaScript packages specifically, prefer shipping plain runnable `.js` plus `.d.ts` and `.d.cts` declarations when possible so normal ESM and CommonJS TypeScript users do not need a build step just to consume the SDK, and prefer shipping runnable packaged example files as part of the published artifact, ideally with a tiny example-local helper surface such as `examples/package.json` scripts in addition to the raw example files, and a small Node launcher such as `examples/index.mjs` when that gives installed users a package-manager-independent way to list and run examples. Before focused JavaScript tests, prove repo-checkout `.js`, `.cjs`, and `.mjs` source syntax with a dependency-free check such as `python3 scripts/check_js_sources.py` using Node's syntax checker, then prove those same public JavaScript, Browser, Node.js, Express, Fastify, NestJS, Angular, Vue, Svelte, React, React Native, and Next.js sources through a strict ESLint ruleset such as `bash scripts/check_js_lint.sh` using exact temp-installed tooling so lint strictness does not leave `node_modules`, lockfiles, or hash artifacts in the public tree, then prove publish-facing manifest and export-map quality with a strict package linter such as `bash scripts/check_js_package.sh` using publint with warnings treated as failures. Prefer smoke installs from the packed `.tgz` through both `npm` and `pnpm` when those are public support targets, bootstrap the temp app through a package-manager-native init flow, using `npm init -y` on the npm path and `pnpm init` on the pnpm path, inspect the packed tarball itself before install, verify npm's own `npm pack --dry-run --json` and `npm pack --json` metadata for the tarball name, integrity, shasum, and file list, verify the temp app `package.json` plus lockfile and dependency-tree resolution in those temp apps, including rewritten dependency entries, resolved tarball targets, integrity metadata in `package-lock.json` and `pnpm-lock.yaml`, generated consumer script entries, a match between those lockfile integrity values and the tarball integrity reported by `npm pack --json`, plain dependency-tree output through `npm ls @logbrew/sdk` and `pnpm ls @logbrew/sdk`, direct consumer dependency edges through `npm explain @logbrew/sdk` and `pnpm why @logbrew/sdk`, plain top-level package-manager list output through `npm list --depth=0` and `pnpm list --depth=0`, and structured package-manager package lists through `npm list --json --depth=0`, and `pnpm list --json --depth=0`, prove that the temp app also survives package-manager-native removal through `npm uninstall @logbrew/sdk` and `pnpm remove @logbrew/sdk` before the tarball is added back, prove that those generated lockfiles can recreate the install through `npm ci` and `pnpm install --frozen-lockfile`, verify the packed and installed package metadata, README payload, split ESM/CommonJS `exports` map, packaged example files, packaged example helper files, packaged launcher files, declaration comments in `index.d.ts`, and the CommonJS `index.d.cts` declaration path, including packed and installed README install commands, explicit package-manager helper discovery commands, explicit package-manager helper runtime commands, explicit CommonJS helper commands, explicit launcher discovery commands, explicit named README-example and CommonJS launcher commands, explicit raw packaged ESM and CommonJS example commands, and fake-placeholder guidance plus event attribute types, error, transport, lifecycle helpers, W3C trace context helpers, optional console capture helpers, optional Pino destination helpers, optional Winston transport helpers, and field-level docs for key public response or transport properties, compile small temp TypeScript consumers through ESM `.ts` and CommonJS `.cts` files via a manifest script entry, and run small installed-user test and runtime scripts through `npm run` and `pnpm run`, including a script that mirrors the published README example before and after reinstall plus the packaged README-style and stronger `real-user-smoke` ESM and CommonJS examples directly from `node_modules` and through the installed example helper surface. W3C trace context helpers should stay dependency-free, validate `traceparent`, reject all-zero trace/span ids, normalize ids to lowercase, expose sampled flags, derive LogBrew span attributes from incoming context, create outgoing `traceparent` values from explicit ids, avoid HTTP client patching, and be proven from installed ESM and CommonJS consumers. Optional console capture must be explicit, call the original console method first, patch only the supplied console object, expose `uninstall()` and `flush()`, map console methods consistently, keep stack text opt-in, and be proven from the installed package. Optional Pino support should stay dependency-free in the SDK: expose a stream-like destination for app-owned Pino instances, parse JSON log lines, map Pino numeric and string levels, capture primitive log fields as `context.*`, skip noisy runtime defaults, keep stack text opt-in, report parse/capture failures through `onError`, and prove real installed `pino` `warn`/`error` calls through npm and pnpm temp apps. Optional Winston support should stay dependency-free in the SDK: expose an object-mode transport for app-owned Winston loggers, keep a two-argument `log(info, callback)` shim for Winston's transport detection, map common Winston levels, capture primitive info fields as `context.*`, capture nested `err`/`error` objects and formatted error stack name/message, keep stack text opt-in, report capture failures through `onError`, and prove real installed `winston` `warn`/`error` calls through npm and pnpm temp apps. Also prove that the repo-checkout helper surface itself stays discoverable enough for plain `npm run`, `npm run help`, `npm run list`, `pnpm run`, `pnpm run help`, and `pnpm run list` to reveal the shipped scripts and launcher-backed example commands before users run a specific helper target. Prove that both the package-manager helper surface and the shipped Node launcher are discoverable enough for plain installed `npm run` and `pnpm run` listing output, the explicit installed `npm --prefix node_modules/@logbrew/sdk/examples run list`, `npm --prefix node_modules/@logbrew/sdk/examples run help`, `pnpm --dir node_modules/@logbrew/sdk/examples run list`, and `pnpm --dir node_modules/@logbrew/sdk/examples run help` commands, launcher `--help`, launcher `--list`, the explicit installed launcher commands `node node_modules/@logbrew/sdk/examples/index.mjs readme-example`, `node node_modules/@logbrew/sdk/examples/index.mjs readme-example:cjs`, `node node_modules/@logbrew/sdk/examples/index.mjs real-user-smoke`, `node node_modules/@logbrew/sdk/examples/index.mjs`, and `node node_modules/@logbrew/sdk/examples/index.mjs real-user-smoke:cjs`, named launcher selection, and explicit raw packaged ESM and CommonJS example commands to reveal copy-pasteable README and `real-user-smoke` example commands before they run, with the packaged README teaching those helper-discovery, helper-runtime, launcher, and raw example commands explicitly instead of leaving them implicit in longer prose, so shipped declaration files, manifest script surfaces, launcher surfaces, runtime entrypoints, packaged examples, package-manager graph, tree, package-list views, and add/remove/reinstall lifecycle are all proven in the temp app.

For Browser integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK with install/uninstall helpers, page-view capture, `error` event capture, `unhandledrejection` capture, page lifecycle flushing, fetch transport helpers, and explicit W3C trace propagation helpers for browser-to-backend requests. Use a browser-scoped `clientKey` in public browser examples while preserving lower-level `apiKey` compatibility, use `addEventListener()` rather than replacing global handler properties, and do not suppress normal browser console/default behavior unless the user opts in. Prefer privacy-conscious metadata defaults: path without query string or hash, no document title or user agent unless explicitly enabled, and a `sanitizeMetadata()` hook. Browser trace propagation should use opt-in `tracePropagationTargets` strings, regular expressions, or functions, preserve existing request headers while replacing stale `traceparent` values, avoid global fetch patching, avoid adding tracing headers to unmatched URLs or unrelated origins, and document the CORS requirement for cross-origin APIs that accept `traceparent`. Prove the integration by packing both the core SDK and Browser package, installing them into a fresh npm app with a browser-like DOM runtime such as current `happy-dom`, exercising real `Window`/`ErrorEvent` dispatch, proving unhandled rejection capture with a `reason`, proving queued events flush on `pagehide` and hidden `visibilitychange`, proving listener removal through `uninstall()`, proving matched and unmatched trace-propagation fetch calls, typechecking a strict DOM TypeScript consumer, checking CommonJS require, validating canonical full-batch JSON separately from browser-specific capture payloads, and running shipped example launcher/helper commands from `node_modules`.

For React integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK and React, with provider, hook, handled-error, and scoped error-boundary APIs that do not own the app's React version. Prefer `clientKey` in public React examples while preserving lower-level `apiKey` compatibility, and expose explicit W3C trace propagation helpers for frontend-to-backend fetch calls without global fetch patching. React trace propagation should use opt-in `tracePropagationTargets` strings, regular expressions, or functions, preserve existing request headers while replacing stale `traceparent` values, avoid adding tracing headers to unmatched URLs or unrelated origins, and document the CORS requirement for APIs that accept `traceparent`. React error helpers should capture boundary `Error` values and app-owned non-`Error` handled failures, include React component-stack metadata by default when React supplies it, omit raw JavaScript stack text by default, and require explicit `includeStack` proof before stack metadata is sent. Prove the integration by packing both the core SDK and React package, installing them into a fresh package-manager-native app with current `react`, `react-dom`, and `react-test-renderer`, rendering provider/hook flows through a real React renderer, proving `LogBrewErrorBoundary` capture and fallback behavior through a real React renderer, proving handled Error and non-Error helper capture with stack text omitted by default and opt-in only, proving matched and unmatched trace-propagation fetch calls, typechecking a strict TypeScript consumer with React, error-helper, and trace target types, validating emitted event JSON against the shared contract/parity fixture, proving package-manager graph/list metadata, checking CommonJS require for the trace and error helpers, and running shipped example launcher/helper commands from `node_modules`.

For Node.js integrations specifically, keep the package a thin dependency-free layer over the core JavaScript SDK and Node's built-in `node:http` runtime. Prefer a wrapper such as `withLogBrewHttpHandler()` that can wrap a normal `http.createServer()` handler, attach request-local `req.logbrew`, capture completed requests from the response `finish` event, capture thrown or rejected handler errors, and send a conservative `500` fallback only when the app has not already written a response. Prefer `serverApiKey` plus `LOGBREW_SERVER_API_KEY` in public server examples while preserving lower-level `apiKey` and `LOGBREW_API_KEY` compatibility, and read env fallbacks through `globalThis.process?.env` so unusual runtimes fail with SDK configuration errors instead of `ReferenceError`. Default request capture should continue valid inbound W3C `traceparent` headers as request span events with a fresh child span id, expose a deterministic `spanIdFactory` for tests, fall back to the existing request log event when the header is absent or malformed so bad client headers do not break the server, and omit query text from automatic request/error metadata by default. Also provide a real outbound HTTP transport through built-in `fetch` so server apps do not need to invent their own delivery layer after leaving the recording examples. Prove the integration by packing both the core SDK and Node package, installing them into a fresh npm app with `@types/node` for strict TypeScript consumer checks, proving npm package graph/list metadata, starting real HTTP servers on ephemeral ports, exercising successful request handling, server-key setup and env fallback, automatic request capture, W3C `traceparent` request-span continuation, query omission, thrown-handler error capture, manual error capture, local HTTP intake delivery with retry behavior, CommonJS require, and packaged example launcher/helper commands from `node_modules`, while keeping the canonical six-event stdout JSON identical to the shared parity fixture and proving Node-specific request/error/transport helper behavior separately.

For Express integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK and Express, with request middleware, error-handler middleware, and request augmentation types that do not own the app's Express version. Prefer `serverApiKey` plus `LOGBREW_SERVER_API_KEY` in public server examples while preserving lower-level `apiKey` and `LOGBREW_API_KEY` compatibility, and read env fallbacks through `globalThis.process?.env` so unusual runtimes fail with SDK configuration errors instead of `ReferenceError`. Default request capture should continue valid inbound W3C `traceparent` headers as request span events with a fresh child span id, expose a deterministic `spanIdFactory` for tests, fall back to the existing request log event when the header is absent or malformed so bad client headers do not break the app, and omit query text from automatic request/error metadata by default. Prove the integration by packing both the core SDK and Express package, installing them into a fresh npm app with current `express` and `@types/express`, proving npm package graph/list metadata, starting a real Express server, exercising middleware through actual HTTP requests, validating emitted event JSON against the shared contract/parity fixture, proving server-key setup, proving automatic request capture, proving W3C `traceparent` request-span continuation, proving query omission, proving async error capture through a four-argument error handler that forwards to the app's own response handler, typechecking a strict TypeScript consumer that reads `req.logbrew` and narrows request event types, checking CommonJS require, and running shipped example launcher/helper commands from `node_modules`. If a packaged example uses separate request and error transports, keep request middleware scoped narrowly so error-only routes do not accidentally reuse the request transport from `req.logbrew`.

For Fastify integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK and Fastify, with a registerable plugin, `onRequest`, `onResponse`, and `onError` hook behavior, and request augmentation types that do not own the app's Fastify version. Prefer `serverApiKey` plus `LOGBREW_SERVER_API_KEY` in public server examples while preserving lower-level `apiKey` and `LOGBREW_API_KEY` compatibility, and read env fallbacks through `globalThis.process?.env` so unusual runtimes fail with SDK configuration errors instead of `ReferenceError`. Default request capture should continue valid inbound W3C `traceparent` headers as request span events with a fresh child span id, expose a deterministic `spanIdFactory` for tests, fall back to the existing request log event when the header is absent or malformed so bad client headers do not break the app, and omit query text from automatic request/error metadata by default. Wrap the plugin with `fastify-plugin` when the desired UX is `app.register(logbrewFastifyPlugin)` followed by normal route declarations, because a plain Fastify plugin is encapsulated and can otherwise surprise users by not decorating sibling routes. Prove the integration by packing both the core SDK and Fastify package, installing them into a fresh npm app with current `fastify`, proving npm package graph/list metadata, starting a real Fastify server, exercising hooks through actual HTTP requests, validating emitted event JSON against the shared contract/parity fixture, proving server-key setup, proving automatic request capture, proving W3C `traceparent` request-span continuation, proving query omission, proving async error capture before the app's own error handler sends the response, typechecking a strict TypeScript consumer that reads `request.logbrew` and narrows request event types, checking CommonJS require, and running shipped example launcher/helper commands from `node_modules`.

For NestJS integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK, `@nestjs/common`, and RxJS, with interceptor behavior that does not own the app's Nest version. Prefer `serverApiKey` plus `LOGBREW_SERVER_API_KEY` in public server examples while preserving lower-level `apiKey` and `LOGBREW_API_KEY` compatibility, and read env fallbacks through `globalThis.process?.env` so unusual runtimes fail with SDK configuration errors instead of `ReferenceError`. Prefer a global `LogBrewInterceptor` that attaches `request.logbrew` before the controller runs, captures successful controller completion, continues valid inbound W3C `traceparent` headers as request span events with fresh child span ids, exposes deterministic `spanIdFactory` hooks for tests, falls back to request log events for missing or malformed propagation, avoids invoking span id hooks before propagation is valid, omits query text from automatic request/error metadata by default, captures thrown route errors with RxJS `catchError`, and then rethrows so Nest's normal exception handling still controls the response. Prove the integration by packing both the core SDK and NestJS package, installing them into a fresh npm app with current `@nestjs/common`, `@nestjs/core`, `@nestjs/platform-express`, `reflect-metadata`, and `rxjs`, proving npm package graph/list metadata, starting real Nest HTTP apps, exercising decorated controllers through actual HTTP requests, validating emitted event JSON against the shared contract/parity fixture while setting `captureRequests: false` for canonical six-event examples, proving server-key setup, automatic request capture, W3C `traceparent` continuation, malformed-header log fallback, query omission, async error capture before Nest's own exception response, strict TypeScript request-event narrowing, checking CommonJS require, and running shipped example launcher/helper commands from `node_modules`.

For Angular integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK and `@angular/core`, with provider, injection-token, `injectLogBrew()`, view-helper, and `ErrorHandler` APIs that do not own the app's Angular version. Prefer app-level provider UX through `provideLogBrew()` so normal Angular `ApplicationConfig.providers` and dependency-injection flows work without a package-specific module. Prefer `clientKey` in public Angular browser examples while preserving lower-level `apiKey` and env compatibility, and read browser-facing env fallbacks through `globalThis.process?.env` so package import does not require a bundler-provided `process`. Expose explicit W3C trace propagation helpers for Angular frontend-to-backend fetch calls without global fetch patching. Angular trace propagation should use opt-in `tracePropagationTargets` strings, regular expressions, or functions, preserve existing request headers while replacing stale `traceparent` values, avoid adding tracing headers to unmatched URLs or unrelated origins, and document the CORS requirement for APIs that accept `traceparent`. Prove the integration by packing both the core SDK and Angular package, installing them into a fresh npm app with current `@angular/core`, `@angular/compiler`, `rxjs`, and `zone.js`, proving npm package graph/list metadata, exercising Angular's real DI APIs through `createEnvironmentInjector`, `runInInjectionContext`, and `inject`, validating emitted event JSON against the shared contract/parity fixture, proving view-helper behavior outside the canonical six-event JSON payload when parity expects the shared fixture shape, proving `ErrorHandler` capture while delegating to an app-owned handler, proving matched and unmatched trace-propagation fetch calls, typechecking a strict TypeScript consumer with trace target types, checking CommonJS require for the trace helpers, and running shipped example launcher/helper commands from `node_modules`. Watch current Angular `engines.node` requirements during smoke output because Angular releases may move faster than the Node version pinned in CI.

For Vue integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK and Vue 3, with plugin, app-level provide/inject, `$logbrew`, composable, view-helper, and component error-capture APIs that do not own the app's Vue version. Prefer preserving any existing `app.config.errorHandler` after telemetry capture so the SDK observes errors without hijacking normal app behavior. Prefer `clientKey` in public Vue browser examples while preserving lower-level `apiKey` and env compatibility, and read browser-facing env fallbacks through `globalThis.process?.env` so package import does not require a bundler-provided `process`. Expose explicit W3C trace propagation helpers for Vue frontend-to-backend fetch calls without global fetch patching. Vue trace propagation should use opt-in `tracePropagationTargets` strings, regular expressions, or functions, preserve existing request headers while replacing stale `traceparent` values, avoid adding tracing headers to unmatched URLs or unrelated origins, and document the CORS requirement for APIs that accept `traceparent`. Prove the integration by packing both the core SDK and Vue package, installing them into a fresh npm app with current `vue` and `@vue/server-renderer`, proving npm package graph/list metadata, rendering plugin and composable flows through Vue SSR, validating emitted event JSON against the shared contract/parity fixture, proving view-helper behavior outside the canonical six-event JSON payload when parity expects the shared fixture shape, proving component error capture while preserving the previous error handler, proving matched and unmatched trace-propagation fetch calls, typechecking a strict TypeScript consumer with trace target types and `types: ["node"]` when `@vue/server-renderer` is used under strict TypeScript, checking CommonJS require for the trace helpers, and running shipped example launcher/helper commands from `node_modules`.

For Svelte integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK and Svelte 5, with context helpers, `useLogBrew()`, view-helper events, and app-owned error capture helpers that do not own the app's Svelte version. Prefer `clientKey` in public Svelte browser examples while preserving lower-level `apiKey` and env compatibility, and read browser-facing env fallbacks through `globalThis.process?.env` so package import does not require a bundler-provided `process`. Expose explicit W3C trace propagation helpers for Svelte frontend-to-backend fetch calls without global fetch patching. Svelte trace propagation should use opt-in `tracePropagationTargets` strings, regular expressions, or functions, preserve existing request headers while replacing stale `traceparent` values, avoid adding tracing headers to unmatched URLs or unrelated origins, and document the CORS requirement for APIs that accept `traceparent`. Prove the integration by packing both the core SDK and Svelte package, installing them into a fresh npm app with current `svelte`, proving npm package graph/list metadata, compiling real Svelte server components through `svelte/compiler`, rendering with `svelte/server`, validating emitted event JSON against the shared contract/parity fixture, proving context setup and missing-context failures, proving Svelte error capture while leaving boundary/hook ownership to the app, proving matched and unmatched trace-propagation fetch calls, typechecking a strict TypeScript consumer with trace target types, checking CommonJS require for the trace helpers, and running shipped example launcher/helper commands from `node_modules`. For Svelte 5, remember that `render()` returns a lazy output object, so smoke tests must read `render(...).html` to trigger component execution and context errors.

For React Native integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK, React, and React Native, with provider/hook APIs plus mobile helpers for screen views, app-state changes, and app-owned handled error capture. Prefer `clientKey` in public mobile examples while preserving lower-level `apiKey` compatibility, and expose explicit W3C trace propagation helpers for mobile fetch calls without global fetch patching. React Native trace propagation should use opt-in `tracePropagationTargets` strings, regular expressions, or functions, preserve existing request headers while replacing stale `traceparent` values, avoid adding tracing headers to unmatched URLs or unrelated origins, and document backend/gateway allow-listing for `traceparent`. React Native error helpers should accept `Error` and non-`Error` values from app-owned error boundaries, async catch blocks, route handlers, or global handlers, capture platform/app-state/screen context, omit stack text by default, and require explicit `includeStack` proof before stack metadata is sent. Ship a Metro-facing `react-native` package entry that can import `AppState` and `Platform`, but keep the default Node entry dependency-injected because React Native source packages are not directly importable in plain Node. Prove the integration by packing both the core SDK and React Native package, installing them into a fresh npm app with current `react`, `react-native`, and `react-test-renderer`, proving npm package graph/list metadata, proving the shipped `react-native` manifest field and native entry payload, rendering provider/hook flows with `react-test-renderer`, proving matched and unmatched trace-propagation fetch calls, proving handled `Error` and non-`Error` issue capture with stack text omitted by default and opt-in only, typechecking a strict TypeScript consumer with React Native app-state, error-helper, and trace target types, validating mobile event JSON against the shared contract, proving screen-view and app-state helper behavior, checking CommonJS require plus default export parity, and running shipped example launcher/helper commands from `node_modules`.

For Next.js integrations specifically, keep the package a thin peer-dependency layer over the core JavaScript SDK and Next/React rather than owning the app's framework version. Prefer App Router Route Handler helpers that return standard `Response` objects, capture successful route responses by default, continue valid inbound W3C `traceparent` headers as request span events with fresh child span ids, expose deterministic `spanIdFactory` and `requestIdFactory` hooks for tests, fall back to request log events for missing or malformed propagation, avoid invoking span id hooks before propagation is valid, and report telemetry failures through `onCaptureError` without changing successful route responses or replacing the app's thrown route error. Prefer `serverApiKey` plus `LOGBREW_SERVER_API_KEY` in Next server examples while preserving lower-level `apiKey` and `LOGBREW_API_KEY` compatibility, and read env fallbacks through `globalThis.process?.env` so unusual runtimes fail with SDK configuration errors instead of `ReferenceError`. Omit query text from automatic request and error metadata by default, and require explicit `includeSearchParams: true` proof before query strings appear in route error metadata. Avoid `NextResponse.next()` inside route handlers, and keep Next.js 16's `proxy.js` naming change in the docs so new examples do not teach deprecated middleware conventions. Prove the integration by packing both the core SDK and Next package, installing them into a fresh npm app with the current `next`, `react`, and `react-dom`, proving npm package graph/list metadata, building a real App Router app with `next build`, invoking the installed route handler wrapper through standard `Request`/`Response`, validating emitted event JSON against the shared contract/parity fixture while setting `captureRequests: false` for canonical six-event examples, proving server-key setup, retry-backed flushing, error capture, default request capture, W3C `traceparent` continuation, malformed-header log fallback, query omission plus opt-in query capture, safe capture-failure callback behavior, strict TypeScript request-event narrowing, and running shipped example launcher/helper commands from `node_modules`.

For Python packages specifically, prefer `pyproject.toml`-based packaging with a smoke path that installs both a built wheel and built source distribution into fresh virtual environments through `pip`, inspects both built archives before install, proves standard installed-package metadata from those artifacts, including `pip show`, `pip show -f`, `pip list --format=json`, `pip freeze`, and pip-written `INSTALLER`, `direct_url.json`, `--report`, and `pip inspect` provenance records, including the expected plain `pip show` package summary fields, proves the installed environments stay clean under `python -m pip check`, proves repo Python source syntax with a dependency-free in-memory compile check such as `python3 scripts/check_python_sources.py` before focused package tests, then proves repo package, examples, tests, and checker scripts through temp-installed Ruff and strict Mypy such as `bash scripts/check_python_static.sh` using exact tool versions plus temp-owned Ruff and Mypy caches so static-analysis strictness does not dirty the public tree. Include a dependency-free HTTP transport for server-side delivery when the core SDK has a transport protocol: use Python's standard-library HTTP stack, configurable endpoint/header/timeout settings, SDK-key delivery through the `authorization` header, status-code mapping for client retry logic, retryable transport errors for connection failures, and a test seam for local intake proof. Prove that both wheel and source-distribution paths survive a clean `python -m pip uninstall -y <package>` removal before reinstalling the same artifact, prove a consumer-owned command surface such as a temp `Makefile` can wrap the installed-user typecheck, unittest, README-example, packaged-example, packaged examples list, packaged examples help, packaged examples entrypoint, packaged real-user example, and happy-path smoke commands on the main install and reinstall paths, prove that plain `make` from that temp project prints copy-pasteable `make smoke-...` commands, with the shorter `make smoke-run` path labeled explicitly as the real-user smoke flow, prove that the packaged examples surface is discoverable through `python -m <package>.examples --help`, `python -m <package>.examples --list`, and named selection through the same entrypoint, with both `--help` and `--list` printing copy-pasteable packaged-example commands, including explicit named README-example and real-user-smoke entrypoint commands, explicit direct example-module commands, and the default no-argument entrypoint being labeled explicitly as the real-user smoke path, instead of only generic argument help or bare example names, and prove that the packaged README teaches those example-entrypoint and direct example-module commands explicitly instead of leaving them implicit in longer prose, prove that the generated freeze output can recreate each install in a fresh virtual environment, prove that a one-line direct requirements file derived from the generated `pip freeze` output reinstalls cleanly under `python -m pip install --require-hashes -r ...`, prove expected installed payload files such as `py.typed`, packaged example modules, and dist-info metadata plus installed README-derived guidance in `METADATA`, prove pip's own installed file listing for key module and dist-info payloads, prove pip's structured installed-package list for the expected package entry and version, prove the installed module, public payload shape types, primary class, public SDK errors, transport, transport errors, response types, and lifecycle method docstrings through normal Python introspection, prove field-level typing metadata for key public response or transport attributes, prove the shipped typing metadata from those artifacts in a typed consumer through a temp `pyproject.toml`-driven typecheck config, prove standard-library `logging.Handler` support without global logging side effects, including level mapping, logger name capture, primitive `extra` metadata, exception name/message metadata, and no full source path by default, prove installed HTTP delivery against a local intake with retry behavior, and do that before any commit or release.

For Python web framework integrations such as FastAPI, Flask, and Django, keep the integration as a separate typed package that depends on the core Python SDK, not as hidden framework-specific behavior inside the core client. Default request spans should continue valid inbound W3C `traceparent` headers using fresh child span ids, expose deterministic `span_id_factory` hooks for tests, fall back to the existing synthetic request span behavior for missing or malformed propagation, avoid invoking span id hooks before propagation is valid, and omit query text from automatic request metadata by default. Prove middleware installation from a fresh app with the framework's current test client, package the examples that users can run after install, validate emitted request span and exception issue events against the shared contract, prove trace continuation plus malformed-header fallback from installed artifacts, and pin any framework test-client dependency exposed by current releases, such as Starlette's `httpx2` requirement, in the integration package metadata instead of only in smoke scripts.

For Flask integrations specifically, prefer explicit app-owned `before_request`, `after_request`, `teardown_request`, and `got_request_exception` hooks over global Flask monkeypatching. Prove route-template grouping with `request.url_rule.rule`, active request trace availability to handlers and `LogBrewLoggingHandler`, valid W3C child-span continuation, malformed `traceparent` fallback without raw header capture, default transport-error swallowing, loud `raise_flush_errors=True` behavior under Flask testing mode, packaged example commands, and strict typed consumer checks.

For Django integrations specifically, implement both the normal middleware call path and `process_exception()` because Django may convert view exceptions into 500 responses instead of letting `__call__` observe the original exception. In packaged examples, avoid `ROOT_URLCONF=__name__` when launching through `python -m <package>.examples`; register a tiny importable URLconf module instead so Django does not re-import the executing example and double-configure settings. Prove the integration against current Django through `django.test.Client`, including successful request span capture, W3C trace continuation, malformed-header fallback, exception issue capture, final error-span capture, default transport-error swallowing, loud `raise_flush_errors=True` behavior, packaged example commands, and strict typed consumer checks.

For Rust crates specifically, prefer a smoke path that creates a fresh Cargo app, inspects the packaged `.crate` artifact so shipped README guidance, core `Cargo.toml` metadata, and the packaged example surface are proven, including both a runnable `examples/readme_example.rs` file and the stronger `examples/real_user_smoke.rs` file plus a tiny `examples/Makefile` wrapper, runs the shipped README example directly from the extracted crate artifact through raw `cargo run --example ...` and through a helper command such as `make run-readme-example`, runs the stronger shipped example both through raw `cargo run --example ...` and the shipped helper commands, proves that helper surface is discoverable enough for plain `make` to print copy-pasteable commands for both example flows while still labeling the shorter `make run` alias explicitly as the real-user smoke path, proves that the repo-checkout README-taught local `cargo run --example readme_example`, raw `cargo run --example real_user_smoke`, plain `make`, helper-backed `make run-readme-example`, helper-backed `make run`, and helper-backed `make run-real-user-smoke` flows are exercised directly in a focused checkout check instead of relying only on the broader packaged-artifact smoke, proves `cargo fmt --check`, strict `cargo clippy --all-targets -- -D warnings`, `cargo clippy --all-targets --features http -- -D warnings`, `cargo test`, and `cargo test --features http` in the public verifier before release, and proves that the packaged README teaches both that short alias and the explicit `make run-real-user-smoke` target instead of leaving the named helper path implicit, before the helper paths execute the examples, proves a separate lifecycle app can add the extracted crate through `cargo add --path`, remove it cleanly with `cargo remove <crate>`, verify that `Cargo.toml`, `Cargo.lock`, locked metadata, and the dependency tree all drop the crate, and then add it back again, adds the extracted packaged crate contents to the main temp app through `cargo add --path` instead of editing the manifest by hand, resolves the dependency graph so the rewritten `Cargo.toml`, `Cargo.lock`, including the expected temp-app and local crate entries, `cargo pkgid` output for the resolved crate identity, locked Cargo metadata with checked package identity/version plus the resolved root package and exact temp-app dependency edge to the crate, a locked dependency fetch, locked `cargo check`, `cargo build`, and `cargo test` commands, a dependency-tree view that explicitly shows the temp app root and direct crate child, and crate plus primary symbol docs through `cargo doc --locked --package <crate> --no-deps` are produced, proves that later run steps also succeed under `--locked`, including through a temp `.cargo/config.toml` alias layer that wraps key installed-user flows, including public SDK errors, transport traits, response types, public batch-shape structs, public event builders, field-level docs for key response or payload structs, queue helpers, and lifecycle helpers such as preview, flush, and shutdown, proves that a temp binary mirroring the published README example also runs under `--locked`, and then runs the app through the public API. Include optional blocking HTTP delivery when the core SDK has a transport protocol: keep the default crate lightweight, put the transport behind a Cargo feature, support endpoint/header/timeout/app-owned agent settings, pass the SDK key through the `authorization` header, return HTTP statuses to retry logic, map network failures to retryable transport errors, render feature-enabled docs for the HTTP symbols, and prove installed-artifact delivery against a local intake with 5xx-to-2xx retry behavior.

For Go modules specifically, prefer a smoke path that creates a fresh temporary module through `go mod init`, resolves the SDK through a normal module path from a proxy-style module artifact rather than only a workspace `replace`, proves the proxy `.info`, `.mod`, and `.zip` files plus the installed module cache keep expected metadata, shipped example files, and README install plus helper guidance, includes both a shipped README-style example file and a stronger shipped `real_user_smoke` example file in that artifact, runs the shipped README example directly from the extracted module artifact through raw `go run`, runs the stronger shipped example both through raw `go run` and a shipped example-local `Makefile`, proves that the repo-checkout README-taught local `go run ./examples/readme_example`, raw `go run ./examples/real_user_smoke`, plain `make`, helper-backed `make run-readme-example`, helper-backed `make run`, and helper-backed `make run-real-user-smoke` flows are exercised directly in focused tests instead of relying only on the broader proxy-artifact smoke, proves repo-checkout formatting with `test -z "$(gofmt -l .)"`, vetting with `go vet ./...`, tests with `go test ./...`, and temp-installed Staticcheck such as `bash scripts/check_go_static.sh` in the public verifier before release, and include dependency-free W3C `traceparent` helpers plus a dependency-free `net/http` transport for server-side delivery when the core SDK has a transport protocol. Go W3C helpers should validate shape, reject version `ff` and all-zero trace/span ids, normalize ids, expose sampled flags, create outgoing `traceparent` values from explicit ids, derive LogBrew span attributes with primitive-only metadata, and be proven from installed modules through `go doc`, readonly tests, and a temp app that covers valid continuation and malformed-header rejection. The Go HTTP transport should support configurable endpoint, headers, custom `*http.Client`, timeout defaults, SDK-key delivery through the `authorization` header, response body draining/closing, HTTP status mapping for client retry logic, and retryable network errors for client delivery failures. Prove that shared helper surface is discoverable enough for plain `make` to print copy-pasteable `make run-readme-example`, `make run`, and `make run-real-user-smoke` commands before the README example runs through `make run-readme-example` and the stronger path runs through `make run` or `make run-real-user-smoke`, and prove that the packaged README teaches both the short `make run` alias and the explicit `make run-real-user-smoke` target instead of leaving the named helper path implicit, prove a separate temp lifecycle module can remove the SDK with `go get <module>@none` before adding it back with `go get <module>@<version>`, adds the SDK through `go get`, runs `go mod tidy`, proves the temp app keeps the expected `go.mod` requirement and `go.sum` hash entries, proves the downloaded module cache passes `go mod verify`, proves Go's own `go mod download -json` output keeps the expected info, mod, zip, extracted-dir, and checksum metadata for the installed SDK artifact, proves both `go list -m all` and `go list -m -json all` keep the expected temp-app root plus installed SDK module entry, proves `go list -deps -json ./...` keeps the expected temp-app package entry plus the installed SDK dependency package metadata, proves a built temp-app binary keeps the expected embedded module metadata through `go version -m`, proves that later `go build`, `go test`, `go vet`, `go list`, `go doc`, and `go run` steps succeed under `-mod=readonly`, including through a temp `Makefile` that wraps the key readonly installed-user flows, proves that a temp program mirroring the published README example also runs under `-mod=readonly`, proves module and dependency metadata through `go list`, proves the package and exported symbol documentation surface through `go doc`, including public payload structs, the public event shape, public SDK errors, W3C trace context types and helpers, transport interfaces, HTTP transport config and constructor docs, response and error types, field-level docs for core config and transport types, helper entrypoints, preview, and lifecycle methods, proves installed HTTP delivery against a local intake with retry behavior, and then runs a tiny app through `go run`.

For C packages specifically, prefer a dependency-free C99 source/header package with a small public header, explicit allocation/free ownership, stable error code/message fields, and a callback transport so users can embed the SDK without a package-manager requirement. Compile source, tests, and examples with `-std=c99 -Wall -Wextra -Wpedantic -Werror`, inspect a source tarball containing README, Makefile, header, source, tests, and shipped examples, then install that archive into a fresh temp native app under a vendor directory. Prove add/remove/re-add of the source package directory, compile the temp app against the installed header/source, validate emitted JSON against the shared contract/parity fixture, keep helper `make` output discoverable while preserving JSON-only stdout for runnable example targets, and exercise success, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, and post-shutdown rejection.

For C++ packages specifically, prefer a dependency-free C++17 source/header package with RAII ownership, stable `SdkException::code()` values, an abstract transport surface, and a deterministic recording transport for examples and tests. Compile source, tests, and examples with `-std=c++17 -Wall -Wextra -Wpedantic -Werror`, inspect a source tarball containing README, Makefile, header, source, tests, and shipped examples, then install that archive into a fresh temp native app under a vendor directory. Prove source package add/remove/re-add, compile the temp app against the installed header/source, validate emitted JSON against the shared contract/parity fixture, keep helper `make` output discoverable while preserving JSON-only stdout for runnable example targets, and exercise success, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, and post-shutdown rejection.

For Objective-C packages specifically, prefer a Foundation-based source/header package that works for Apple and mixed Swift/Objective-C apps without third-party runtime dependencies. Compile SDK source, tests, and examples with ARC plus `-Wall -Wextra -Wpedantic -Werror`, expose stable `NSError` domain/user-info codes, inspect a source tarball containing README, Makefile, header, source, tests, and shipped examples, then install that archive into a fresh temp native app under a vendor directory. Prove source package add/remove/re-add, compile the temp app against the installed header/source with `-framework Foundation`, validate emitted JSON against the shared contract/parity fixture, keep helper `make` output discoverable while preserving JSON-only stdout for runnable example targets, and exercise success, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, and post-shutdown rejection.

For Java packages specifically, prefer a JDK-toolchain baseline when Maven or Gradle are unavailable locally: compile SDK, tests, and examples with `javac -Xlint:all -Werror --release 11`, run temp-owned SpotBugs bytecode analysis such as `bash scripts/check_java_static.sh` with pinned distribution checksum verification, run package tests, generate Javadocs with doclint failures treated as errors, build binary and source jars with `jar`, inspect the jar payload for package classes, Maven metadata, README guidance, shipped examples, and helper `Makefile`, and prove the shipped README-style and stronger real-user examples against the packaged jar and extracted source artifact. Include dependency-free HTTP delivery when the core SDK has a transport protocol: use Java 11 `java.net.http.HttpClient`, configurable endpoint/header/custom client/timeout settings, SDK-key delivery through the `authorization` header, discarded response bodies, HTTP status mapping for client retry logic, and retryable transport errors for client delivery failures. Include a zero-dependency `java.util.logging.Handler` bridge before heavier Java logging-framework integrations when it improves adoption without owning the app's SLF4J/Logback/Log4j choices; it should be attachable only by app code, map JUL levels consistently, capture logger/source/thread/sequence/exception metadata, avoid mutating root logger configuration, and omit full stack traces unless explicitly enabled. Add optional SLF4J/Logback support when it can remain app-owned: use optional dependency metadata, checksum-verified temp Logback artifacts in checks, capture logger name, native level, thread, MDC, markers, fluent key/value pairs, and exception type/message, omit stack text unless explicitly enabled, and prove capture from a real app-owned Logback logger. Because Spring Boot starters default to Logback, also prove the packaged SDK inside a fresh Gradle Spring Boot app without adding a required Spring dependency to the SDK. The smoke path should create fresh temp classpath apps, prove add/remove/re-add behavior by compiling with and without the jar, validate emitted stdout against the shared contract/parity fixture, exercise successful send, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, post-shutdown rejection, installed-artifact HTTP delivery against a local intake with retry behavior, installed-artifact JUL handler capture from a real logger, installed-artifact Logback appender capture from a real SLF4J logger, and installed-artifact Spring Boot Logback capture with MDC and SLF4J key/value metadata.

For .NET packages specifically, prefer a `netstandard2.0` library package when broad app compatibility matters, with nullable reference types enabled, warnings treated as errors, built-in .NET analyzers enabled, `AnalysisMode=All`, code-style enforcement in build, and analyzer warnings treated as errors. Pack a real NuGet `.nupkg`, inspect the packaged DLL, README, examples, helper files, and package metadata, then install that package into fresh console apps instead of only referencing the source project. Include `System.Net.Http` delivery when the core SDK has a transport protocol: support configurable endpoint/header/custom client/timeout settings, SDK-key delivery through the `authorization` header, response disposal, HTTP status mapping for client retry logic, and retryable transport errors for request/time-out failures. Include an opt-in `Microsoft.Extensions.Logging` provider when it improves adoption without replacing app-owned providers; it should register through `ILoggingBuilder`, respect normal `LoggerFactory` filtering, materialize structured state inside synchronous `ILogger.Log`, capture category, native level, event id/name, primitive structured values, primitive `BeginScope` values, and exception type/message, omit stack text unless explicitly enabled, and queue by default unless a transport plus flush-on-log behavior is configured. Prove package-manager-native add/remove/re-add behavior, shipped README-style and stronger real-user examples, installed-artifact HTTP delivery against a local intake with retry behavior, installed-artifact `ILogger` capture from a real `LoggerFactory`, emitted JSON validation/parity, successful send, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, and post-shutdown rejection. Keep `bin` and `obj` output out of the public worktree through script-owned artifact removal and ignore rules.

For Unity packages specifically, prefer a source-only UPM package with `package.json`, an assembly definition, README guidance, `Samples~` examples, and a helper surface that can be tested without the Unity Editor. Keep Unity helpers thin over the C# event client, avoid requiring `UnityEngine` for core serialization and retry behavior, and add engine context through explicit helper APIs for scene, log, exception, frame, platform, object, and session metadata. Compile the analyzer harness with nullable reference types, warnings as errors, built-in .NET analyzers, `AnalysisMode=All`, code-style enforcement in build, and analyzer warnings treated as errors, but document narrow SDK-aware exceptions where app-oriented analyzer rules conflict with public UPM APIs or older Unity profile compatibility. Include dependency-free HTTP delivery when the core SDK has a transport protocol: use `System.Net.Http.HttpClient`, configurable endpoint/header/custom client/timeout/requester settings, SDK-key delivery through the `authorization` header, JSON content type, HTTP status mapping for client retry logic, and retryable transport errors for request/time-out failures. Prove null-argument fail-fast behavior, the packed `.tgz` payload, installed `Packages/` layout, dependency add/remove/re-add behavior through a temp project manifest, shipped sample execution, emitted JSON validation/parity, Unity helper metadata, installed-artifact HTTP delivery against a local intake with retry behavior, successful send, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, and post-shutdown rejection.

For Kotlin packages specifically, prefer Kotlin/JVM source with Android-friendly helpers that do not import Android SDK classes unless a separate Android SDK verifier is available. Run pinned ktlint from a temp-owned artifact such as `bash scripts/check_kotlin_style.sh`, verify the downloaded linter checksum, compile with `kotlinc -Werror`, prove forward-compatibility warnings are fixed structurally instead of suppressed, build normal jars with Maven metadata, README guidance, and shipped examples, and install them into fresh temp Kotlin apps from a local Maven-style repository. Include dependency-free HTTP delivery when the core SDK has a transport protocol: use JDK `HttpURLConnection`, configurable endpoint/header/connect-timeout/read-timeout/requester settings, SDK-key delivery through the `authorization` header, JSON content type, HTTP status mapping for client retry logic, and retryable transport errors for request/time-out failures. Android logging helpers should mirror Android `Log` priority integers without importing Android classes, record tags as logger names, capture primitive context metadata, capture throwable type/message, and keep stack text opt-in. Keep OkHttp out of the core Kotlin artifact; optional `co.logbrew:logbrew-kotlin-okhttp` checks must prove a one-interceptor app-owned OkHttp flow, exactly one normalized `traceparent`, request child trace scoping, response status/duration capture, original exception rethrow, capture-failure reporting without breaking the HTTP call, and no request/response body, arbitrary header, full URL, query, fragment, baggage, or tracestate capture. Prove Gradle dependency resolution, dependency add/remove/re-add behavior, shipped README-style and stronger real-user examples, emitted JSON validation/parity, installed-artifact HTTP delivery against a local intake with retry behavior, installed-artifact OkHttp interceptor capture, Android helper metadata for activity, screen, Android `Log` priority-style message, throwable-safe issue capture, legacy logcat-style message, device, OS, and session context, successful send, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, and post-shutdown rejection.

For Ruby gems specifically, prefer pure standard-library runtime code with a normal `.gemspec`, `ruby -w -c` syntax/warning checks, dependency-free tests, RDoc generation for public classes, `gem build --strict`, and direct gem archive inspection through `gem unpack`. Include dependency-free `Net::HTTP` delivery when the core SDK has a transport protocol: support configurable endpoint/header/timeout settings, SDK-key delivery through the `authorization` header, HTTP status mapping for client retry logic, and retryable transport errors for request/time-out failures. Include standard `Logger` support when it improves adoption: subclass or wrap Ruby's stdlib logger without global side effects, respect severity thresholds and lazy block messages, map Ruby severities consistently, capture primitive metadata, `progname`, and exception type/message, omit exception backtrace text unless explicitly enabled, queue by default, and prove the installed gem through real logger calls. Include Rack/Rails-compatible middleware when Ruby framework adoption matters: keep Rails and Rack app-owned instead of mandatory gem dependencies, wrap any Rack-shaped `call(env)` app, capture successful responses as spans, capture unhandled app exceptions as issue plus error-span events, re-raise app exceptions so framework behavior stays normal, omit query text and exception backtrace text by default, queue by default, and prove installed middleware behavior from the packaged gem. Add a separate Rails error reporter subscriber when handled/manual reports matter: implement the `report(error, handled:, severity:, context:, source:, **options)` shape, capture handled state, severity, source, primitive context, and exception metadata, avoid stack text by default, document using it beside Rack middleware without double capture, and prove it from the installed gem. The smoke path should install the built `.gem` into a fresh `GEM_HOME`, prove `gem list`, `require "logbrew"`, installed README/example/helper payloads, shipped README-style and stronger real-user examples, package-manager-native uninstall and reinstall, and a fresh temp app that validates stdout against the shared contract/parity fixture while exercising installed HTTP delivery against a local intake with retry behavior, installed `LogBrew::Logger` capture, installed `LogBrew::RackMiddleware` request/error capture, installed `LogBrew::RailsErrorSubscriber` handled/manual report capture, successful send, empty flush, validation failure, unauthenticated response, retry recovery, retry-budget exhaustion, non-retryable transport-status handling, graceful shutdown, and post-shutdown rejection.

For Swift packages specifically, prefer a SwiftPM-native package with a library product, README-style executable example, stronger retry-backed real-user example, Swift Testing coverage, SwiftFormat plus SwiftLint style enforcement through a focused script such as `bash scripts/check_swift_style.sh`, source-archive inspection through `swift package archive-source`, and an automated smoke path that creates a fresh temporary SwiftPM executable app with a normal package dependency on the SDK product. Treat SwiftLint violations as errors with `--strict`, keep SwiftFormat and SwiftLint configuration inside the package, prefer structural fixes such as splitting large source files over disabling maintainability rules, and explicitly configure away formatter/linter disagreements that do not improve user outcomes. Include dependency-free HTTP delivery when the core SDK has a transport protocol: use `URLSession`, configurable endpoint/header/timeout/requester settings, SDK-key delivery through the `authorization` header, JSON content type, HTTP status mapping for client retry logic, and retryable transport errors for request/time-out failures. Include an opt-in logger facade for Swift and Apple-platform apps when it improves adoption: mirror common Apple logging levels, record category/logger context, preserve subsystem/category metadata, generate ids and timestamps by default, keep app logging calls non-throwing, and report capture failures through an app-owned error callback. Prove `swift build`, `swift test`, the source archive payload, shipped `examples/Makefile` discovery and runtime commands, emitted JSON validation/parity, SwiftPM `describe` and `show-dependencies` metadata, installed-temp-app logger capture, installed-temp-app HTTP delivery against a local intake with retry behavior, and temp-app `swift run` execution through isolated scratch paths so the verifier does not dirty the public worktree.

For PHP packages specifically, prefer a smoke path that creates a fresh Composer project through `composer init`, proves repo-checkout PHP source syntax with a dependency-free check such as `python3 scripts/check_php_sources.py` using `php -l` before focused package tests, then proves repo source, shipped examples, and package tests through temp-installed PHPStan at `level=max` such as `bash scripts/check_php_static.sh`. Keep PHPStan and Composer state in temp directories, give PHPStan an explicit memory limit for deterministic local runs, and use `treatPhpDocTypesAsCertain: false` when public shaped-array PHPDoc is used as static consumer guidance while runtime validation still accepts raw user arrays. Include dependency-free HTTP delivery when the core SDK has a transport protocol: use PHP stream context HTTP support, post JSON, pass the SDK key through the `authorization` header, support endpoint/header/timeout/requester settings, handle modern `http_get_last_response_headers()` while preserving PHP 8.2 compatibility, return HTTP statuses to retry logic, and map request failures to retryable transport errors. Include PSR-3 logger support when it improves adoption: depend on `psr/log`, implement `LoggerInterface`, map PSR levels consistently, interpolate placeholders, capture primitive context values, capture exception type/message, omit exception trace text unless explicitly enabled, queue by default, and prove the installed artifact through a real Composer app. Include optional Monolog/Laravel support when PHP framework adoption matters: keep Monolog app-owned instead of a mandatory SDK dependency, extend a normal Monolog handler, capture channel/level/template/context/extra/exception metadata safely, document Laravel `config/logging.php` channel wiring, avoid interrupting app logging by default, and prove real installed Monolog calls through Composer scripts before and after reinstall. Inspect the packaged archive itself, add that archive through `composer require` against a Composer artifact repository, prove the generated consumer project stays valid under `composer validate`, prove normal Composer install artifacts and package metadata through the rewritten root `composer.json`, plain `composer show`, `composer show --format=json`, `composer why`, `composer licenses --format=json`, `composer.lock`, `vendor/composer/installed.json`, and generated autoload/version helpers under `vendor/composer/`, including the expected human-facing `composer show` summary fields for package name, description, selected version, type, MIT license, artifact zip, install path, autoload block, PHP requirement, and `psr/log` dependency, prove the temp project also survives a package-manager-native `composer remove <vendor>/<package>` removal before the artifact is required again, prove the direct consumer dependency edge through Composer's own graph output, prove the installed package license through Composer's own license report, prove the structured Composer package view for description, type, dist, install path, autoload, and PHP requirement metadata, prove that the generated lockfile can recreate the install through a clean `composer install`, prove that `composer dump-autoload --optimize` preserves the SDK autoload surface after install and reinstall, prove a consumer-owned Composer script surface can wrap the installed-user test, static-analysis, README-example, PSR-3 logger script, Monolog handler script, HTTP transport script, happy-path smoke run, and shipped vendor-example commands before and after reinstall, prove expected installed payload files and manifest fields in `vendor/`, prove archive and installed README install and placeholder guidance plus both a shipped README-style example file and a stronger shipped vendor example file plus a tiny shipped example-local helper such as `examples/Makefile`, prove that the repo-checkout local `php examples/readme_example.php`, raw `php examples/real_user_smoke.php`, plain `make`, helper-backed `make run-readme-example`, helper-backed `make run`, and helper-backed `make run-real-user-smoke` flows are exercised directly in a focused package check instead of relying only on the broader installed-artifact smoke, prove that the shipped README example runs directly from the installed package through raw `php`, prove that helper gives users one discoverable surface for both flows by printing copy-pasteable `make run-readme-example`, `make run`, and `make run-real-user-smoke` commands before the README example runs through `make run-readme-example` and the stronger real-user path runs through `make run` or `make run-real-user-smoke`, and prove that the packaged README teaches both the short `make run` alias, the explicit `make run-real-user-smoke` target, and the raw installed `php vendor/.../readme_example.php` plus `php vendor/.../real_user_smoke.php` commands instead of leaving any of those paths implicit, prove the shipped PHPDoc payload-shape aliases plus the client, PSR-3 logger, Monolog handler, SDK error, transport interface, `HttpTransport`, recording transport, response, lifecycle method surface, and key public property docs through reflection and shaped-array consumer experience with static analysis, prove installed HTTP delivery against a local TCP intake with retry behavior, run tiny scripts, and validate the emitted batch JSON against the shared contract.

## Release checks

- Shared release metadata: `python3 scripts/check_release_metadata.py`
- SDK-originated backend contract reports: `python3 scripts/check_backend_contract_reports.py`
- GitHub release safety settings before publishing: `python3 scripts/check_github_release_safety.py`
- JavaScript release-artifact dry-run proof: `bash scripts/real_user_js_release_artifact_smoke.sh`
- JavaScript release-artifact installed CLI prep/manifest/frame proof: `bash scripts/real_user_js_release_artifact_cli_smoke.sh`
- Vite release-artifact installed plugin proof: `bash scripts/real_user_vite_release_artifact_smoke.sh`
- Next.js release-artifact installed helper proof: `bash scripts/real_user_next_release_artifact_smoke.sh`
- React Native release-artifact installed helper/build proof: `bash scripts/real_user_react_native_release_artifact_smoke.sh`
- React Native native release-artifact proof: `bash scripts/real_user_react_native_native_release_artifact_smoke.sh`
- JavaScript release-artifact upload proof: `bash scripts/real_user_js_release_artifact_upload_smoke.sh`
- Native/mobile release-artifact dry-run proof: `bash scripts/real_user_native_release_artifact_smoke.sh`
- Native/mobile release-artifact upload proof: `bash scripts/real_user_native_release_artifact_upload_smoke.sh`
- Public registry versions after real publishes: `python3 scripts/check_registry_publication.py --target all`; for Maven verification while optional artifacts are still pending publication, pass `--maven-artifact` and `--maven-version` only for the currently public artifacts.
- Shared Markdown docs: `python3 scripts/check_markdown_links.py`
- Shared shell smoke/package scripts: `bash scripts/check_shell_static.sh`
- JavaScript, Browser, Node.js, Express, Fastify, NestJS, Angular, Vue, Svelte, React, React Native, or Next.js: `python3 scripts/check_js_sources.py`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, package `npm test`, framework smoke scripts, and `npm pack --dry-run`
- Python, FastAPI, Flask, and Django: `python3 scripts/check_python_sources.py`, `bash scripts/check_python_static.sh`, `bash scripts/check_fastapi_package.sh`, `bash scripts/check_flask_package.sh`, `bash scripts/check_django_package.sh`, `bash scripts/real_user_fastapi_smoke.sh`, `bash scripts/real_user_flask_smoke.sh`, `bash scripts/real_user_django_smoke.sh`, `python -m build`, and `twine check dist/*`
- Rust: `cargo publish --dry-run`
- Go: `test -z "$(gofmt -l .)"`, `go vet ./...`, `go test ./...`, and `bash scripts/check_go_static.sh`
- C: `bash scripts/check_c_package.sh` and `bash scripts/real_user_c_smoke.sh`
- C++: `bash scripts/check_cpp_package.sh` and `bash scripts/real_user_cpp_smoke.sh`
- Objective-C: `bash scripts/check_objc_package.sh` and `bash scripts/real_user_objc_smoke.sh`
- Java: `bash scripts/check_java_static.sh`, `bash scripts/check_java_package.sh`, `bash scripts/real_user_java_smoke.sh`, `bash scripts/real_user_java_queue_trace_smoke.sh`, and `bash scripts/real_user_spring_boot_smoke.sh`
- .NET: `bash scripts/check_dotnet_package.sh` and `bash scripts/real_user_dotnet_smoke.sh`
- Unity: `bash scripts/check_unity_package.sh` and `bash scripts/real_user_unity_smoke.sh`
- Kotlin: `bash scripts/check_kotlin_style.sh`, `bash scripts/check_kotlin_package.sh`, and `bash scripts/real_user_kotlin_smoke.sh`
- Ruby: `bash scripts/check_ruby_package.sh` and `bash scripts/real_user_ruby_smoke.sh`
- Swift: `bash scripts/check_swift_style.sh` and `bash scripts/check_swift_package.sh`
- PHP: `python3 scripts/check_php_sources.py`, `bash scripts/check_php_static.sh`, and `composer validate`

For Go modules specifically, prefer a smoke path that creates a fresh temp module through `go mod init`, generates or fetches a real proxy artifact, proves the proxy `.info`, `.mod`, and `.zip` files plus the installed module cache keep the expected metadata and README guidance, ships both the public README example and stronger real-user example in the downloaded artifact, and gives those shipped examples one discoverable helper surface such as `examples/Makefile` where plain `make` prints copy-pasteable `make run-readme-example`, `make run`, and `make run-real-user-smoke` commands before the README example runs through `make run-readme-example` and the stronger path runs through `make run` or `make run-real-user-smoke`. Keep proving any narrower smoke-only helper like `examples/real_user_smoke/Makefile` too, but prefer a downloaded top-level helper surface once the artifact ships multiple public example flows. The same smoke path should still prove a separate lifecycle module can remove the dependency with `go get ...@none` and add it back, prove repo-checkout `gofmt`, `go vet`, `go test`, and Staticcheck, prove readonly build, test, vet, list, doc, and run flows, prove installed W3C traceparent helper parsing/creation/span-attribute derivation from a temp app, prove installed `net/http` delivery against a local intake without external calls, and keep the public module/package metadata, checksums, and built-binary module metadata visible from real user commands.

Use protected GitHub-hosted release workflows and trusted publishing where available.
