# LogBrew SDK Readiness Memory

- 2026-06-07: All initial `0.1.0` public package marketplaces are live and verified: npm JS packages, PyPI core/FastAPI/Django packages, NuGet `LogBrew`, RubyGems, Packagist, crates.io, Maven Central Java/Kotlin artifacts, and OpenUPM Unity package.
- 2026-06-07: Published GitHub Releases now own repeat package updates through `publish-release.yml`, which dispatches `publish-packages.yml` with real `target=all`, PyPI extras, crates, Packagist, and Maven Central enabled. Manual dispatch remains dry-run-safe by default.
- 2026-06-07: Packagist auto-updates from GitHub; optional Packagist update-hook settings can speed up refresh, but absence is non-blocking because the workflow verifies the public registry after retries.
- 2026-06-07: Maven Central repeat automation has release-environment auth and signing settings configured. Local signed Central bundle proof built 80 entries; the next versioned GitHub Release is the first real automated Central upload proof because `0.1.0` cannot be uploaded again.
- 2026-06-07: User granted standing permission for autonomous commits, pushes, tags, GitHub Releases, and public registry package publishing when thermo-nuclear review, verifier evidence, and GitHub Actions are healthy. Resolve blockers first with available GitHub/Chrome/Computer access, then report only if blocked.
- 2026-06-08: User clarified that this is durable permission for all public SDK release-path local/network/tool access and approval/escalation use. Do not pause to ask for conversational permission on `LogBrewCo/sdk`; use available escalation paths directly, and only report when an external platform, account login, or 2FA block remains after attempting available access.
- 2026-06-08: User explicitly does not want sidecar repos. Do not create new GitHub/org repositories, Homebrew taps, registry helper repos, or package-hosting repos without explicit user permission for that exact repo name, even when other public SDK release work is pre-approved.
- 2026-06-11: User clarified the minimum bar: LogBrew SDKs must become better and more useful than competitor SDKs, especially Sentry. Treat competitor install/use testing and comparison as a required input for meaningful SDK/docs work, not optional research. Prefer concrete gaps that make LogBrew safer, clearer, lighter, more agent-readable, easier to integrate, or better correlated across logs/issues/traces/actions/metrics than Sentry/Datadog/OpenTelemetry examples. Record concise competitor pros/cons and turn them into SDK code, docs, examples, tests, or backend contract reports.
- 2026-06-13: User clarified that when LogBrew is worse than competitors, the automation must inspect how competitors solve that exact gap, including their public open-source SDK repositories when available, not just docs. For each meaningful gap, compare real-user behavior plus source-level implementation patterns from official competitor repos such as Sentry, Datadog, OpenTelemetry, PostHog, and relevant framework integrations; identify what they do better, what complexity/privacy cost they pay, and what LogBrew can implement in a lighter, safer, more agent-readable way. Do not copy competitor code; use source reading to guide API shape, tests, reliability patterns, docs, and backend contract reports, with citations/paths/commits recorded when used.
- 2026-06-08: Public docs need product focus, not only confidentiality safety. READMEs should explain what LogBrew does, how developers install it into their own projects, how AI coding assistants can wire integrations, signal/privacy defaults, and service setup; keep maintainer-only verifier internals, registry mechanics, CI run IDs, artifact-inspection prose, and "prove this smoke path" language out of user-facing README pages. Competitor docs/repos such as Sentry, Datadog, and OpenTelemetry should be checked for current README/product-doc structure before meaningful docs work.
- 2026-06-08: Public docs pass rewrote the root README around product purpose, package selection, quick starts, privacy defaults, local JSON preview, and agent usage, and removed maintainer-oriented prose from the core JS and PHP package READMEs. Check evidence: `python3 scripts/check_markdown_links.py && python3 scripts/check_confidentiality_scan.py && git diff --check`, a root/JS/PHP product-focus text scan, `bash scripts/real_user_js_smoke.sh`, and `bash scripts/real_user_php_smoke.sh` passed. Residual next priority: apply the same product-focused README pass to the remaining package READMEs that still contain maintainer-oriented language.
- 2026-06-08: Second public docs pass cleaned Go, Python, Java, Objective-C, and Rust package READMEs so their public pages now emphasize install, usage, examples, privacy/signal behavior, and AI assistant wiring guidance rather than maintainer process detail. Evidence: markdown links, confidentiality scan, release metadata, product-focus text scan, and diff whitespace checks passed locally. Next docs priority is to keep this public-docs standard enforced as new package examples and integrations are added.
- 2026-06-08: Public README product refresh now spans all SDK package READMEs. User-facing docs must not tell developers or AI assistants to run maintainer workflow validation or internal example targets; examples should be framed as copyable integration source for the developer's own app and LogBrew as a hosted observability service. The centralized confidentiality scan now also rejects maintainer-process wording in public `README.md` files. Evidence: public README wording scan, markdown links, confidentiality scan, release metadata, diff hygiene, package metadata validators for C/C++/ObjC/.NET/Java/Kotlin/Unity/Ruby/Swift, exact Python/Go/PHP local gates, and installed-artifact validation paths for JS/Python/Go/Java/.NET/PHP/Ruby/Rust passed locally.
- 2026-06-08: Competitor complaint signal: users want agents to analyze hundreds of sessions instead of watching replay recordings one by one. Treat this as an SDK/product priority for agent-readable observability: structured action timelines, session/trace/log/error/network/funnel correlation, privacy-first redaction/sampling, and copyable AI assistant wiring docs. Browser SDK now exposes explicit `captureBrowserAction()` / `createBrowserActionEvent()` helpers for app-owned product steps with primitive sanitized metadata such as `sessionId`, `routeTemplate`, `funnel`, `step`, and `traceId`; it does not auto-capture every click or claim visual session replay. Local evidence: JS source syntax, ESLint, publint package checks, `bash scripts/real_user_browser_smoke.sh` with happy-dom@20.10.1, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed.
- 2026-06-08: Browser agent-readable timeline work now includes explicit `captureBrowserNetwork()` / `createBrowserNetworkEvent()` helpers over the existing action event type for app-owned API milestones. The helper records route templates, methods, status codes, durations, session IDs, trace IDs, and primitive metadata while stripping query/hash text from route templates, dropping nested metadata, and avoiding global fetch patching or payload/header capture. Local evidence: current Sentry/Datadog/OpenTelemetry docs still point toward replay/RUM timelines plus privacy controls, `node --check` for browser entrypoints/examples, `python3 scripts/check_js_sources.py js/logbrew-browser`, `cd js/logbrew-browser && npm test`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, `bash scripts/real_user_browser_smoke.sh` with happy-dom@20.10.1, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed.
- 2026-06-08: React agent-readable timeline work now includes `useLogBrewAction()` / `useLogBrewNetwork()` hooks plus `createReactActionEvent()` / `captureReactAction()` / `createReactNetworkEvent()` / `captureReactNetwork()` helpers. They emit existing action events for app-owned product steps and API milestones, merge hook default metadata with per-call metadata, strip query/hash from route templates, drop nested metadata, default network failures from HTTP status codes, and avoid fetch patching or payload/header capture. Keep canonical six-event parity stdout separate from React-specific timeline previews. Local evidence: current Sentry/Datadog/OpenTelemetry docs still point toward RUM/replay timelines plus privacy controls, `node --check` for React entrypoints/examples, `python3 scripts/check_js_sources.py js/logbrew-react`, `cd js/logbrew-react && npm test`, `bash scripts/real_user_react_smoke.sh` with React/React DOM/React Test Renderer 19.2.7, JS ESLint, publint package checks, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed.
- 2026-06-08: React Native agent-readable timeline work now includes direct and provider-hook `captureReactNativeAction()` / `captureReactNativeNetwork()` helpers plus event factories. They emit existing action events for app-owned mobile product steps and API milestones, merge React Native platform/app-state/screen context, strip query/hash from route templates, drop nested metadata, default network failures from HTTP status codes, and avoid fetch patching, visual replay, payload capture, or header capture. Keep the main installed smoke stdout stable while proving mobile timeline previews separately. Local evidence: current Sentry/Datadog/OpenTelemetry mobile docs still point toward RUM/replay/action/resource timelines plus privacy controls, `node --check` for React Native entrypoints/examples, `python3 scripts/check_js_sources.py js/logbrew-react-native`, `cd js/logbrew-react-native && npm test`, `bash scripts/real_user_react_native_smoke.sh` with react-native@0.85.3, react@19.2.7, react-test-renderer@19.2.7, JS ESLint, publint package checks, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed.
- 2026-06-08: Kotlin Android agent-readable timeline work now includes explicit `LogBrewAndroid.captureProductAction(...)` and `LogBrewAndroid.captureNetworkMilestone(...)` helpers. They emit existing action events for app-owned Android product steps and API milestones, merge `AndroidContext` session/screen/device metadata, normalize methods, strip query/hash from route templates, reduce full URLs to paths, validate HTTP status/duration, reject nested metadata, default network failures from HTTP status codes, and avoid HTTP patching, visual replay, payload capture, or header capture. Keep canonical parity stdout separate from Android timeline preview proof. Local evidence: `bash scripts/check_kotlin_style.sh && bash scripts/check_kotlin_package.sh && bash scripts/real_user_kotlin_smoke.sh`, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: Swift/Apple-platform agent-readable timeline work now includes `LogBrewClient.captureProductAction(...)`, `LogBrewClient.captureNetworkMilestone(...)`, and `ProductTimelineContext`. They emit existing action events for app-owned product steps and API milestones, normalize methods, strip query/hash from route templates, reduce HTTP(S) URLs to paths, validate route/status/duration/finite metadata, default network failures from HTTP status, and avoid URLSession patching, visual replay, payload capture, or header capture. Thermo-nuclear review caught and fixed a query-only route sanitization gap; keep canonical parity stdout separate from Swift timeline preview proof. Local evidence: current Sentry/Datadog/OpenTelemetry Apple docs still support explicit app-owned timeline/resource signals with privacy controls, `bash scripts/check_swift_style.sh && bash scripts/check_swift_package.sh && bash scripts/real_user_swift_smoke.sh`, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: Objective-C agent-readable timeline work now includes primitive action metadata plus `captureProductActionWithID:...` and `captureNetworkMilestoneWithID:...` helpers. They emit existing action events, normalize methods, strip query/hash from route templates, reduce HTTP(S) URLs to paths, validate primitive metadata plus integer HTTP status/duration, default network failures from HTTP status, and avoid NSURLSession patching, visual replay, payload capture, or header capture. Thermo-nuclear review caught and fixed fractional status-code truncation before commit. Keep canonical parity stdout separate from Objective-C timeline preview proof. Local evidence: current Sentry/Datadog/OpenTelemetry Apple docs still point toward app-owned timeline/resource signals with privacy controls, `bash scripts/check_objc_package.sh && bash scripts/real_user_objc_smoke.sh` passed with Apple clang 21.0.0, plus markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: Objective-C delivery now has Foundation `NSURLSession`-backed `LBWHTTPTransport` with default hosted endpoint, custom non-reserved headers, positive timeout validation, SDK-managed authorization/content-type, retryable network failure mapping, and local HTTP 503-to-202 retry proof from an installed source archive. Keep it app-owned and explicit: no global `NSURLSession` patching, payload capture, or arbitrary header capture. Keep hosted delivery in `src/LBWHTTPTransport.m` so preview/custom-transport users can compile only `src/LogBrew.m`. Local evidence: current OpenTelemetry exporter docs reinforce endpoint/header/timeout delivery surfaces; `bash scripts/check_objc_package.sh` and `bash scripts/real_user_objc_smoke.sh` passed with Apple clang 21.0.0.
- 2026-06-09: Objective-C core now has explicit `metricWithID:timestamp:attributes:error:` support with finite numeric value validation, kind-specific temporality/non-negative checks, primitive low-cardinality metadata guidance, packaged README/header checks, and installed source-archive smoke proof. Keep Objective-C README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-source smoke paths. Local evidence: current OpenTelemetry metrics and Datadog custom metrics docs reinforce explicit instrumentation and cardinality control; `bash scripts/check_objc_package.sh && bash scripts/real_user_objc_smoke.sh` passed with Apple clang 21.0.0, plus markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: C++ agent-readable timeline work now includes primitive `MetadataValue`/`Metadata`, optional action metadata, and `capture_product_action(...)` / `capture_network_milestone(...)` helpers. They emit existing action events, preserve numeric/bool metadata, normalize methods, strip query/hash from route templates, reduce HTTP(S) URLs to paths, validate status/duration/finite metadata, default network failures from HTTP status, and avoid HTTP patching, visual replay, payload capture, or header capture. Keep canonical parity stdout separate from C++ timeline preview proof. Local evidence: current Sentry Native, Datadog RUM/resource, and OpenTelemetry C++ docs support explicit app-owned signals with privacy controls; `bash scripts/check_cpp_package.sh` and `bash scripts/real_user_cpp_smoke.sh` passed with Apple clang 21.0.0, plus markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: C++ delivery now has optional libcurl-backed `HttpTransport` with default hosted endpoint, custom non-reserved headers, positive timeout validation, SDK-managed authorization/content-type, retryable network failure mapping for curl request failures, and local HTTP 503-to-202 retry proof from an installed source archive. Keep `src/logbrew_http_transport.cpp` optional so preview/custom-transport users do not inherit libcurl; public build snippets must distinguish core source from optional HTTP source and curl flags. Local evidence: current Sentry Native and OpenTelemetry exporter docs reinforce endpoint/header/timeout delivery surfaces; `bash scripts/check_cpp_package.sh` and `bash scripts/real_user_cpp_smoke.sh` passed with Apple clang 21.0.0.
- 2026-06-09: C++ core now has explicit `client.metric(...)` support with `MetricAttributes`, finite `double` value validation, kind-specific temporality/non-negative checks, low-cardinality metadata guidance, unit coverage, packaged README checks, and installed source-archive smoke proof. Keep C++ README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-source smoke paths. Local evidence: current OpenTelemetry C++ and Datadog custom metrics docs reinforce explicit instrumentation and cardinality control; `bash scripts/check_cpp_package.sh` and `bash scripts/real_user_cpp_smoke.sh` passed with Apple clang 21.0.0, plus markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: C agent-readable timeline work now includes primitive `LogBrewMetadataEntry`, `LogBrewProductTimelineContext`, `logbrew_client_product_action(...)`, and `logbrew_client_network_milestone(...)`. They emit existing action events, preserve numeric/bool metadata, normalize methods, strip query/hash from route templates, reduce HTTP(S) URLs to paths, validate status/duration/finite metadata, default network failures from HTTP status, and avoid HTTP patching, visual replay, payload capture, or header capture. The C source package core compiles `src/logbrew.c`, `src/logbrew_metric.c`, `src/logbrew_recording_transport.c`, and `src/logbrew_timeline.c`; keep README/build snippets aligned with all four source files. Keep canonical parity stdout separate from C timeline preview proof. Local evidence: current Sentry Native, Datadog RUM/resource, and OpenTelemetry C/C++ docs support explicit app-owned signals with privacy controls; `bash scripts/check_c_package.sh` and `bash scripts/real_user_c_smoke.sh` passed with Apple clang 21.0.0, plus markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-09: C core now has explicit `logbrew_client_metric(...)` support with `LogBrewMetricAttributes`, finite `double` value validation, kind-specific temporality/non-negative checks, primitive low-cardinality metadata guidance, packaged README/header checks, and installed source-archive smoke proof. Keep C README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-source smoke paths. Local evidence: current OpenTelemetry Metrics API and Datadog custom metrics docs reinforce explicit instrumentation and cardinality control; `bash scripts/check_c_package.sh && bash scripts/real_user_c_smoke.sh` passed with Apple clang 21.0.0.
- 2026-06-08: C delivery now has optional libcurl-backed `LogBrewHttpTransport` with default hosted endpoint, custom non-reserved headers, positive timeout validation, SDK-managed `authorization`/`content-type`, retryable `network_failure` mapping for curl request failures, and local HTTP 503-to-202 retry proof from an installed source archive. Keep `src/logbrew_http_transport.c` optional so preview/custom-transport users do not inherit libcurl; public build snippets must distinguish core sources from the optional HTTP source and `curl-config --libs`. Local evidence: `bash scripts/check_c_package.sh`, `bash scripts/real_user_c_smoke.sh`, markdown links, confidentiality scan with narrow libcurl API false-positive handling, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: Package UX lesson: public docs should teach users to install only the runtime package and framework integrations their own app needs; the published packages are separate registry entry points, not a bundle. Treat this package-split and changed-package release model as the default unless the user explicitly asks for a different release approach. Release automation should prefer changed-package versioning/publishing when practical. Do not publish or ask developers to update unrelated ecosystems for docs or package changes that do not affect them.
- 2026-06-07: JavaScript core now has explicit `client.metric()` support in the event contract with metric `name`, `kind`, numeric `value`, `unit`, `temporality`, low-cardinality metadata guidance, strict runtime validation, discriminated TypeScript declarations, schema/validator coverage, and README/type-smoke proof. Keep the shared parity fixture at releases/environments/issues/logs/spans/actions until other SDKs add metric helpers; do not advertise automatic metrics capture yet.
- 2026-06-07: Metric verifier evidence: `python3 -m unittest tests/test_validate_fixtures.py` passed 20 tests, `node --test js/logbrew-js/test/sdk.test.js` passed 41 tests, fixture validation/parity passed, `bash scripts/real_user_js_smoke.sh` passed twice after metric edits, direct installed TypeScript proof passed via `node .../typescript/lib/tsc.js --project`, and public scan/markdown links/ShellCheck 0.11.0/release metadata passed. A later duplicate full JS smoke rerun hit a stale `/usr/bin/env node ... tsc` launcher with no CPU; kill only the stale verifier and use direct TypeScript invocation for focused declaration proof rather than repeating the whole package loop.
- 2026-06-07: Python core now also has explicit `client.metric()` support with `MetricAttributes`, Literal-typed kind/temporality fields, finite value validation, kind-specific temporality/non-negative checks, README guidance, unit tests, and installed wheel/sdist proof through `scripts/real_user_python_smoke.sh`. Keep canonical README/package examples at the six parity event types; metric proof belongs in focused tests/type-smoke until more SDKs support metrics.
- 2026-06-07: Python metric verifier evidence: `PYTHONPATH=python/logbrew_py/src python3 -m unittest python/logbrew_py/tests/test_sdk.py` passed 21 tests, `python3 scripts/check_python_sources.py python/logbrew_py/src python/logbrew_py/tests` passed, `bash scripts/real_user_python_smoke.sh` passed after building/installing/reinstalling wheel and sdist, `python3 scripts/check_confidentiality_scan.py`, `python3 scripts/check_markdown_links.py`, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, `python3 scripts/validate_fixtures.py fixtures/valid-batch.json`, and `python3 -m unittest tests/test_validate_fixtures.py` passed.
- 2026-06-07: Go core now has explicit `Client.Metric` support with `MetricAttributes`, finite `float64` value validation, kind-specific temporality/non-negative checks, README guidance, unit tests, go-doc proof, and installed module smoke coverage. Keep the shared parity fixture at the six event types until more SDKs support metrics; metric proof belongs in focused tests and installed-artifact smoke paths.
- 2026-06-07: Go metric verifier evidence: `cd go/logbrew && test -z "$(gofmt -l .)" && go vet ./... && go test ./... && cd ../.. && bash scripts/check_go_static.sh` passed with Staticcheck 2025.1.1 (0.6.1), `bash scripts/real_user_go_smoke.sh` passed with install/remove/reinstall and installed metric preview/go-doc proof, and `python3 scripts/check_markdown_links.py && python3 scripts/check_confidentiality_scan.py && git diff --check` passed.
- 2026-06-07: Java core now has explicit `LogBrewClient.metric(...)` support with `MetricAttributes`, finite `double` value validation, kind-specific temporality/non-negative checks, README guidance, unit tests, javadoc/package assertions, and installed jar smoke coverage. Keep Java README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-jar smoke until more SDKs support metrics.
- 2026-06-07: Java metric verifier evidence: `bash scripts/check_java_static.sh` passed with SpotBugs 4.9.8, `bash scripts/check_java_package.sh` passed with 25 package tests plus javadoc/source/binary jar assertions, `bash scripts/real_user_java_smoke.sh` passed with installed metric proof, `bash scripts/real_user_spring_boot_smoke.sh` passed with spring-boot@4.0.6, and `python3 scripts/check_markdown_links.py && python3 scripts/check_confidentiality_scan.py && git diff --check` passed.
- 2026-06-08: .NET core now has explicit `LogBrewClient.Metric(...)` support with `MetricAttributes`, finite `double` value validation, kind-specific temporality/non-negative checks, low-cardinality metadata guidance, unit tests, packaged README checks, and installed NuGet smoke proof. Keep .NET README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-package smoke until more SDKs support metrics.
- 2026-06-08: .NET metric verifier evidence: `bash scripts/check_dotnet_package.sh` passed outside sandbox with 19 package tests after sandboxed `dotnet` hit macOS `CSSM_ModuleLoad()`, `bash scripts/real_user_dotnet_smoke.sh` passed outside sandbox with installed metric proof, and `python3 scripts/check_markdown_links.py && python3 scripts/check_confidentiality_scan.py && bash scripts/check_shell_static.sh && git diff --check` passed with ShellCheck 0.11.0.
- 2026-06-08: PHP core now has explicit `LogBrewClient::metric(...)` support with `MetricAttributes`, finite numeric value validation, kind-specific temporality/non-negative checks, low-cardinality metadata guidance, unit tests, packaged README checks, PHPStan consumer proof, and installed Composer smoke proof. Keep PHP README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-package smoke until more SDKs support metrics.
- 2026-06-08: PHP metric verifier evidence: `bash scripts/check_php_static.sh`, `bash scripts/real_user_php_smoke.sh`, and the exact CI slice `composer update --no-interaction && python3 scripts/check_php_sources.py && bash scripts/check_php_static.sh && cd php/logbrew-php && php tests/run.php` passed on PHP 8.5.6 with Composer 2.10.0; generated `php/logbrew-php/vendor` and `composer.lock` were removed after local CI proof. `python3 scripts/check_markdown_links.py && python3 scripts/check_confidentiality_scan.py && bash scripts/check_shell_static.sh && git diff --check` also passed.
- 2026-06-08: Ruby core now has explicit `client.metric(...)` support with finite `Integer`/`Float` value validation, kind-specific temporality/non-negative checks, low-cardinality metadata guidance, unit coverage, packaged README checks, and installed gem proof. Keep Ruby README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-package paths until more SDKs support metrics.
- 2026-06-08: Ruby metric local evidence: `bash scripts/check_ruby_package.sh` passed with 30 tests, `bash scripts/real_user_ruby_smoke.sh` passed with installed metric proof, and `python3 scripts/check_confidentiality_scan.py && python3 scripts/check_markdown_links.py && python3 scripts/check_release_metadata.py && bash scripts/check_shell_static.sh && git diff --check` passed with ShellCheck 0.11.0.
- 2026-06-08: Swift core now has explicit `client.metric(...)` support with `MetricAttributes`, finite `Double` value validation, kind-specific temporality/non-negative checks, low-cardinality metadata guidance, unit coverage, packaged README checks, and installed SwiftPM consumer proof. Keep Swift parity examples at the six canonical event types; metric proof belongs in focused tests and installed-package paths until more SDKs support metrics.
- 2026-06-08: Swift metric local evidence: current Sentry Apple metrics docs show the old metrics beta retired, OpenTelemetry Swift lists metrics as Development, and Datadog iOS docs reinforce app-owned client setup/custom metric care, so LogBrew kept explicit app-owned metrics instead of automatic capture. `bash scripts/check_swift_style.sh && bash scripts/check_swift_package.sh && bash scripts/real_user_swift_smoke.sh`, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-08: Kotlin core now has explicit `client.metric(...)` support with `MetricAttributes`, finite `Double` value validation, kind-specific temporality/non-negative checks, low-cardinality metadata guidance, unit coverage, packaged README checks, and installed Maven/JAR consumer proof. Keep Kotlin parity examples at the six canonical event types; metric proof belongs in focused tests and installed-package paths until more SDKs support metrics.
- 2026-06-08: Kotlin metric local evidence: current Sentry/OpenTelemetry/Datadog Kotlin/Android docs point toward app-owned metric/session/privacy signals but avoid making automatic broad capture the default, so LogBrew kept metrics explicit and dependency-light. `bash scripts/check_kotlin_style.sh`, `bash scripts/check_kotlin_package.sh`, `bash scripts/real_user_kotlin_smoke.sh`, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-09: Rust core now has explicit `client.metric(...)` support with `MetricEvent`, finite `f64` value validation, kind-specific temporality/non-negative checks, primitive low-cardinality metadata guidance, packaged README/doc checks, and installed crate smoke proof. Keep Rust README/examples parity output at the six canonical event types; metric proof belongs in focused tests and installed-package paths until safe automatic or framework-owned metric helpers exist. Local evidence: current OpenTelemetry Rust metrics, Sentry Rust, and Datadog custom metrics docs reinforce explicit instrumentation and cardinality control; `cd rust/logbrew && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test`, `cd rust/logbrew && cargo test --all-features`, `bash scripts/real_user_rust_smoke.sh`, markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-09: Express now has opt-in framework-owned request duration metrics through `captureRequestMetrics`, `createRequestMetricEvent(...)`, `metricName`, `metricIdFactory`, and `requestMetricEvent`. It emits an explicit `http.server.duration` histogram with primitive metadata (`framework`, `method`, `routeTemplate`, `statusCode`, `statusCodeClass`), omits query/hash text, prefers Express route templates such as `/orders/:id`, supports metrics-only mode with `captureRequests: false`, and preserves app-owned response handling. Local evidence: current OpenTelemetry JS instrumentation docs show `http.server.duration` for Express-style request latency and Datadog/APM docs reinforce route/status duration visibility plus cardinality control; `node --check js/logbrew-express/index.js js/logbrew-express/index.cjs`, `python3 scripts/check_js_sources.py js/logbrew-express`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, `bash scripts/real_user_express_smoke.sh` with express@5.2.1/@types/express@5.0.6, release metadata, markdown links, confidentiality scan, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-09: Fastify now has opt-in framework-owned request duration metrics through `captureRequestMetrics`, `createRequestMetricEvent(...)`, `metricName`, `metricIdFactory`, and `requestMetricEvent`. It emits an explicit `http.server.duration` histogram with primitive metadata (`framework`, `method`, `routeTemplate`, `statusCode`, `statusCodeClass`), omits query/hash text, prefers Fastify route templates such as `/orders/:id`, supports metrics-only mode with `captureRequests: false`, and preserves app-owned response handling. Local evidence: current Fastify docs expose route templates through request lifecycle hooks, OpenTelemetry HTTP JS docs show HTTP server duration metrics, and Datadog/APM docs reinforce route/status duration visibility plus cardinality control; `node --check js/logbrew-fastify/index.js js/logbrew-fastify/index.cjs`, `python3 scripts/check_js_sources.py js/logbrew-fastify`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, and `bash scripts/real_user_fastify_smoke.sh` passed with fastify@5.8.5.
- 2026-06-09: NestJS now has opt-in framework-owned request duration metrics through `captureRequestMetrics`, `createRequestMetricEvent(...)`, `metricName`, `metricIdFactory`, and `requestMetricEvent`. It emits an explicit `http.server.duration` histogram with primitive metadata (`framework`, `method`, `routeTemplate`, `statusCode`, `statusCodeClass`), omits query/hash text, prefers Nest/Express route templates such as `/metrics/:id`, supports metrics-only mode with `captureRequests: false`, and preserves Nest's normal controller/exception behavior. Local evidence: current NestJS interceptor/execution-context docs support wrapping HTTP request/response flow, OpenTelemetry HTTP JS docs show HTTP server duration metrics, and Datadog/APM docs reinforce route/status duration visibility plus cardinality control; `node --check js/logbrew-nestjs/index.js js/logbrew-nestjs/index.cjs`, `python3 scripts/check_js_sources.py js/logbrew-nestjs`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, and `bash scripts/real_user_nestjs_smoke.sh` passed with `@nestjs/common@11.1.26`, `@nestjs/core@11.1.26`, and `@nestjs/platform-express@11.1.26`.
- 2026-06-09: Package publishing tooling now supports targeted manual npm package dispatch through `npm_packages` on `publish-packages.yml` and `publish-release.yml`; empty keeps the existing all-npm behavior, while values such as `@logbrew/nestjs` or `js/logbrew-nestjs` restrict pack/publish and registry verification to selected packages. JS metadata now validates public semver by default and accepts explicit `--npm-version PACKAGE=VERSION` checks so changed-package bumps are not blocked by the old shared-version policy. `@logbrew/nestjs` is bumped to `0.1.1` for the request-metrics release while core `@logbrew/sdk` remains `0.1.0`; publish only the selected NestJS package after dry-run and CI proof. Local evidence: current npm trusted publishing docs still support OIDC/provenance from GitHub Actions and GitHub workflow dispatch inputs are string-based; `python3 -m unittest tests.test_registry_publication tests.test_release_metadata`, `python3 scripts/check_release_metadata.py --npm-version @logbrew/nestjs=0.1.1`, `python3 scripts/check_registry_publication.py --target npm --npm-package @logbrew/nestjs --npm-version @logbrew/nestjs=0.1.0 --retries 0`, `bash scripts/real_user_nestjs_smoke.sh`, JS lint/package checks, actionlint, YAML parse, markdown links, confidentiality scan, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-10: `@logbrew/nestjs@0.1.1` is the first changed-package repeat publish after public 0.1.0. CI was green for the feature commit (`27206646111`) and targeted release tooling commit (`27209112611`); dry-run dispatch `27276270596` selected only `@logbrew/nestjs`, packed `logbrew-nestjs-0.1.1.tgz`, and did not publish; real dispatch `27276310790` published only `@logbrew/nestjs` and workflow registry verification passed. Independent registry proof passed with `npm view @logbrew/nestjs version --json` returning `0.1.1`, `npm view @logbrew/nestjs@0.1.1 dist.tarball --json` returning the public tarball URL, and `python3 scripts/check_registry_publication.py --target npm --npm-package @logbrew/nestjs --npm-version @logbrew/nestjs=0.1.1 --retries 0` now reporting the explicit npm override. Keep using changed-package bumps/publishes instead of holding all package updates for a broad release or bumping unrelated ecosystems.
- 2026-06-11: `@logbrew/sdk@0.1.1` is published for the core JS product-timeline helpers. Because there are no known production users yet, the default should be to release healthy scoped improvements after CI, dry-run, thermo-nuclear review, and registry proof instead of letting market-readiness work sit unreleased; still avoid unrelated ecosystem bumps. Version-bump commit `f29c0abbf7ec72d681dd37a72d9c322171a0d857` updated only `js/logbrew-js/package.json` and made `scripts/real_user_js_smoke.sh` derive the tarball/version from the package manifest. Local evidence: JS syntax/tests, `check_js_sources`, JS lint/package checks, `real_user_js_smoke`, markdown links, confidentiality scan, generated-artifact hygiene, release metadata override, diff check, and thermo-nuclear review passed. Publish evidence: short-SHA dry-run `27348751330` failed at checkout, proving `publish-packages.yml` needs a full commit SHA for `inputs.ref`; corrected dry-run `27348819620` packed only `@logbrew/sdk`; real publish `27348849244` published only `@logbrew/sdk` and workflow registry verification passed. Independent registry proof: `npm view @logbrew/sdk version dist.tarball --json` returned `0.1.1` and `https://registry.npmjs.org/@logbrew/sdk/-/sdk-0.1.1.tgz`, and `python3 scripts/check_registry_publication.py --target npm --npm-package @logbrew/sdk --npm-version @logbrew/sdk=0.1.1 --retries 0` passed. Follow-up CI lesson: when the core JS package version changes, all JS framework real-user smokes must derive the core SDK version from `js/logbrew-js/package.json` instead of hard-coding `@logbrew/sdk@0.1.0`; local browser/node/express/fastify/nestjs/angular/vue/svelte/react/react-native/next smokes passed after this verifier fix.
- 2026-06-10: Backend coordination is mandatory for any backend-relevant SDK/product improvement idea, gap, verifier result, or product concern. When findings involve ingestion, event contracts, delivery reliability, privacy enforcement, symbolication/source maps/debug symbols, dashboard/API behavior, release artifacts, or mobile/backend support, write/update a backend contract report with expected shape, observed SDK gap, user impact, suggested event/API fields, verifier evidence, and priority, then send it to backend automation/thread whenever a target/tool is discoverable. Stay in touch after backend updates arrive by reconciling SDK docs/tests/contracts promptly. If no backend target/tool is discoverable, record the report and explicitly say backend handoff is pending rather than silently dropping it.
- 2026-06-10: Next.js App Router now has opt-in request duration metrics through `captureRequestMetrics`, `createRequestMetricEvent(...)`, `metricName`, `metricIdFactory`, `requestMetricEvent`, and explicit stable `routeTemplate`. It emits `http.server.duration` histogram metrics with primitive metadata (`framework`, `method`, `routeTemplate`, `statusCode`, `statusCodeClass`), strips query/hash text, supports metrics-only mode with `captureRequests: false`, preserves standard `Request`/`Response` route ownership, and keeps malformed trace propagation non-fatal. Local evidence: current Next docs confirm App Router Route Handlers use standard Web `Request`/`Response`, OpenTelemetry HTTP semantic conventions reinforce `http.server.duration` and route/status metadata, and Sentry Next.js docs show App Router observability expectations; `node --check js/logbrew-next/index.js`, `node --check js/logbrew-next/index.cjs`, `bash -n scripts/real_user_next_smoke.sh`, `python3 scripts/check_js_sources.py js/logbrew-next`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, `bash scripts/real_user_next_smoke.sh` passed with `next@16.2.9`, `react@19.2.7`, `react-dom@19.2.7`, plus markdown links, confidentiality scan, release metadata, and diff check passed.
- 2026-06-11: Frontend trace propagation hardening after competitor comparison: Browser, React, React Native, Angular, Vue, and Svelte `tracePropagationTargets` string entries now use same-origin plus path-prefix checks for absolute URL targets and path-prefix checks for relative targets, avoiding broad substring propagation to lookalike origins while keeping RegExp/function targets for advanced app-owned cases. Keep this as a trace/privacy differentiator against heavyweight SDK defaults. Local evidence: JS syntax checks for all affected ESM/CJS entries, `python3 scripts/check_js_sources.py js/logbrew-browser js/logbrew-react js/logbrew-react-native js/logbrew-angular js/logbrew-vue js/logbrew-svelte`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, installed-artifact real-user smokes for browser/react/react-native/angular/vue/svelte, public confidentiality scan, markdown links, and `git diff --check` passed locally.
- 2026-06-11: JS and Python core now expose explicit outbound trace carrier helpers: `createTraceparentHeaders(...)` and `create_traceparent_headers(...)`. They reuse the existing W3C validation/normalization helpers and return only a `traceparent` header map, preserving app-owned HTTP clients and avoiding payload/header auto-capture or client patching. Local evidence: current W3C/OpenTelemetry/Sentry docs reinforce explicit propagation carriers plus allowlisted injection; `node --check js/logbrew-js/index.cjs js/logbrew-js/index.js`, `node --test js/logbrew-js/test/sdk.test.js`, `python3 -m py_compile` for touched Python files, `PYTHONPATH=python/logbrew_py/src python3 -m unittest discover -s python/logbrew_py/tests`, `python3 scripts/check_js_sources.py js/logbrew-js`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, `bash scripts/real_user_js_smoke.sh`, `python3 scripts/check_python_sources.py`, `bash scripts/check_python_static.sh`, `bash scripts/real_user_python_smoke.sh`, confidentiality scan, markdown links, ShellCheck 0.11.0, and diff check passed locally.
- 2026-06-11: Backend severity contract is now SDK-aligned across JS, Python, Go, Java, .NET, PHP, Ruby, Rust, Swift, Kotlin, Objective-C, C++, and C. Public/user-facing severities are `info`, `warning`, `error`, and `critical`; SDK validators still accept runtime aliases `trace`/`debug` -> `info`, `warn` -> `warning`, `fatal`/`critical` -> `critical`, serialize canonical `level` values before queue/flush, and keep native logger metadata where useful. Docs/examples should present canonical categories first and treat aliases as compatibility inputs, not product labels. Local evidence: core/unit/package/real-user gates passed for all touched ecosystems, including JS lint/package/smoke, Python static/unittest, Go vet/static/smoke, Java package/Spring smoke, .NET package/smoke, PHP static/smoke, Ruby package/smoke, Rust fmt/test/all-features/smoke, Swift style/package/smoke, Kotlin style/package/smoke, Objective-C/C++/C package and installed smoke checks, plus markdown links, confidentiality scan, release metadata, ShellCheck 0.11.0, and diff hygiene.
- 2026-06-11: Local generated-output hygiene now has an explicit guard. `scripts/check_generated_artifacts.py` detects leftover disposable outputs such as dependency folders, build directories, caches, and temporary lockfiles after SDK checks; `check_public_sdks.sh` removes known outputs and then runs this guard as its final step. Local evidence: dry-run output review, generated-output guard, focused guard tests, full `tests/` unittest discovery, `bash -n scripts/check_public_sdks.sh`, ShellCheck 0.11.0, markdown links, confidentiality scan, and diff hygiene passed.
- 2026-06-11: Core JavaScript now has competitor-differentiating, Sentry-breadcrumb-alternative timeline builders: `createProductActionAttributes(...)` and `createNetworkMilestoneAttributes(...)`. They emit existing `action` events for app-owned product steps and API milestones, keep primitive metadata only, strip query/hash/full URLs from route templates, normalize HTTP methods, infer failed network milestones from status codes, preserve trace/session correlation, and avoid visual replay, scopes, global HTTP patching, payload capture, or header capture. The automation fallback prompt was updated locally to make "better and more useful than competitors, especially Sentry" the minimum bar and to require practical competitor install/use comparisons for meaningful SDK/docs work. Local evidence: current Sentry docs show breadcrumbs/scopes, replay summaries, broad context normalization, and trace propagation controls; `node --check js/logbrew-js/index.cjs js/logbrew-js/index.js js/logbrew-js/test/sdk.test.js`, `node --test js/logbrew-js/test/sdk.test.js`, `python3 scripts/check_js_sources.py js/logbrew-js`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, `bash scripts/real_user_js_smoke.sh`, `python3 scripts/check_markdown_links.py`, `python3 scripts/check_confidentiality_scan.py`, `PYTHONDONTWRITEBYTECODE=1 python3 scripts/check_generated_artifacts.py`, `git diff --check`, TOML parse/prompt-size validation, and thermo-nuclear review passed locally. Keep future JS test additions below the 1k-line guardrail or split tests first.
- 2026-06-11: Public severity docs now have a canonical `docs/severity-contract.md` source of truth. Keep LogBrew's product vocabulary at `info`, `warning`, `error`, and `critical`; treat `trace`, `debug`, `warn`, and `fatal` as SDK/runtime compatibility aliases only. Competitor comparison signal: Sentry exposes six levels including `debug` and `fatal`, while OpenTelemetry separates runtime severity text/number; LogBrew should stay simpler for user-facing product categories while preserving logger compatibility and canonical backend payloads. Local evidence: markdown links, confidentiality scan, generated-artifact hygiene, diff review, and thermo-nuclear review passed locally.
- 2026-06-11: Competitor install comparison documented at `docs/competitor-research/sentry-javascript-2026-06-11.md`. Fresh npm lockfile and registry checks showed `@logbrew/sdk@0.1.1` is dependency-free with 90,289 unpacked bytes/12 files, `@logbrew/browser@0.1.0` is dependency-free peer-only with 64,807 bytes/10 files, while `@sentry/browser@10.57.0` is 2,681,257 bytes/692 files with replay/feedback/core dependencies and `@sentry/node@10.57.0` is 3,889,201 bytes/1,071 files with OpenTelemetry/import-hook dependencies. Protect this lightweight install advantage; compete by making explicit W3C trace/action/network/session timelines easier and more agent-readable, not by copying heavyweight automatic instrumentation, global patching, visual replay, payload/header capture, or broad Sentry-style configuration.
- 2026-06-11: JS core now has a drop-only `eventFilter` for last-mile privacy/sampling before events enter the queue. The filter runs after validation/canonical severity normalization, receives a defensive event copy, and can return `false` to drop without adding mutable Sentry-style event processors, scopes, or hidden context. `@logbrew/sdk@0.1.2` was published from commit `852de3143fc52c0d4abf1c74dc3bf838838853f3`. Local evidence: JS syntax, `cd js/logbrew-js && npm test`, `python3 scripts/check_js_sources.py js/logbrew-js`, JS lint/package checks, `bash scripts/real_user_js_smoke.sh`, release metadata override for `@logbrew/sdk=0.1.2`, markdown links, confidentiality scan, generated-artifact hygiene, diff hygiene, and thermo-nuclear review passed locally. CI `27374670118` passed. Targeted npm dry-run `27375354595` selected only `@logbrew/sdk` and packed `logbrew-sdk-0.1.2.tgz`; real publish `27375490014` published only `@logbrew/sdk` and workflow npm verification passed. Independent registry proof: `npm view @logbrew/sdk version dist.tarball --json` returned `0.1.2` and `https://registry.npmjs.org/@logbrew/sdk/-/sdk-0.1.2.tgz`, and `python3 scripts/check_registry_publication.py --target npm --npm-package @logbrew/sdk --npm-version @logbrew/sdk=0.1.2 --retries 0` passed.
- 2026-06-11: `@logbrew/sdk@0.1.3` ships installed ESM/CJS `agent-timeline` examples that prove explicit app-owned product action and network milestone timelines with traceparent headers, primitive metadata, route-template query/hash stripping, and `eventFilter`-dropped info noise. Keep packaged examples as a competitor differentiator against heavyweight Sentry setup: users and agents can run a realistic checkout timeline directly from an installed package without global HTTP patching, visual replay, payload capture, or arbitrary header capture. Local evidence: JS syntax checks, `cd js/logbrew-js && npm test`, `python3 scripts/check_js_sources.py js/logbrew-js`, JS lint/package checks, `bash scripts/check_js_package.sh`, `bash scripts/check_shell_static.sh`, `bash scripts/real_user_js_smoke.sh`, release metadata override for `@logbrew/sdk=0.1.3`, markdown links, confidentiality scan, generated-artifact hygiene, diff hygiene, and thermo-nuclear review passed. Thermo initially caught `sdk.test.js` crossing the 1k-line guardrail, fixed by splitting packaged example tests into `js/logbrew-js/test/examples.test.js`; keep future JS example coverage decomposed. CI `27377253006` passed. Targeted npm dry-run `27377828819` selected only `@logbrew/sdk` and packed `logbrew-sdk-0.1.3.tgz` with `examples/agent-timeline.cjs` and `.mjs`; real publish `27377904971` published only `@logbrew/sdk` with npm provenance and workflow registry verification passed. Independent registry proof: `npm view @logbrew/sdk version dist.tarball --json` returned `0.1.3` and `https://registry.npmjs.org/@logbrew/sdk/-/sdk-0.1.3.tgz`, and `python3 scripts/check_registry_publication.py --target npm --npm-package @logbrew/sdk --npm-version @logbrew/sdk=0.1.3 --retries 0` passed.
- 2026-06-12: Python core now matches the JS agent-readable timeline UX with `create_product_action_attributes(...)`, `create_network_milestone_attributes(...)`, and an installed `python -m logbrew_sdk.examples agent-timeline` example. The helpers emit existing action events, keep primitive metadata only, strip query/hash/full URLs from route templates, normalize methods, infer failed network milestones from status codes, preserve trace/session correlation, and avoid visual replay, global HTTP patching, payload capture, or header capture. Thermo review caught `__init__.py` crossing the 1k-line guardrail; fixed by moving the implementation into focused `_timeline.py` and keeping only public re-exports in `__init__.py`. Local evidence: py_compile, focused unittest, `python3 scripts/check_python_sources.py`, `bash scripts/check_python_static.sh`, full `bash scripts/real_user_python_smoke.sh` with wheel/sdist install-uninstall-reinstall proof, markdown links, confidentiality scan, generated-artifact hygiene, diff check, and thermo-nuclear review passed. Next publish requires a scoped PyPI version bump because current package metadata remains `0.1.0`.
- 2026-06-12: Python core/FastAPI/Django `0.1.1` are published on PyPI for the agent-readable timeline release. Version-bump commit `2ad6679e80a58ca3e713b9e13be9c315098010fc` bumped `logbrew-sdk`, `logbrew-fastapi`, and `logbrew-django`; framework packages now depend on `logbrew-sdk>=0.1.1,<0.2.0`; package/smoke scripts derive wheel/sdist expectations from `pyproject.toml` through `scripts/python_package_version.sh`. Local evidence: Python source/static/unit gates, `check_fastapi_package`, `check_django_package`, `real_user_python_smoke`, `real_user_fastapi_smoke`, `real_user_django_smoke`, release metadata, ShellCheck 0.11.0, markdown links, confidentiality scan, generated-artifact hygiene, diff check, and thermo-nuclear review passed. CI `27416823862` passed. PyPI dry-run `27418119712` built core/extras without publishing; real publish `27418157399` uploaded `logbrew-sdk`, `logbrew-fastapi`, and `logbrew-django`; independent registry proof `python3 scripts/check_registry_publication.py --target pypi --include-pypi-extras --version 0.1.1` passed. Verifier lesson: do not run FastAPI and Django package builds in parallel against the same local checkout because Python build writes shared `python/logbrew_py/build` state; run them sequentially or harden scripts to build from temp copies.
- 2026-06-12: Brand assets are committed in this repo under `assets/brand` from commit `0d31fe7`. Use `assets/brand/logbrew-logo-espresso-bg-512.png` or `assets/brand/github-social-preview.png` for store/social/preview/icon surfaces, and `assets/brand/logbrew-logo-transparent-512.png` for docs/README/UI surfaces with their own background. Raw URLs are `https://raw.githubusercontent.com/LogBrewCo/sdk/main/assets/brand/logbrew-logo-espresso-bg-512.png`, `https://raw.githubusercontent.com/LogBrewCo/sdk/main/assets/brand/logbrew-logo-transparent-512.png`, and `https://raw.githubusercontent.com/LogBrewCo/sdk/main/assets/brand/github-social-preview.png`. Palette: espresso `#3C2B24`, gold `#FFC61A`, edge `#111011`, foam `#FFFFFF`. Do not redraw, recolor, crop, smooth, outline, shadow, or alter the pixel logo geometry.
- 2026-06-12: Python FastAPI and Django now match the JS server framework request-metric UX with opt-in `capture_request_metrics` and `create_request_metric_attributes(...)` / `capture_request_metric(...)` helpers. They emit explicit `http.server.duration` histogram metrics with primitive low-cardinality metadata (`framework`, `method`, `routeTemplate`, `statusCode`, `statusCodeClass`), strip query/hash text, support metrics-only mode with successful request spans disabled, and preserve framework-owned responses plus existing trace/error behavior. This closes a competitor-readiness gap versus Sentry/Datadog/OpenTelemetry expectations for request duration visibility without adopting heavyweight auto-instrumentation, payload/header capture, or global patching. Local evidence: current Sentry/OpenTelemetry/Datadog FastAPI/Django/Python docs confirm request tracing/duration visibility expectations; `python3 scripts/check_python_sources.py`, `bash scripts/check_python_static.sh`, `bash scripts/check_fastapi_package.sh`, `bash scripts/check_django_package.sh`, `bash scripts/real_user_fastapi_smoke.sh` with fastapi@0.136.3, `bash scripts/real_user_django_smoke.sh` with django@6.0.6, release metadata, markdown links, confidentiality scan, generated-artifact hygiene, `git diff --check`, and thermo-nuclear review passed. Direct local unittest discovery without venv dependencies failed because global Python lacked FastAPI/Django; the installed-artifact package checks are the authoritative proof. Next release step: bump/publish scoped Python framework packages as `0.1.2` or first harden PyPI workflow for changed-package extras so core is not republished unnecessarily.
- 2026-06-12: Python FastAPI/Django `0.1.2` are published on PyPI for opt-in request duration metrics while core stays `logbrew-sdk@0.1.1`. PyPI release tooling now supports package-specific `--pypi-version PACKAGE=VERSION` registry verification, and `publish-packages.yml` reads core/FastAPI/Django package versions separately via `scripts/python_package_version.sh`, so changed framework artifacts can be published and verified without republishing core. Local evidence: `python3 -m unittest tests.test_registry_publication tests.test_release_metadata`, `python3 scripts/check_release_metadata.py`, `bash scripts/check_shell_static.sh`, Python source/static checks, FastAPI/Django package checks, FastAPI/Django real-user smokes, generated-artifact hygiene, markdown links, confidentiality scan, and `git diff --check` passed. CI `27421705588` passed. PyPI dry-run `27422463084` built Python distributions only; real publish `27422501361` uploaded `logbrew-fastapi` and `logbrew-django` and workflow PyPI verification passed. Independent registry proof: `python3 scripts/check_registry_publication.py --target pypi --include-pypi-extras --version 0.1.1 --pypi-version logbrew-fastapi=0.1.2 --pypi-version logbrew-django=0.1.2` passed.
- 2026-06-12: Go now matches JS/Python agent-readable timeline UX with `CreateProductActionAttributes(...)`, `CreateNetworkMilestoneAttributes(...)`, `ProductActionInput`, `NetworkMilestoneInput`, and packaged `examples/agent_timeline`. Helpers emit normal action events, keep primitive metadata only, strip query/hash/full URL text from route templates, normalize methods, infer network failure from 4xx/5xx status codes, preserve trace/session correlation, and avoid visual replay, global `net/http` patching, payload capture, or header capture. Thermo review caught internal `any` optional carriers for status/duration and an unnecessary second metadata parameter; fixed with explicit `(value, present, error)` helpers and one typed input per helper before staging. Local evidence: current Sentry Go breadcrumb docs, OpenTelemetry Go instrumentation/context propagation docs, and Datadog Go tracing docs confirm breadcrumb/span/attribute/correlation expectations; `gofmt`, `go test ./...`, `go vet ./...`, `bash scripts/check_go_static.sh` with Staticcheck 2025.1.1, `bash scripts/real_user_go_smoke.sh`, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, markdown links, confidentiality scan, generated-artifact hygiene, and `git diff --check` passed. Release evidence: commit `dccdf88` pushed to `main`, CI `27424484921` passed, tag `go/logbrew/v0.1.1` pushed, and `GOPROXY=https://proxy.golang.org,direct GOSUMDB=sum.golang.org python3 scripts/check_registry_publication.py --target go --version 0.1.1 --retries 20 --retry-delay 30` passed.
- 2026-06-12: Public README logo usage now follows the brand contract: all package/root README docs use `logbrew-logo-transparent-512.png` instead of the espresso-background asset, while espresso-background/social-preview assets remain reserved for store/social/icon surfaces. Evidence: `rg` found no `logbrew-logo-espresso-bg-512.png` references in README files, and 29 README references now point at `logbrew-logo-transparent-512.png`; markdown links, confidentiality scan, generated-artifact hygiene, and diff hygiene passed locally.
- 2026-06-13: GitHub Releases no longer have to stay stuck at repo-wide `v0.1.0` for scoped SDK progress. `publish-release.yml` now dispatches registry publishing only for repo-wide `vMAJOR.MINOR.PATCH` releases and skips tags containing `/`; release docs and `check_release_metadata.py` enforce this. Commit `1eab3c3` CI `27459840636` passed; follow-up historical-tag warning commit `5e6d149` CI `27460578796` passed. Go SDK Release `go/logbrew/v0.1.1` now exists and is latest, but creating it from a historical pre-guard tag proved GitHub evaluates `release` workflows from the tag commit: old workflow run `27460501620` spawned `publish-packages.yml` run `27460503823`, which was cancelled. Duplicate npm/NuGet/crates/RubyGems attempts were rejected as already published, PyPI/Maven/Packagist were cancelled, and registry checks confirmed expected versions (`@logbrew/sdk@0.1.3`, PyPI core `0.1.1`, FastAPI/Django `0.1.2`, crates/RubyGems/NuGet/Packagist `0.1.0`). Durable rule: never create GitHub Releases for historical scoped tags unless `publish-release.yml` is disabled or the tag workflow has been audited; create future scoped release-note tags only after the guard is present.
- 2026-06-13: GitHub Releases now surface the already-published scoped SDK progress without republishing registries. Created guarded-current-main release-note tags `js/logbrew-js/v0.1.3`, `js/logbrew-nestjs/v0.1.1`, `python/logbrew_py/v0.1.1`, `python/logbrew_fastapi/v0.1.2`, and `python/logbrew_django/v0.1.2`; kept `js/logbrew-js/v0.1.3` as latest. Release workflow runs `27461505966`, `27461557833`, `27461558125`, `27461558501`, and `27461558853` all succeeded, and log assertions proved `PUBLISH_PACKAGES: false` plus `RELEASE_KIND: scoped` for each. `gh run list` showed no new `Publish Packages` dispatch after these scoped Releases. Continue creating future package-specific GitHub Releases only from guarded tags at current `main`, with registry proof in notes and `--latest=false` unless intentionally changing the Releases page default.
- 2026-06-13: Java core now matches JS/Python/Go agent-readable timeline UX with `ProductTimeline.productAction(...)` and `ProductTimeline.networkMilestone(...)` builders. They emit existing action events, keep primitive metadata only, strip query/hash/full URL text from route templates, normalize HTTP methods, infer network failure from 4xx/5xx status codes, preserve trace/session correlation, and avoid visual replay, global HTTP patching, payload capture, or header capture. Thermo review replaced a varargs metadata helper with direct typed metadata construction before staging. Local evidence: current Sentry Java breadcrumb docs, OpenTelemetry Java instrumentation/context-propagation docs, and Datadog Java custom instrumentation docs reinforce explicit app-owned timeline/span metadata; `bash scripts/check_java_package.sh`, `bash scripts/real_user_java_smoke.sh`, markdown links, confidentiality scan, release metadata, generated-artifact hygiene, and `git diff --check` passed. Next parity priority: .NET/PHP/Ruby/Rust timeline helpers before broad release confidence.
- 2026-06-13: .NET core now matches JS/Python/Go/Java agent-readable timeline UX with `ProductTimeline.ProductAction(...)` and `ProductTimeline.NetworkMilestone(...)` builders. They emit existing action events, copy primitive metadata defensively, strip query/hash/full URL text from route templates, normalize HTTP methods, infer network failure from 4xx/5xx status codes, preserve trace/session correlation, and avoid visual replay, HTTP client patching, payload capture, or header capture. Thermo review kept the logic in a focused new file instead of growing `LogBrew.cs` past its current 1k+ size and tightened internal metadata/status typing before staging. Local evidence: current Sentry .NET breadcrumb docs, OpenTelemetry context-propagation docs, and Datadog custom instrumentation docs reinforce explicit app-owned timeline/span metadata; `bash scripts/check_dotnet_package.sh`, `bash scripts/real_user_dotnet_smoke.sh`, markdown links, confidentiality scan, ShellCheck 0.11.0, release metadata, generated-artifact hygiene, and `git diff --check` passed. Next parity priority: PHP/Ruby/Rust timeline helpers before broad release confidence.
- 2026-06-13: Backend contract coordination reports stored/returned severity aliases are canonicalized (`warn` returns as `warning`, `fatal` issues return as `critical`) while ingest accepts runtime aliases, and `GET /api/logs` accepts canonical `severity=...` plus legacy `level=...`. SDKs may keep mapping native runtime names into accepted aliases, but public examples/copy and any API-read docs should prefer canonical `info`/`warning`/`error`/`critical` and `severity`, not raw `warn`/`fatal` or `level`. Release and environment remain required on first-party telemetry ingest.
- 2026-06-13: PHP core now matches JS/Python/Go/Java/.NET agent-readable timeline UX with `ProductTimeline::productAction(...)` and `ProductTimeline::networkMilestone(...)` builders. They emit existing action events, copy primitive metadata defensively, strip query/hash/full URL text from route templates, normalize HTTP methods, infer network failure from 4xx/5xx status codes, preserve trace/session correlation, validate reserved `source` metadata too, and avoid visual replay, HTTP client patching, payload capture, or header capture. Thermo review kept the logic in focused `ProductTimeline.php` and fixed a reserved-key validation gap plus a smoke script ordering bug before staging. Local evidence: `composer update --no-interaction && php tests/run.php`, `python3 scripts/check_php_sources.py`, `bash scripts/check_php_static.sh`, `bash scripts/real_user_php_smoke.sh` with Composer archive/install/remove/reinstall, ProductTimeline runtime, reflection, and phpstan consumer proof, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, generated-artifact hygiene, and `git diff --check` passed. Next parity priority: Ruby/Rust timeline helpers before broad release confidence.
- 2026-06-13: Ruby core now matches JS/Python/Go/Java/.NET/PHP agent-readable timeline UX with `LogBrew::ProductTimeline.product_action(...)` and `LogBrew::ProductTimeline.network_milestone(...)` builders. They emit existing action events, copy primitive metadata defensively, strip query/hash/full URL text from route templates, normalize HTTP methods, infer network failure from 4xx/5xx status codes, preserve trace/session correlation, validate reserved `source` metadata while keeping SDK source authoritative, and avoid visual replay, `Net::HTTP` patching, payload capture, or header capture. Thermo review avoided pushing `lib/logbrew.rb` over 1k lines by placing logic in focused `lib/logbrew/product_timeline.rb` and removed duplicate action-status constants in favor of canonical `LogBrew::ACTION_STATUSES`. Local evidence: Ruby syntax checks, `bash scripts/check_ruby_package.sh`, `bash scripts/real_user_ruby_smoke.sh` with gem build/unpack/install/remove/reinstall and installed ProductTimeline proof, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, generated-artifact hygiene, and `git diff --check` passed. Next parity priority: Rust timeline helpers before broad release confidence.
- 2026-06-13: Rust core now matches JS/Python/Go/Java/.NET/PHP/Ruby agent-readable timeline UX with `ProductTimeline::product_action(...)` and `ProductTimeline::network_milestone(...)` builders. They emit existing action events, copy primitive metadata defensively, strip query/hash/full URL text from route templates, normalize HTTP methods, infer network failure from 4xx/5xx status codes, preserve trace/session correlation, validate reserved `source` metadata while keeping SDK source authoritative, and avoid visual replay, HTTP client patching, payload capture, or header capture. Thermo review kept `lib.rs` under 1k lines by placing logic in focused `src/product_timeline.rs` and reused canonical `ACTION_STATUSES`. Local evidence: `cargo fmt --check`, `cargo test --manifest-path rust/logbrew/Cargo.toml`, `cargo clippy --manifest-path rust/logbrew/Cargo.toml --all-targets --all-features -- -D warnings`, `cargo test --manifest-path rust/logbrew/Cargo.toml --all-features`, `bash scripts/real_user_rust_smoke.sh` with crate package/install/remove/reinstall, rustdoc, ProductTimeline timeline binary, and optional HTTP 503-to-202 proof, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, generated-artifact hygiene, and `git diff --check` passed. Next priority: decide whether to create simple repo-wide GitHub Releases for publishable version bumps or guarded scoped release-note tags for already-published scoped packages.
- 2026-06-13: Release safety now has an explicit repo-wide version guard: `scripts/check_repo_wide_release_versions.py vX.Y.Z` verifies that every package manifest published by the simple `vMAJOR.MINOR.PATCH` GitHub Release path already matches the tag before `publish-release.yml` dispatches registry jobs. This prevents accidental `v0.1.1` repo-wide Releases while package versions are mixed (`@logbrew/sdk@0.1.3`, FastAPI/Django `0.1.2`, many 0.1.0 packages). Use scoped/manual changed-package publishing until a deliberate all-package version alignment is chosen. Local evidence: focused unittest coverage, expected-failure `v0.1.1` matrix output, release metadata, markdown links, confidentiality scan, ShellCheck 0.11.0, generated-artifact hygiene, diff check, and thermo-nuclear review passed.
- 2026-06-13: Honest competitor gap recorded: LogBrew is still worse than Sentry/Datadog on source maps, native debug symbols, and build-artifact symbolication. Source/docs reviewed: Sentry JavaScript source-map docs plus `getsentry/sentry-javascript@4e12c7a` paths `dev-packages/test-utils/src/sourcemap-upload-utils.ts` and `packages/sveltekit/src/vite/sourceMaps.ts`; Datadog source-map docs plus `DataDog/datadog-ci@74ed439` paths under `packages/base/src/commands/sourcemaps/`, `helpers/git/format-git-sourcemaps-data.ts`, and `commands/react-native/injectDebugId.ts`. Added `docs/competitor-research/source-maps-debug-symbols-2026-06-13.md` and backend contract report `docs/backend-contracts/release-artifact-symbolication-2026-06-13.md`. Backend handoff remains pending because no backend automation/thread target was exposed. Next priority: backend release-artifact API contract, then a dependency-light JS artifact tool with dry-run manifest and real Vite/Next/React Native symbolication proof before public support claims.
- 2026-06-13: First SDK-side source-map gap reduction landed as `scripts/create_js_release_artifact_manifest.py`, a dependency-free local dry-run manifest generator for JavaScript build output. It validates release/environment/service/minified-path-prefix metadata, minified files, source-map presence, source-map JSON shape, matching debug IDs, query/hash-stripped minified URLs, SHA-256/byte-size metadata, and sensitive `sourcesContent` default blocking while emitting no raw source content. Focused tests in `tests/test_js_release_artifact_manifest.py` cover ready manifests, missing maps, `sourcesContent` blocking/explicit allow, debug-ID mismatch, and CLI nonzero exit on blocked manifests. This is not source-map support yet: no backend upload, auth, storage, lookup, or real minified stack symbolication proof exists. Backend handoff remains pending without a target; next priority is fake-intake upload validation and Vite/Next/React Native proof only after the backend contract is actionable.
- 2026-06-13: Second SDK-side source-map gap reduction landed as `scripts/prepare_js_release_artifact_debug_ids.py`, a dependency-free dry-run-first Debug ID preparation tool for built JavaScript/source-map pairs. It plans matching Debug IDs, blocks mismatches, propagates map-only IDs into minified files, writes only with explicit `--write`, keeps sourceMappingURL as the final line, is idempotent after mutation, and emits no raw source content. Focused tests in `tests/test_js_release_artifact_debug_ids.py` cover dry-run no-write behavior, explicit write, idempotency, map-only propagation, mismatch blocking without partial writes, and CLI nonzero exit on blocked plans. This still is not source-map support: no backend upload, auth, storage, lookup, or real minified stack symbolication proof exists. Backend handoff remains pending without a target; next priority remains fake-intake upload validation and Vite/Next/React Native proof only after the backend contract is actionable.
- 2026-06-14: JavaScript release artifact tooling now has `scripts/real_user_js_release_artifact_smoke.sh`, a local real-user smoke that creates temporary build output, proves Debug ID prep dry-run, explicit `--write`, idempotency, manifest blocking for sensitive `sourcesContent`, explicit allow, query/hash stripping, and no raw source content in emitted manifests. Thermo review caught bytecode artifact leakage; the smoke now exports `PYTHONDONTWRITEBYTECODE=1`. Local evidence: `bash scripts/real_user_js_release_artifact_smoke.sh`, `bash scripts/check_shell_static.sh`, focused Python source checks, full unittest discovery, markdown links, confidentiality scan, generated-artifact hygiene, and diff check passed. This is still not source-map support; backend upload/storage/lookup/symbolication proof remains the blocker.
- 2026-06-14: Backend coordination is now mechanically gated. `scripts/check_backend_contract_reports.py` and `tests/test_backend_contract_reports.py` validate every `docs/backend-contracts/*.md` report for title, status/handoff, priority (`P0`-`P3`), user impact, expected backend capability/API or event fields, observed SDK gap, and concrete verification evidence. The release-artifact symbolication backend report now carries `P1` priority while handoff remains pending because no backend automation/thread tool is exposed. Keep future backend-relevant ideas/gaps passing this checker before committing.
- 2026-06-14: Backend contract report validation is now a named gate in both `.github/workflows/ci.yml` and `.github/workflows/release-readiness.yml`, not only an indirect unittest assertion. This keeps backend-relevant SDK/product gaps visible during PR, push, and manual release-readiness checks before any release claims or registry work.
- 2026-06-14: The broad local `scripts/check_public_sdks.sh` verifier now also has a dedicated "Backend contract report checks" step, so maintainers running the full public SDK readiness path get the same backend handoff/report validation that CI and release-readiness enforce.
- 2026-06-15: `tests/test_check_public_sdks.py` now has regression coverage that the broad public verifier still lists and executes `python3 scripts/check_backend_contract_reports.py`, preventing future script edits from silently dropping the backend contract gate.
- 2026-06-15: The broad public SDK verifier now runs `bash scripts/real_user_js_release_artifact_smoke.sh` before generated-artifact hygiene, so release-artifact dry-run/Debug ID/source-map privacy proof is included in the full readiness path without claiming backend upload or symbolication support. `tests/test_check_public_sdks.py` also checks declared step labels match executable `begin_step` calls.
- 2026-06-15: `docs/sdk-readiness-checklist.md` now lists `bash scripts/real_user_js_release_artifact_smoke.sh` under release checks, matching the public verifier while preserving the no-backend-symbolication claim boundary.
- 2026-06-15: CI and release-readiness now also run the named `Run JavaScript release artifact smoke` step, and `tests/test_release_artifact_smoke_gates.py` guards CI, release-readiness, and checklist alignment for the smoke command.
- 2026-06-15: Source-map/debug-symbol contract docs now record the latest official-doc drift: Sentry still centers Debug IDs for source-map matching, while Datadog documents an optional build plugin that uploads `.js`/`.map` pairs with Git metadata during builds. The backend contract report now also records that release-artifact smoke proof is green in CI `27526101433`, release-readiness `27526535467`, public verifier, and checklist, while preserving the no-upload/no-symbolication claim boundary.
- 2026-06-15: GitHub branch protection is now enabled on `main` with strict required status check context `Contract checks`, force pushes disabled, and branch deletion disabled; PR reviews/admin enforcement remain off so existing maintainer workflows are not unexpectedly locked down. `docs/github-actions.md` now says to keep that protection instead of listing initial enablement as future work.
- 2026-06-15: GitHub environment protection is now enabled for the shared `release` environment with a branch-policy rule and `deployment_branch_policy.protected_branches=true`, `custom_branch_policies=false`; admin bypass remains enabled by the environment. This matches the current release path because `publish-release.yml` dispatches `publish-packages.yml` from the default protected branch while `publish-packages.yml` checks out the requested release tag or commit via its `ref` input. Evidence: `gh api repos/LogBrewCo/sdk/environments/release --jq '{name, protection_rules, deployment_branch_policy, can_admins_bypass}'`.
- 2026-06-15: Release-safety drift now has a local verifier: `python3 scripts/check_github_release_safety.py` checks that `main` has strict required `Contract checks`, force pushes and branch deletion disabled, and the shared `release` environment deploys only from protected branches. The checker supports fixture JSON so unit tests can cover the policy without live GitHub access, and it fails closed when protection fields are missing. Local evidence: live checker passed, `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests`, Python source syntax, markdown links, confidentiality scan, generated-artifact hygiene, and diff check passed.
- 2026-06-15: GitHub release-safety drift is now mechanically gated beyond local maintainer runs: CI `Contract checks` and release-readiness `Contract checks` run `python3 scripts/check_github_release_safety.py --allow-public-branch-summary` because built-in Actions auth cannot read full branch-protection settings, while the broad `scripts/check_public_sdks.sh` verifier keeps the full admin-read `python3 scripts/check_github_release_safety.py` check. `tests/test_github_release_safety_gates.py` and `tests/test_check_public_sdks.py` guard that wiring. Local evidence: live release-safety check, workflow YAML parse, focused gate tests, full unittest discovery, shell static analysis, Python syntax, markdown links, confidentiality scan, generated-artifact hygiene, and diff check passed.
- 2026-06-14: `docs/sdk-readiness-checklist.md` now lists `python3 scripts/check_backend_contract_reports.py` in release checks, matching CI, release-readiness, and the broad public SDK verifier so backend-relevant SDK gaps stay visible in both docs and automation.
- 2026-06-15: Competitor comparison must stay real-install and first-useful-signal driven, not only docs-based. Current Node evidence from isolated npm installs: `@logbrew/sdk@0.1.3` + `@logbrew/node@0.1.0` installed in 0.8s with 200 KiB/25 files/2 lock entries, versus `@sentry/node@10.57.0` 2.2s/43,372 KiB/5,556 files/20 entries, `dd-trace@5.108.0` 11.8s/99,580 KiB/2,510 files/58 entries, OpenTelemetry quickstart packages 6.4s/76,260 KiB/8,184 files/197 entries, and `posthog-node@5.37.0` 1.7s/3,176 KiB/475 files/3 entries. Added `docs/competitor-research/javascript-node-first-telemetry-2026-06-15.md`, `js/logbrew-node/examples/first-useful-telemetry.mjs`, and Node README first-useful telemetry guidance. Thermo review caught a bad shared-client lifecycle pattern; keep startup release/environment flushed with a startup client and let `withLogBrewHttpHandler` own per-request client shutdown. Local evidence: `bash scripts/real_user_node_smoke.sh` on Node v22.18.0, `npm test` in `js/logbrew-node`, `python3 scripts/check_js_sources.py`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh` with publint 0.3.21, markdown links, confidentiality scan, generated-artifact hygiene, and diff check passed.
- 2026-06-15: Python first-useful telemetry comparison used isolated PyPI installs and official/source docs for Sentry, Datadog, OpenTelemetry, and PostHog. `logbrew-sdk==0.1.1` measured 115 KiB/21 files/1 distribution versus Sentry 4,987 KiB/486 files, Datadog `ddtrace` 28,455 KiB/2,230 files, OpenTelemetry quickstart packages 51,415 KiB/1,494 files/21 distributions, and PostHog 6,786 KiB/437 files. Added `docs/competitor-research/python-first-telemetry-2026-06-15.md`, a packaged `first-useful-telemetry` Python example, README guidance, and installed smoke coverage proving release/environment/log/action/network/metric/span output with W3C trace linkage and no query/payload/header leakage. Keep Python logger capture app-owned and explicit; do not clear app handlers or add global logging/HTTP monkeypatch defaults. Local evidence: direct packaged example schema/privacy proof, `bash scripts/check_python_static.sh`, `python3 scripts/check_python_sources.py`, focused and full Python unittest discovery, `bash scripts/real_user_python_smoke.sh`, markdown links, confidentiality scan, generated-artifact hygiene, and diff check passed.
- 2026-06-15: Documentation coordination rule from user clarification: when SDK work creates a docs gap outside the files being edited, notify a documentation automation/thread if a concrete target/tool is discoverable. If no docs target is exposed, record documentation handoff pending in memory/automation and update repo-local public docs directly when the needed docs are in scope. Do not claim a docs automation was informed unless an actual message was sent.
- 2026-06-15: Go first-useful telemetry comparison used isolated `go get` installs plus current official/source docs for Sentry, Datadog, OpenTelemetry, and PostHog. `github.com/LogBrewCo/sdk/go/logbrew@v0.1.1` measured 76 KiB/13 files/1 module versus Sentry Go 49,368 KiB/1,223 files/17 modules, Datadog tracer 76,552 KiB/6,643 files/262 modules with an automatic Go 1.25.0 toolchain download, OpenTelemetry quickstart packages 34,983 KiB/1,844 files/30 modules, and PostHog Go 12,462 KiB/869 files/15 modules. Added `docs/competitor-research/go-first-telemetry-2026-06-15.md`, packaged `go/logbrew/examples/first_useful_telemetry`, README guidance, Makefile target, unit coverage, and focused `scripts/check_go_first_useful_payload.py` installed smoke proof for release/environment/log/product action/network milestone/metric/span output with W3C trace linkage and no query/payload/header leakage. Keep Go app-owned and dependency-free; next first-useful parity targets are Java, .NET, PHP, Ruby, and Rust. Local evidence: direct example fixture validation, `go test ./...`, `go vet ./...`, `bash scripts/check_go_static.sh` with staticcheck 2025.1.1, `python3 scripts/check_python_sources.py`, `bash scripts/real_user_go_smoke.sh`, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, markdown links, confidentiality scan, and generated-artifact hygiene passed.
- 2026-06-15: Java first-useful telemetry comparison used isolated Gradle/Maven Central resolution plus current official/source docs for Sentry, Datadog, OpenTelemetry, and PostHog. Published `co.logbrew:logbrew-sdk:0.1.0` measured 36.3 KiB/1 jar/30 entries versus Sentry Java 1,192.9 KiB/800 entries, Datadog Java agent 33,271.6 KiB/18,733 entries, OpenTelemetry Java agent 23,643.6 KiB/16,097 entries, and PostHog Java 3,712.2 KiB/9 jars/2,097 entries. Added dependency-free `Traceparent` W3C helpers, packaged `FirstUsefulTelemetry` example, Java README guidance, competitor report, and focused `scripts/check_java_first_useful_payload.py` installed smoke proof for release/environment/log/product action/network milestone/metric/span output with W3C trace linkage and no query/payload/header leakage. Thermo review removed a hidden `toMap()` validation side effect so Traceparent stays a trace-context helper and canonical span validation remains in `LogBrewClient.span(...)`. Local evidence: `bash scripts/check_java_static.sh`, `bash scripts/check_java_package.sh` with 30 tests, `bash scripts/real_user_java_smoke.sh`, `python3 scripts/check_python_sources.py`, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, markdown links, confidentiality scan, generated-artifact hygiene, and `git diff --check` passed. Next first-useful parity targets are .NET, PHP, Ruby, and Rust.
- 2026-06-15: Backend leadership coordination note: SDK owns public SDK UX and client behavior around stable ingestion/auth contracts only. Backend owns shared auth/account behavior, provider/OAuth setup, sessions/key lifecycle, API contracts, ingestion, WebSocket behavior, storage/durability, symbolication/source-map backend contracts, and deploy/ops. On every cycle or whenever discovered, report backend-related auth/API/ingest/WebSocket/storage/deploy/contract issues to backend automation with expected SDK/API shape, observed response/error/gap, reproduction, verifier evidence, user impact, workaround, priority, and requested backend action. Keep reports public-safe and do not inspect non-public backend state. Current SDK docs may describe SDK-key delivery headers and unauthenticated transport errors as part of the stable ingestion contract, but not account/provider internals.
- 2026-06-15: .NET first-useful telemetry comparison used isolated NuGet installs plus current official/source docs for Sentry, Datadog, OpenTelemetry, and PostHog. `LogBrew` 0.1.0 measured 10,652 KiB/214 files including `Microsoft.Extensions.Logging`, versus Sentry.Extensions.Logging 240,044 KiB/467 files, Datadog.Trace 952 KiB/17 files but Datadog.Trace.Bundle 624,348 KiB/178 files for per-app tracer setup, OpenTelemetry quickstart packages 23,336 KiB/522 files, and PostHog 22,968 KiB/512 files. Added dependency-free `Traceparent` W3C helpers, packaged `FirstUsefulTelemetry.cs`, .NET README guidance, competitor report, and focused `scripts/check_dotnet_first_useful_payload.py` installed smoke proof for release/environment/log/product action/network milestone/metric/span output with W3C trace linkage and no query/payload/header leakage. Keep .NET explicit and app-owned: no global HTTP patching, no payload/header capture, no visual replay, stack text remains opt-in. Local evidence: `bash scripts/check_dotnet_package.sh` with 23 tests, `bash scripts/real_user_dotnet_smoke.sh`, `python3 scripts/check_python_sources.py`, `python3 scripts/check_release_metadata.py`, `bash scripts/check_shell_static.sh` with ShellCheck 0.11.0, full unittest discovery, markdown links, confidentiality scan, generated-artifact hygiene, and `git diff --check` passed. Next first-useful parity targets are PHP, Ruby, and Rust; next .NET gap is an app-owned ASP.NET Core request helper with W3C child spans, query-free route templates, and opt-in request duration metrics.
- 2026-06-15: Backend coordination update: account persistence and data storage remain backend-owned alongside auth/account/provider/session lifecycle and API/ingestion contracts. SDKs should consume stable ingestion/auth behavior only and report any auth/account/ingest/API drift with expected SDK/API shape, observed response/error/gap, reproduction, verifier evidence, user impact, workaround, priority, and requested backend action. No backend drift was found in the .NET first-useful work; no backend thread tool was exposed here beyond sub-agent controls, so handoff remains pending unless a concrete backend issue appears or a target becomes available.
- 2026-06-15: Backend owner directive to preserve public auth/account semantics: different app surfaces may resolve identity through different callback/session mechanics, but once provider identity resolves they represent the same LogBrew account with the same subscription, projects, telemetry, account lifecycle, and API access. SDKs must not invent account, subscription, project-scope, key, provider, removal/recovery, ingest acceptance, or API semantics; if tests/examples reveal drift, report expected contract, observed behavior, reproduction, impact, workaround, and requested backend action with sensitive values omitted.
- 2026-06-15: User wants periodic honest competitor-status reports, not just implementation summaries. After each meaningful compare/improvement cycle, notify with a concise report covering current status, where LogBrew is better and more useful, where it is worse, what must be improved next, what evidence changed, and which prompt/memory lessons were added. If no meaningful evidence changed, stay quiet rather than sending a stale report.
- 2026-06-15: PHP first-useful telemetry comparison used isolated Composer installs plus current official/source docs for Sentry, Datadog, OpenTelemetry, and PostHog. Current public Packagist `logbrew/sdk` 0.1.0 measured 3,568 KiB/399 files, smaller than Sentry Laravel 13,424 KiB, Datadog 11,812 KiB, and OpenTelemetry quickstart 6,904 KiB, but worse than Sentry core 1,948 KiB and PostHog PHP 588 KiB. The local subtree archive consumed by `scripts/real_user_php_smoke.sh` measured 164 KiB/20 files, so the next PHP packaging focus is to make public Packagist distribution match the lean subtree path before making footprint claims. Added dependency-free `Traceparent` W3C helpers, packaged `first_useful_telemetry.php`, README guidance, `docs/competitor-research/php-first-telemetry-2026-06-15.md`, and focused `scripts/check_php_first_useful_payload.py` installed smoke proof for release/environment/log/product action/network milestone/metric/span output with W3C trace linkage and no query/payload/header leakage. Keep PHP explicit and app-owned: no Laravel/Symfony middleware yet, no global HTTP patching, no payload/header capture. Local evidence: `composer update --no-interaction`, `python3 scripts/check_php_sources.py`, `php php/logbrew-php/tests/run.php`, direct first-useful payload validation, `bash scripts/check_php_static.sh`, `bash scripts/real_user_php_smoke.sh`, `bash scripts/check_shell_static.sh`, `python3 scripts/check_release_metadata.py`, `python3 scripts/check_markdown_links.py`, `python3 scripts/check_confidentiality_scan.py`, full unittest discovery, generated-artifact hygiene, and `git diff --check` passed. Next PHP priorities are public package trimming and a thin framework-owned request helper with W3C child spans, query-free route templates, and opt-in request duration metrics; next first-useful parity targets are Ruby and Rust.
- 2026-06-15: Backend owner status update: backend native telemetry insert behavior is reported ready in backend code with backend quick gate and real-user flows passing. SDK lane should preserve stable public DTO/client behavior and continue reporting backend-affecting storage/ingest/WebSocket/auth/API drift with expected contract, observed behavior, reproduction, impact, workaround, and requested backend action. No backend drift was found in the PHP first-useful telemetry pass.
- 2026-06-15: Backend/website/mobile convergence for SDK install tracking and usage limits is now recorded in `docs/backend-contracts/sdk-install-usage-limits-2026-06-15.md`. SDK constraints: all core SDKs can reliably provide `api_key`/ingest key, `sdk_name`, and `sdk_version`; runtime/platform can be derived by SDK package; release/environment are reliable on first telemetry only when the app provides them; project id/key id require backend-provided public-safe fields. V1 recommendation: first accepted telemetry should be the primary installed-and-working proof; optional setup check-in should only exist after backend publishes a write-only scoped endpoint and redacted response envelope. Usage/over-limit behavior remains backend-owned; SDKs should surface stable redacted codes and avoid aggressive retry without backend retry hints. Backend handoff remains pending because no backend thread/automation sender is exposed here.
- 2026-06-15: Ruby first-useful telemetry comparison used isolated RubyGems installs on local Ruby 2.6.10 plus current official/source docs for Sentry, Datadog, OpenTelemetry, and PostHog. Public `logbrew-sdk` 0.1.0 installed at 72 KiB/7 files; local package after this pass installs at 100 KiB/10 files. Current latest Sentry Ruby, Datadog, OpenTelemetry Ruby, and PostHog Ruby packages did not complete installation on Ruby 2.6.10 due runtime-version constraints, so their partial dependency folders are install-friction evidence, not complete footprint claims. Added dependency-free `LogBrew::Traceparent` W3C helpers, packaged `first_useful_telemetry.rb`, README guidance, `docs/competitor-research/ruby-first-telemetry-2026-06-15.md`, and focused `scripts/check_ruby_first_useful_payload.py` installed-smoke proof for release/environment/log/product action/network milestone/metric/span output with W3C trace linkage and no query/body/header leakage. Keep Ruby explicit and app-owned: no Rack/Rails global side effects, no `Net::HTTP` patching, no body/header capture. Next Ruby priority is a thin Rack/Rails request helper with W3C child spans, query-free route templates, and opt-in request duration metrics; next first-useful parity target is Rust.
- 2026-06-15: Backend dogfooding coordination: backend services may dogfood LogBrew SDKs only after a backend-minted project-scoped write-only ingest configuration path is ready enough. SDK docs/examples must never recommend account/session bearer values as SDK ingest configuration, must keep release/environment and canonical severity vocabulary explicit, and must treat backend dogfooding friction as SDK-owned unless the report reveals a missing backend contract. No backend drift was found in the Ruby first-useful pass, and no backend/docs automation sender was exposed beyond repo-local documentation updates.
- 2026-06-15: Rust first-useful telemetry comparison used isolated Cargo installs plus current official/source docs for Sentry, Datadog, OpenTelemetry, and PostHog. `logbrew` 0.1.0 measured 13 lock entries and 9,552 KiB/527 Cargo-cache files versus Sentry 268 entries/464,876 KiB, OpenTelemetry quickstart 46 entries/44,592 KiB, Datadog `ddtrace` 162 entries/148,528 KiB, and `posthog-rs` 261 entries/460,628 KiB. Added dependency-free `Traceparent` W3C helpers, public `Metadata`/`MetadataValue` aliases so real users do not need a direct `serde_json` dependency for metadata snippets, packaged `first_useful_telemetry.rs`, README guidance, `docs/competitor-research/rust-first-telemetry-2026-06-15.md`, and focused `scripts/check_rust_first_useful_payload.py` installed-smoke proof for release/environment/log/product action/network milestone/metric/span output with W3C trace linkage and no query/body/header leakage. Thermo review simplified the API by adding `Traceparent::span_attributes_from_context(...)` so examples parse once and reuse validated context. Local evidence: `cargo fmt --check`, `cargo clippy --manifest-path rust/logbrew/Cargo.toml --all-targets --all-features -- -D warnings`, `cargo test --manifest-path rust/logbrew/Cargo.toml --all-features` with 22 tests, direct first-useful payload validation, `bash scripts/real_user_rust_smoke.sh`, Python source syntax, ShellCheck 0.11.0, release metadata, markdown links, confidentiality scan, backend contract reports, generated-artifact hygiene, and `git diff --check` passed. Rust is now first-useful parity-complete with JS/Python/Go/Java/.NET/PHP/Ruby; next Rust focus is a thin Tower/Axum request helper with W3C child spans, query-free route templates, and opt-in request duration metrics. Backend dogfooding remains pending until backend-owned project-scoped ingest configuration is deployed; no backend/docs automation sender was exposed in this cycle.
- 2026-06-15: Rust HTTP server request telemetry gap reduced after reviewing current official/source competitor signals: Sentry Axum docs plus `getsentry/sentry-rust@e33b7ff20eb5bf948eacf89d7eecdcc59b31d4f3` `sentry-tower/src/lib.rs`, OpenTelemetry Rust getting-started docs plus `open-telemetry/opentelemetry-rust@88821497a893ff6dd4dd916621a2224394ebb0a4` trace-context propagation source, and Datadog Rust library docs. Added dependency-free `HttpRequestTelemetry`, shared `http_fields` route/method/status/duration helpers, packaged `examples/http_server_request.rs`, README request-helper guidance, `docs/competitor-research/rust-http-server-request-2026-06-15.md`, and `scripts/check_rust_http_server_payload.py` proof. The helper builds a request span plus optional `http.server.duration` metric, continues valid incoming W3C `traceparent`, falls back non-fatally for malformed propagation, emits outgoing `traceparent`, sanitizes route templates, normalizes methods, captures status class, maps `5xx` to span `error`, and avoids payload/header/query/hash capture. Thermo review removed a private one-use optional-duration trait and kept request logic in focused modules; `lib.rs` remains under 1k lines while `scripts/real_user_rust_smoke.sh` is still a large legacy verifier to split later. Local evidence: `cargo fmt`, `cargo test --manifest-path rust/logbrew/Cargo.toml --all-features` with 24 tests, `cargo clippy --manifest-path rust/logbrew/Cargo.toml --all-targets --all-features -- -D warnings`, direct packaged example payload validation, `bash scripts/real_user_rust_smoke.sh` with 18 packaged files/138.1 KiB/27.4 KiB compressed, Python source syntax, ShellCheck 0.11.0, release metadata, markdown links, confidentiality scan, backend contract reports, generated-artifact hygiene, and `git diff --check` passed. Honest status: LogBrew is now lighter and more explicit than Sentry/OpenTelemetry for app-owned Rust request spans/metrics, but still worse for drop-in Tower/Axum middleware, Rust `tracing` subscriber/layer conversion, Actix/Rocket examples, source-map/native symbolication, and backend-owned setup/usage/quota contracts. Next Rust focus: installed Axum or Tower mini-app smoke showing exact optional middleware glue, then evaluate a minimal `tracing` bridge. No backend drift was found and no backend/docs automation sender was exposed beyond repo-local docs/memory updates.
- 2026-06-15: Backend setup/ingest-key coordination update, public-safe only: the project setup and ingest-key contract is reported implemented but not production-confirmed, so public SDK docs/examples must not claim availability yet. When backend confirms availability, SDKs should accept a project-scoped write-only ingest-key DTO with public fields such as `id`, `label`, `kind`, `created_at`, `expires_at`, and a separate raw key value for one-time setup; raw one-time keys use a distinct `lbw_ingest_` prefix and authorize telemetry only for the matching `project_id`; account/session bearer values must never be documented, printed, persisted, or reused as SDK ingest keys. Invalid, expired, or wrong-project keys should surface a redacted `ingest_key_invalid` error with an actionable next step and no echoed key text. Keep release/environment explicit in telemetry examples and canonical severity vocabulary `info`/`warning`/`error`/`critical`. Record SDK friction back to backend with runtime, reproduction, expected vs observed behavior, impact, and whether any workaround is key-safe. Updated `docs/backend-contracts/sdk-install-usage-limits-2026-06-15.md`; no public SDK README/example availability claims were changed.
- 2026-06-15: Backend usage/limit coordination update, public-safe only: configurable account usage enforcement is reported present in backend code but not production-confirmed, so SDK docs/examples must not claim availability yet. When backend confirms readiness, native ingest may return HTTP `429` with a redacted JSON envelope containing `code: "usage_limit_exceeded"`, `limit: "events" | "bytes"`, `reset_at`, `error`, and actionable `next`; SDKs should surface it cleanly, redact keys, avoid aggressive retry, and not derive quota locally because `GET /api/account/usage` is backend-owned. Account/session bearer values remain separate from project-scoped write-only ingest keys. Updated `docs/backend-contracts/sdk-install-usage-limits-2026-06-15.md`; no SDK implementation was added.
- 2026-06-15: Backend live-usage coordination update, public-safe only: backend code reportedly publishes live account usage updates after successful native ingest, but production verification is pending and no SDK implementation is requested. Future stable behavior should use backend-owned event types `usage_updated`, `usage_limit_warning`, and `usage_limit_blocked` with the same redacted payload shape as `GET /api/account/usage`; SDKs should not calculate quota locally and should keep HTTP `429` `usage_limit_exceeded` as the hard-block path that is surfaced cleanly and not retried aggressively. Updated `docs/backend-contracts/sdk-install-usage-limits-2026-06-15.md`; no public availability claims were added.
- 2026-06-15: Rust Axum request integration gap reduced without adding default dependency weight. Added packaged `examples/axum_request_middleware.rs`, public README guidance, `scripts/check_rust_axum_payload.py`, and generated `axum-app` installed smoke proof showing Axum `route_layer`/`from_fn_with_state` glue around `HttpRequestTelemetry`. The proof reads only W3C `traceparent`, uses Axum's matched route template `/checkout/{cart_id}` instead of raw URI/query, emits request span plus `http.server.duration` metric, returns outgoing `traceparent`, and keeps Axum/Tokio/Tower outside the default `cargo add logbrew` dependency path. Honest status: LogBrew is now better for lightweight app-owned Axum request telemetry than a full Sentry/OpenTelemetry stack, but still worse for one-line Tower/Axum `Layer`, existing `tracing` span/log conversion, and Actix/Rocket examples. Verifier lesson: public-check tests should assert label ordering or parse the step list instead of hard-coding step numbers, because adding a legitimate smoke step shifts later step IDs. Next Rust focus should evaluate a minimal optional `tracing` bridge or Tower layer only if route ownership and privacy stay explicit.
- 2026-06-15: Rust now has an optional `tower` feature with `TowerRequestTelemetryLayer`, `TowerRequestIds`, and an Axum `route_layer` example that queues the same safe request span plus `http.server.duration` metric through app-owned client state. The layer reads only W3C `traceparent`, relies on caller-provided route extraction and trace/span IDs, injects outgoing `traceparent`, ignores telemetry failures without interrupting responses, and keeps Tower/Axum/Tokio out of default installs. Installed Axum proof now uses `cargo add logbrew --features tower`; core Rust smoke avoids duplicate Axum execution and only checks package/Makefile presence. Package proof is now 21 files/162.9 KiB/33.4 KiB compressed. Backend setup/usage coordination added pending setup-start on safe ingest-key creation plus account usage DTO fields `percent_used`, `warning`, `blocked`, `limit`, and `next` to the backend contract report without claiming production availability. Local evidence: default Rust tests, all-feature Rust tests with 25 tests, clippy all-targets/all-features, format check, `bash scripts/real_user_rust_smoke.sh`, `bash scripts/real_user_rust_axum_smoke.sh`, ShellCheck 0.11.0, backend contract reports, release metadata, markdown links, confidentiality scan, unittest discovery, generated-artifact hygiene, and `cargo publish --dry-run --allow-dirty` passed. Thermo review split the Tower test into `tests/tower_layer.rs` so `tests/sdk_flow.rs` stayed under 1k lines. Remaining next Rust gap: a minimal optional `tracing` bridge and Actix/Rocket examples, only if privacy and route ownership remain explicit.
- 2026-06-15: Backend setup/ingest-key coordination clarification, public-safe only: backend-owned setup progress should not be recorded until project-scoped ingest-key storage succeeds. If setup tracking fails after safe key storage, backend is expected to still return the one-time key while avoiding unpersisted setup live updates. SDK guidance remains unchanged: do not add local setup markers, do not document account/session bearer values as SDK ingest config, and wait for production-confirmed backend-minted project-scoped write-only ingest keys before changing public setup snippets.
- 2026-06-15: Rust now has an optional `tracing` feature with `LogBrewTracingLayer`, converting app-owned `tracing` events into canonical LogBrew log events without default dependency weight or arbitrary field capture. The bridge composes as a `tracing_subscriber::Layer`, accepts an app-owned timestamp function, maps trace/debug to `info`, warn to `warning`, error to `error`, captures `tracingTarget`/`tracingLevel`, and only copies primitive fields explicitly allowlisted with `with_allowed_fields(...)`; route-template fields are query/hash sanitized and debug-formatted non-primitives are ignored even when allowlisted. It does not yet convert `tracing` spans. Competitor/current signals: `sentry-tracing` supports events/breadcrumbs/logs/spans with filters and custom mapping, `tracing-opentelemetry` remains the exporter baseline, and Datadog Rust remains preview/OpenTelemetry-oriented. Local evidence: `cargo fmt --check --manifest-path rust/logbrew/Cargo.toml`, default Rust tests, all-feature Rust tests with 27 tests, `cargo clippy --manifest-path rust/logbrew/Cargo.toml --all-targets --all-features -- -D warnings`, `bash scripts/real_user_rust_tracing_smoke.sh` installed proof, and `bash scripts/real_user_rust_smoke.sh` packaged proof passed; package proof is now 24 files/179.1 KiB/36.6 KiB compressed. Next Rust gap: privacy-bounded `tracing` span conversion or Actix/Rocket examples.
- 2026-06-15: Backend setup install tracking coordination, public-safe only: backend reports `POST /api/projects/{project_id}/setup/seen` is live for future SDK setup/init use when an SDK is initialized with a backend-minted project-scoped write-only ingest/client setup value. SDKs may call it once during setup/init, not per telemetry event; body may include `runtime` and `environment`; response is `ProjectSetupState` and may report `sdk_seen` before first telemetry. SDKs must still not accept, print, store, or repurpose account/session API values as ingest config, and public setup snippets should wait for SDK implementation around backend-minted setup values rather than adding local setup markers. Telemetry examples still require release plus environment and canonical severity values.
- 2026-06-16: Rust optional `tracing` bridge now supports explicit `with_span_events()` span conversion in addition to event-to-log conversion. It stays default-off, generates W3C-shaped trace/span IDs, derives parent/child links from active `tracing` spans, adds trace correlation to logs emitted inside converted spans, records span duration on close, and marks only the current span as `error` when an error-level event occurs inside it. Field capture remains primitive allowlist only, route fields are query/hash sanitized, debug-formatted non-primitives are ignored, and no payload/header/raw URL capture or subscriber replacement was added. Thermo review tightened the boundary so helper lookups pass only span correlation IDs instead of cloning full span state. Current competitor signals: Sentry `sentry-tracing` and `tracing-opentelemetry` both support richer span bridges/exporter stacks, while Datadog Rust still points toward OpenTelemetry/manual instrumentation. Local evidence: `cargo fmt --check --manifest-path rust/logbrew/Cargo.toml`, default Rust tests, all-feature Rust tests with 28 tests, `cargo clippy --manifest-path rust/logbrew/Cargo.toml --all-targets --all-features -- -D warnings`, `bash scripts/real_user_rust_tracing_smoke.sh`, `bash scripts/real_user_rust_smoke.sh`, markdown links, release metadata, backend contract reports, confidentiality scan, generated-artifact hygiene, `git diff --check`, and `cargo publish --dry-run --allow-dirty --manifest-path rust/logbrew/Cargo.toml` passed; package proof is now 24 files/189.7 KiB/38.3 KiB compressed. Honest gaps: Rust tracing span conversion is basic compared with Sentry/OTel because it does not consume an existing OTel context or model rich span events/exceptions; Actix/Rocket examples remain next Rust gaps.
- 2026-06-16: Rust Actix request telemetry gap reduced after reviewing current Sentry Actix docs, `sentry-actix`, `tracing-actix-web`, and `opentelemetry-instrumentation-actix-web` expectations for middleware-based request spans/metrics. Added packaged `examples/actix_request_middleware.rs`, README guidance, `scripts/check_rust_actix_payload.py`, `scripts/real_user_rust_actix_smoke.sh`, and CI/release-readiness/public-check integration. The generated `actix-app` proof installs default `logbrew` plus app-owned `actix-web@4`, reads only W3C `traceparent`, uses Actix's matched route `/checkout/{cart_id}` instead of raw URI/query, emits request span plus `http.server.duration`, injects outgoing `traceparent`, and rejects query/body/header leakage. Package proof is now 25 files/226.8 KiB/47.5 KiB compressed. Thermo review removed brittle public-check step renumbering by adding `begin_next_step` auto-numbering from `STEP_LABELS`, with runtime label-order validation; CI parser tests were updated accordingly. Local evidence: Rust format, default tests, all-feature tests with 28 tests, clippy all-targets/all-features, `bash scripts/real_user_rust_smoke.sh`, `bash scripts/real_user_rust_axum_smoke.sh`, `bash scripts/real_user_rust_actix_smoke.sh`, `bash scripts/real_user_rust_tracing_smoke.sh`, full `python3 -m unittest discover -s tests -p 'test_*.py'` with 92 tests, step-label parser proof for 62 public SDK steps, release metadata, markdown links, confidentiality scan, backend contract reports, ShellCheck 0.11.0, workflow YAML parsing, generated-artifact hygiene, and diff hygiene passed. Honest gap: Rust still lacks a Rocket example and richer externally supplied OpenTelemetry/Sentry-style span context/events; keep framework integrations optional and dependency-light.
- 2026-06-16: User explicitly raised tracing as the highest-value competitor gap because Sentry wins users on trace quality, and clarified this priority applies across all SDK languages, not only Rust. Treat Rust as the first concrete slice and reusable pattern, then improve tracing in popularity/evidence order across browser/JS frameworks, Node, Python, Go, Java, .NET, PHP, Ruby, mobile/native, and Rust: W3C context continuity, trace-log-error correlation, framework route spans, privacy-bounded metadata, and Sentry-level debugging usefulness without copying heavier defaults. Rust `LogBrewTracingLayer::with_span_events()` now closes one safe part by accepting explicit W3C `traceparent`/`trace_parent` fields on root `tracing` spans, continuing upstream trace ID/parent span ID, generating fresh local child span IDs, propagating sampled state into span/log metadata, ignoring malformed propagation non-fatally, and never serializing the raw propagation field. This keeps LogBrew lighter than Sentry/OpenTelemetry for app-owned Rust tracing while acknowledging remaining Rust gaps: no existing OpenTelemetry context ingestion, rich span events/exceptions, or Rocket example yet. Competitor evidence refreshed from Sentry Rust tracing/OpenTelemetry docs, `sentry-tracing`, `tracing-opentelemetry`, `OpenTelemetrySpanExt`, and OpenTelemetry context propagation docs. Local evidence: `cargo fmt --check --manifest-path rust/logbrew/Cargo.toml`, focused `cargo test --manifest-path rust/logbrew/Cargo.toml --features tracing tracing_layer` with 4 tracing tests, `cargo test --manifest-path rust/logbrew/Cargo.toml --all-features` with 29 Rust tests, `cargo clippy --manifest-path rust/logbrew/Cargo.toml --all-targets --all-features -- -D warnings`, `bash scripts/real_user_rust_tracing_smoke.sh`, `bash scripts/real_user_rust_smoke.sh`, and package proof at 25 files/232.0 KiB/48.1 KiB compressed.
- 2026-06-16: Node.js tracing correlation gap reduced as the first post-Rust all-language tracing slice. Current Sentry Node docs emphasize traces that link logs for diagnostic context, OpenTelemetry JS expects active context across async execution, Node's `AsyncLocalStorage` is stable for request-lifetime state, and Datadog proves user demand through Node log/trace injection. `@logbrew/node` now attaches `logbrew.trace` for valid W3C `traceparent`, exposes `getActiveLogBrewTrace()` inside async work started by the wrapped handler, reuses the same generated request span ID for the emitted request span, and passes trace context to custom request/error/flush/failure/error callbacks. Default handler-error capture now adds `traceId`, `spanId`, `parentSpanId`, and `sampled` metadata when a request trace exists. Public docs and the first-useful Node example now use `logbrew.trace` instead of manual `parseTraceparent` in app code, and the installed smoke proves async trace preservation plus error correlation from packaged artifacts. Keep this pattern for Express/Fastify/NestJS/Next.js next, then Python web frameworks and Go/Java/.NET server helpers: expose active trace context, correlate logs/errors, stay privacy-bounded, and avoid global logger/HTTP patching by default. Local evidence: `npm --prefix js/logbrew-node test`, `python3 scripts/check_js_sources.py js/logbrew-node`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh`, and `bash scripts/real_user_node_smoke.sh` passed with Node `v22.18.0`; direct repo-root example execution failed only because bare package imports require the packaged temp app, and the installed smoke runs those examples successfully. See `docs/competitor-research/node-trace-correlation-2026-06-16.md`.
- 2026-06-16: Express tracing correlation gap reduced as the first JS framework follow-up to the Node active-trace pattern. Current Sentry Express docs emphasize framework tracing and distributed tracing, Express 5 forwards rejected async handlers to error middleware, OpenTelemetry JS expects async context propagation, and Datadog proves trace/log correlation demand. `@logbrew/express` now attaches `req.logbrew.trace`, exposes `getActiveLogBrewTrace()` from async work started by the middleware, reuses the same request span ID for default request spans, passes trace context to custom request/metric/flush/failure/error callbacks, and adds `traceId`, `spanId`, `parentSpanId`, and `sampled` to default error metadata when a valid request trace exists. Docs now show app-owned log correlation without raw propagation/header/body/query capture or undefined metadata fields. Local evidence: `npm --prefix js/logbrew-express test`, `python3 scripts/check_js_sources.py js/logbrew-express`, and `bash scripts/real_user_express_smoke.sh` passed with `express@5.2.1` and `@types/express@5.0.6`; previous Node tracing CI run `27606919267` for `980fa93` completed successfully. Keep the next tracing slices focused on Fastify, NestJS, and Next.js before Python/Go/Java/.NET server helpers. See `docs/competitor-research/express-trace-correlation-2026-06-16.md`.
- 2026-06-16: Fastify tracing correlation gap reduced as the next JS framework slice. Current Sentry Fastify docs treat Fastify as a first-class tracing/error framework, Fastify hook docs put telemetry naturally in `onResponse`/`onError`, OpenTelemetry JS expects async context propagation, and Datadog confirms structured log trace IDs are valuable. `@logbrew/fastify` now attaches `request.logbrew.trace`, exposes `getActiveLogBrewTrace()` through Fastify route async work via `onRequest`/`preHandler` AsyncLocalStorage context, reuses the same request span ID for default spans, passes trace context to request/metric/flush/failure/error callbacks, and adds `traceId`, `spanId`, `parentSpanId`, and `sampled` to default error metadata when a valid request trace exists. Docs now show app-owned log correlation without raw propagation/header/body/query capture. Local evidence: `npm --prefix js/logbrew-fastify test`, `python3 scripts/check_js_sources.py js/logbrew-fastify`, and `bash scripts/real_user_fastify_smoke.sh` passed with `fastify@5.8.5`. Keep the next tracing slices focused on NestJS and Next.js, then Python/Go/Java/.NET server helpers. See `docs/competitor-research/fastify-trace-correlation-2026-06-16.md`.
- 2026-06-16: NestJS tracing correlation gap reduced as the next JS framework slice. Current Sentry NestJS docs treat tracing as a first-class debugging path, NestJS interceptors execute through RxJS, OpenTelemetry JS expects async context propagation, and Datadog confirms structured log trace IDs are valuable. `@logbrew/nestjs` now attaches `request.logbrew.trace`, exposes `getActiveLogBrewTrace()` through controller async work by wrapping the NestJS/RxJS subscription with `AsyncLocalStorage`, reuses the same request span ID for default spans, passes trace context to request/metric/flush/failure/error callbacks, and adds trace metadata to default/custom error capture when provided. Docs now show app-owned controller log correlation without raw propagation/header/body/query capture or undefined metadata fields. Local evidence: `npm --prefix js/logbrew-nestjs test`, `python3 scripts/check_js_sources.py js/logbrew-nestjs`, and `bash scripts/real_user_nestjs_smoke.sh` passed with `@nestjs/common@11.1.27`, `@nestjs/core@11.1.27`, and `@nestjs/platform-express@11.1.27`. Keep the next tracing slices focused on Next.js, then Python/Go/Java/.NET server helpers. See `docs/competitor-research/nestjs-trace-correlation-2026-06-16.md`.
- 2026-06-16: Next.js tracing correlation gap reduced, completing the first JS server/framework active-trace sweep after Node, Express, Fastify, and NestJS. Current Sentry Next.js docs center errors/logs/traces across client/server/edge runtimes, Next.js Route Handlers use standard Web `Request`/`Response`, OpenTelemetry JS expects active async context, and Datadog confirms trace/span IDs on logs are a core debugging pivot. `@logbrew/next` now exposes `helpers.trace`, exports `getActiveLogBrewTrace()` through `AsyncLocalStorage`, reuses one route span ID across request span/app telemetry/error metadata, passes trace context to request/metric/flush/failure/error callbacks, and adds trace metadata to default route error capture. Docs show app-owned log correlation without raw propagation/header/body/cookie/query capture. Local evidence: `npm --prefix js/logbrew-next test`, `python3 scripts/check_js_sources.py js/logbrew-next`, `bash scripts/real_user_next_smoke.sh` passed with `next@16.2.9`, `react@19.2.7`, and `react-dom@19.2.7`, plus JS ESLint, publint 0.3.21, markdown links, confidentiality scan, and backend contract reports passed. Previous NestJS CI run `27609724798` for `4e4b44b` completed successfully. Next tracing slices should move to Python, Go, Java, .NET, PHP, Ruby, mobile/native, and Rust OTel interop rather than reworking completed JS wrappers. See `docs/competitor-research/nextjs-trace-correlation-2026-06-16.md`.
- 2026-06-16: User corrected the research bar: whenever LogBrew is behind a competitor and the competitor SDK/integration source is public, docs-only research is not enough. Future gap work must read the competitor source code first, record exact repo paths plus tags/commits and methods/classes/functions studied, summarize the design pattern and tradeoffs, then implement a lighter, safer LogBrew-native version without copying code or license text. Only skip source-level reading when the relevant source is unavailable or closed, and say that explicitly in the evidence report.
- 2026-06-16: Python/FastAPI/Django tracing correlation gap reduced after public source review of Sentry Python (`getsentry/sentry-python@1df9835e485a346f670ca5615106536a20212795`), OpenTelemetry Python contrib (`open-telemetry/opentelemetry-python-contrib@97017a46805e34faf5c181b895e002f9e0cefab5`), and Datadog `dd-trace-py` (`DataDog/dd-trace-py@96412c9279099fe5338b7104b06e65f50cac29d1`). Source paths/functions studied are recorded in `docs/competitor-research/python-trace-correlation-2026-06-16.md`. `logbrew-sdk` now has `LogBrewTraceContext` in focused `_trace_context.py`, `create_logbrew_trace_context()`, `get_active_logbrew_trace()`, `use_logbrew_trace()`, `trace_metadata()`, and `span_attributes_from_trace_context()` backed by Python `contextvars`; `LogBrewLoggingHandler` automatically adds active trace/span IDs to standard-library logs. `logbrew-fastapi` and `logbrew-django` now create one request-local W3C-shaped context per request, reuse its span ID for the request span, expose active trace during handler/view execution, and add the same IDs to captured exception issues. This keeps LogBrew lighter and more privacy-bounded than Sentry/OTel/Datadog by avoiding monkey-patching, global logger injection, payload/header/cookie/query/raw-traceparent capture, and automatic DB/template spans. Local evidence: core unittest 27 tests, `bash scripts/real_user_fastapi_smoke.sh` with `fastapi@0.137.1`, `bash scripts/real_user_django_smoke.sh` with `django@6.0.6`, `bash scripts/check_fastapi_package.sh`, `bash scripts/check_django_package.sh`, `bash scripts/check_python_static.sh`, Python source syntax, ShellCheck 0.11.0, and markdown links passed. Thermo review extracted active trace context into `_trace_context.py` so `__init__.py` stayed below 1k lines. Honest gaps: Python still lacks rich Sentry/OTel-style auto child spans, DB spans, baggage, and existing OpenTelemetry context ingestion. Backend coordination update: deployed backend reports `GET /api/account/usage/history?interval=daily|weekly|monthly|yearly` with bounded zero-filled usage buckets and actionable wrong-method envelopes for auth provider availability / telemetry ingest; SDK tooling must consume backend-owned usage/history contracts if needed later and never derive quota/history from client counters or flush state.
- 2026-06-16: Go `net/http` + `slog` tracing correlation gap reduced after public source review of Sentry Go (`getsentry/sentry-go@c299195fc44e4c637d24a6ba1f2b38e0ccedb816`), OpenTelemetry Go contrib (`open-telemetry/opentelemetry-go-contrib@abd6958a5b955e3a2bd8c11f968b221c7cace421`), and Datadog Go (`DataDog/dd-trace-go@9ac2a9993837a76bb5fc4151aa2a4a09c7057128`). Source paths/functions studied are recorded in `docs/competitor-research/go-trace-correlation-2026-06-16.md`. `github.com/LogBrewCo/sdk/go/logbrew` now has request-local `TraceContext`, `NewTraceContext`, context attach/read helpers, trace-aware log/issue/span attribute helpers, a dependency-free `NewHTTPHandler` that reads only W3C `traceparent`, emits one request span plus optional `http.server.duration`, and a `NewSlogHandler` that preserves app-owned `slog.Handler` output while queueing correlated LogBrew logs. `Client` queue access is mutex-protected for request/logger concurrency. `examples/http_trace_correlation` and `scripts/check_go_http_trace_payload.py` prove release/environment/log/issue/span/metric correlation from an installed artifact, with query/hash/payload/non-primitive slog metadata rejected. Local evidence: `go test ./...` in `go/logbrew`, `bash scripts/check_go_static.sh` with staticcheck `2025.1.1`, direct `make run-http-trace-correlation` payload validation, and `bash scripts/real_user_go_smoke.sh` passed. Honest gaps: Go still lacks Sentry/OTel-style panic recovery, existing OpenTelemetry context ingestion, baggage, rich span events/exceptions, and framework integrations for Gin/Chi/Echo/Fiber/gRPC/database/outbound HTTP. Backend coordination update: deployed backend reports documented project setup, ingest-key, live-feed ticket, and live-feed socket wrong-method calls now return stable `method_not_allowed` JSON envelopes with actionable `next`; SDKs may use this only as clearer recovery guidance and must not change ingest setup docs until backend-minted project-scoped ingest keys are safe to document.
- 2026-06-16: Java tracing correlation gap reduced after mandatory public source review of Sentry Java (`getsentry/sentry-java@6dff1c9970ad612ac431980c08abb138218465e0`), OpenTelemetry Java instrumentation (`open-telemetry/opentelemetry-java-instrumentation@8f759eec78231d806bc38aebfcbd93f901ff1615`), and Datadog Java tracer (`DataDog/dd-trace-java@a099456128b53425e34e5147ec1f15ea2d28dc9b`). Exact source paths/functions studied are recorded in `docs/competitor-research/java-trace-correlation-2026-06-16.md`. `co.logbrew:logbrew-sdk` now has dependency-free `LogBrewTraceContext`, `LogBrewTrace`, and `LogBrewHttpRequestTelemetry`; JUL and optional Logback capture automatically add active `traceId`/`spanId`/`parentSpanId`/`traceFlags`/`traceSampled` metadata; malformed incoming request propagation falls back to a local root trace while strict `Traceparent.parse(...)` remains available; `LogBrewClient` queue methods are synchronized for request/logger concurrency; packaged `HttpTraceCorrelation` proves one W3C request trace links a JUL log, issue, request span, and `http.server.duration` metric without raw propagation/header/body/query/full URL capture. Local evidence: `bash scripts/check_java_package.sh` with 30 package tests + 6 trace tests, `bash scripts/check_java_static.sh` with SpotBugs `4.9.8`, `bash scripts/real_user_java_smoke.sh`, `bash scripts/check_shell_static.sh` with ShellCheck `0.11.0`, markdown links, confidentiality scan, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Prior Go tracing CI run `27616415162` completed green. Honest gaps: Java still lacks Servlet/Spring filters, async executor/task decorator integration beyond explicit `LogBrewTrace.wrapCurrent(...)`, existing OpenTelemetry context ingestion, JDBC/messaging/cache/outbound HTTP spans, baggage, and rich span events/exceptions. Backend coordination updates this cycle were public-safe only: backend reports stable wrong-method envelopes for feed/OAuth/browser-session logout routes and a backend-owned account usage history contract; SDKs should treat these as recovery/tooling guidance only and still avoid account/session API values as SDK ingest config.
- 2026-06-16: .NET tracing correlation gap reduced after mandatory public source review of Sentry .NET (`getsentry/sentry-dotnet@2f2842f20f9581468a0ab4e971bfd507557161b3`), OpenTelemetry .NET (`open-telemetry/opentelemetry-dotnet@41d2bb5a5f29aa8ad8f6566041434dccfb6b3252`), and Datadog .NET (`DataDog/dd-trace-dotnet@f308e58bebac1cf0f977fae6ddbe68e2f3ff399a`). Exact source paths/functions studied are recorded in `docs/competitor-research/dotnet-trace-correlation-2026-06-16.md`. `LogBrew` now has dependency-free `LogBrewTraceContext`, `LogBrewTrace`, and `LogBrewHttpRequestTelemetry`; `Microsoft.Extensions.Logging` capture automatically adds active trace metadata; malformed request propagation falls back to a local root trace; `LogBrewClient` queue access is lock-protected for request/logger concurrency; packaged `HttpTraceCorrelation.cs` proves one W3C request trace links an async `ILogger` log, issue, product action, request span, `http.server.duration` metric, and outgoing `traceparent` without raw propagation/header/body/query/full URL/non-primitive metadata capture. Thermo review centralized primitive metadata filtering in `Validation.CopyPrimitiveMetadata(...)` and removed an ambiguous request-helper overload by using explicit `StartWithTraceContext(...)`. Local evidence: `bash scripts/check_dotnet_package.sh` with 29 package tests, `bash scripts/real_user_dotnet_smoke.sh`, ShellCheck `0.11.0`, Python source syntax, markdown links, confidentiality scan, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Honest gaps: .NET still lacks ASP.NET Core middleware/filter, `Activity.Current` or OpenTelemetry context ingestion, outbound `HttpClient`/DB/messaging child spans, baggage/tracestate, and rich span events/exceptions. Backend coordination update: deployed telemetry read/update wrong-method routes now return stable `method_not_allowed` JSON envelopes with actionable `next`; SDKs may use them as recovery guidance only and must not change ingest setup, auth, or storage semantics.
- 2026-06-16: PHP tracing correlation gap reduced after mandatory public source review of Sentry PHP/Laravel (`getsentry/sentry-php@d0c1a8f9e510db803c6dded07bbdcb342e48d17e`, `getsentry/sentry-laravel@fe9d07e3f4b8a09c94afac88cd241f01d14549a1`), OpenTelemetry PHP (`open-telemetry/opentelemetry-php@2f1c57fda6b2b6172e42996fe4256915a08120b7`), and Datadog PHP (`DataDog/dd-trace-php@43427006ec94fa23a01bed64a955e436b9fde306`). Exact source paths/functions studied are recorded in `docs/competitor-research/php-trace-correlation-2026-06-16.md`. `logbrew/sdk` now has dependency-free `LogBrewTraceContext`, stack-backed `LogBrewTrace`/`LogBrewTraceScope`, and `LogBrewHttpRequestTelemetry`; PSR-3 and Monolog capture automatically add active trace metadata; malformed request propagation falls back to a local root trace; packaged `http_trace_correlation.php` proves one W3C request trace links a PSR log, issue, product action, request span, `http.server.duration` metric, and outgoing `traceparent` without raw propagation/header/body/query/full URL/non-primitive metadata capture. Thermo review replaced previous-pointer scope state with a scope-id stack so out-of-order close/destruct removes the right active scope instead of leaking older context, and centralized primitive metadata filtering through `LogBrewClient::copyPrimitiveMetadata(...)`. Local evidence: `php php/logbrew-php/tests/run.php`, `bash scripts/check_php_static.sh`, `python3 scripts/check_php_sources.py`, `bash scripts/real_user_php_smoke.sh`, `bash scripts/check_shell_static.sh` with ShellCheck `0.11.0`, markdown links, confidentiality scan, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Honest gaps: PHP still lacks Laravel/Symfony request middleware, OpenTelemetry context ingestion, outbound HTTP/DB/cache/queue spans, baggage/tracestate, and rich span events/exceptions; current public Packagist footprint remains worse than Sentry core/PostHog despite lean local subtree proof. Backend coordination update: latest backend-only work reported no SDK API/auth/ingest/WebSocket/storage DTO impact.
- 2026-06-16: Ruby tracing correlation gap reduced after mandatory public source review of Sentry Ruby (`getsentry/sentry-ruby@b53bfe12a0e36eeb085d35f2c71403e0248403b2`), OpenTelemetry Ruby (`open-telemetry/opentelemetry-ruby@fd3b31c6480d7c03e31f97d071a0f90e0de8a632`), and Datadog Ruby (`DataDog/dd-trace-rb@9f17636537642d3209b065c52faa08cbbe3738ed`). Exact source paths/functions studied are recorded in `docs/competitor-research/ruby-trace-correlation-2026-06-16.md`. `logbrew-sdk` now has dependency-free `LogBrew::TraceContext`, fiber-local `LogBrew::Trace`/`TraceScope`, Rack request trace activation, `LogBrew::Logger` and direct client trace metadata capture, Rails error-subscriber correlation, malformed propagation fallback, and packaged `http_trace_correlation.rb` proof linking a Ruby log, issue, product action, request span, `http.server.duration` metric, and outgoing `traceparent` without raw propagation/header/body/query/full URL/non-primitive metadata capture. Thermo review changed the active stack from thread-wide variables to Ruby fiber-local `Thread.current[]` storage to avoid async server cross-request leakage, and tightened metadata merging so symbol/string keys do not duplicate trace fields. Local evidence: `ruby ruby/logbrew-ruby/tests/run.rb` with 35 package tests plus 5 trace tests, direct example payload validation, `bash scripts/check_ruby_package.sh`, `bash scripts/real_user_ruby_smoke.sh`, `bash scripts/check_shell_static.sh` with ShellCheck `0.11.0`, Python checker syntax, markdown links, confidentiality scan, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Honest gaps: Ruby still lacks dedicated Rails/Sinatra middleware packages, controller/view/ActiveRecord/Sidekiq/outbound HTTP/DB/cache/queue spans, baggage/tracestate, rich span events/exceptions, OpenTelemetry context ingestion, and a Ruby 3.3+ competitor footprint benchmark.
- 2026-06-16: Swift/mobile trace correlation gap reduced after mandatory public source review of Sentry Cocoa (`getsentry/sentry-cocoa@4da7fcb73836342b63f1aca7449766c2c15f2822`), OpenTelemetry Swift (`open-telemetry/opentelemetry-swift@291fe3fff413ae9277ac36aec9fd9b51c1caa7e0`), and Datadog iOS (`DataDog/dd-sdk-ios@6462c0b81f5221072008443925d8bbf18aa5750b`). Exact source paths/functions studied are recorded in `docs/competitor-research/swift-trace-correlation-2026-06-16.md`. `logbrew-swift` now has dependency-free `LogBrewTraceContext` and task-local `LogBrewTrace`; valid W3C `traceparent` continues with a fresh local span ID, malformed propagation falls back non-fatally, core issue/log/action/metric events and product timeline helpers get authoritative active `traceId`/`spanId`/`parentSpanId`/`traceFlags`/`traceSampled` metadata, `LogBrewLogger` inherits correlation through the client, `LogBrewTrace.spanAttributes(...)` emits correlated spans, `outgoingHeaders()` returns only normalized `traceparent`, and the client queue is lock-protected for logger/async usage. Packaged `TraceCorrelationExample` and `scripts/check_swift_trace_correlation_payload.py` prove one trace links issue, logger log, product action, network milestone, metric, and span without raw propagation/header/body/query/fragment capture or URLSession patching. Local evidence: `swift test --package-path swift/logbrew-swift --scratch-path /tmp/logbrew-swift-trace-test`, direct packaged trace example payload validation, `bash scripts/check_swift_style.sh`, `bash scripts/check_swift_package.sh`, and `bash scripts/real_user_swift_smoke.sh` passed. Honest gaps: Swift still lacks SwiftUI/UIKit/AppKit lifecycle spans, URLSession child-span instrumentation, baggage/tracestate, existing OpenTelemetry context ingestion, and rich span events/exceptions; Objective-C, C, C++, Kotlin Android, Unity, and React Native still need the newer active trace/error-correlation pattern or richer mobile-native equivalents.
- 2026-06-16: Objective-C/mobile trace correlation gap reduced after mandatory public source review of Sentry Cocoa (`getsentry/sentry-cocoa@4da7fcb73836342b63f1aca7449766c2c15f2822`), OpenTelemetry Swift (`open-telemetry/opentelemetry-swift@291fe3fff413ae9277ac36aec9fd9b51c1caa7e0`), and Datadog iOS (`DataDog/dd-sdk-ios@6462c0b81f5221072008443925d8bbf18aa5750b`). Exact source paths/functions studied are recorded in `docs/competitor-research/objc-trace-correlation-2026-06-16.md`. `logbrew-objc` now has dependency-free `LBWTraceContext`, thread-local stack-backed `LBWTrace`/`LBWTraceScope`, W3C `traceparent` continuation with fresh local span IDs, strict validation plus non-fatal malformed fallback, active trace metadata on issue/log/action/metric and timeline helpers, span attribute helpers, and outgoing `traceparent` headers for app-owned clients. Packaged `trace_correlation.m` plus `scripts/check_objc_trace_correlation_payload.py` prove one trace links issue, log, product action, network milestone, metric, span, and outgoing propagation without raw propagation/header/body/query/fragment capture or `NSURLSession` patching. Local evidence so far: `bash scripts/check_objc_package.sh`, `bash scripts/real_user_objc_smoke.sh`, `python3 -m py_compile scripts/check_objc_trace_correlation_payload.py`, and generated-artifact hygiene passed. Honest gaps: Objective-C still lacks UIKit/AppKit lifecycle spans, automatic `NSURLSession` instrumentation, OpenTelemetry context ingestion, baggage/tracestate, and rich span events/exceptions; next mobile/native targets remain C, C++, Kotlin Android, Unity, and React Native richer active trace correlation.
- 2026-06-16: C/native trace correlation gap reduced after mandatory public source review of Sentry Native (`getsentry/sentry-native@10a29ab2e594944d1dfabb4fd261e7f314dbd8d7`), OpenTelemetry C++ (`open-telemetry/opentelemetry-cpp@de26178fe5275a632a749792b4a72b625422d2ff`), and Datadog C++ (`DataDog/dd-trace-cpp@0a1dc56b418262cee758c33fa6f488e9e44ab6ed`). Exact source paths/functions studied are recorded in `docs/competitor-research/c-trace-correlation-2026-06-16.md`. `logbrew-c` now has dependency-free `LogBrewTraceContext`, compiler-TLS `LogBrewTraceScope`, strict W3C `traceparent` parsing with malformed fallback, fresh local span IDs, active trace metadata on issue/log/action, explicit trace metadata/timeline/span/outgoing-header helpers, and packaged `trace_correlation.c` proof linking issue, log, action, span, metric, product action, network milestone, and outgoing `traceparent` without raw propagation/header/body/query/fragment capture or HTTP patching. C metric metadata now serializes under `attributes.metadata`, aligning with the shared event contract instead of the prior flat metric metadata shape. Local evidence: `bash scripts/check_c_package.sh` and `bash scripts/real_user_c_smoke.sh` passed with Apple clang `21.0.0`. Honest gaps: C still lacks fully portable C99 TLS on non-GCC/Clang/MSVC compilers, native lifecycle spans, outbound HTTP child-span helpers, OpenTelemetry context ingestion, baggage/tracestate, rich span events/exceptions, and crash/symbolication integration; next native/mobile targets remain C++, Kotlin Android, Unity, and richer React Native active trace correlation.
- 2026-06-16: C++/native trace correlation gap reduced after mandatory public source review of Sentry Native (`getsentry/sentry-native@10a29ab2e594944d1dfabb4fd261e7f314dbd8d7`), OpenTelemetry C++ (`open-telemetry/opentelemetry-cpp@de26178fe5275a632a749792b4a72b625422d2ff`), and Datadog C++ (`DataDog/dd-trace-cpp@0a1dc56b418262cee758c33fa6f488e9e44ab6ed`). Exact source paths/functions studied are recorded in `docs/competitor-research/cpp-trace-correlation-2026-06-16.md`. `logbrew-cpp` now has dependency-free `TraceContext`, RAII `TraceScope`, strict W3C `traceparent` parsing with malformed fallback, fresh local span IDs, active trace metadata on issue/log/action/metric, explicit trace metadata/timeline/span/outgoing-header helpers, and packaged `trace_correlation.cpp` proof linking issue, log, action, span, metric, product action, network milestone, and outgoing `traceparent` without raw propagation/header/body/query/fragment capture or HTTP patching. Local evidence: `bash scripts/check_cpp_package.sh` and `bash scripts/real_user_cpp_smoke.sh` passed with Apple clang `21.0.0`. Honest gaps: C++ still lacks OpenTelemetry context ingestion, baggage/tracestate, lifecycle spans, outbound HTTP/DB/cache/queue child spans, rich span events/exceptions, and native crash/symbolication integration; next native/mobile targets are Kotlin Android, Unity, and richer React Native active trace correlation.
- 2026-06-16: Kotlin Android/JVM trace correlation gap reduced after mandatory public source review of Sentry Java/Android (`getsentry/sentry-java@6dff1c9970ad612ac431980c08abb138218465e0`), OpenTelemetry Java/API/instrumentation (`open-telemetry/opentelemetry-java@1ec8bce84b378508c802788215afb0a6c67cfbff`, `open-telemetry/opentelemetry-java-instrumentation@7a4599ea8c410876098ec1a84141b56d881949e7`), and Datadog Android (`DataDog/dd-sdk-android@e07c4cc6a23b51d4a45602787cea3f3f1db7b8b0`). Exact source paths/functions studied are recorded in `docs/competitor-research/kotlin-android-trace-correlation-2026-06-16.md`. `logbrew-kotlin` now has dependency-free `LogBrewTraceContext`, stack-backed `LogBrewTraceScope`, strict W3C `traceparent` parsing with malformed fallback, fresh local span IDs, active trace metadata on issue/log/action/metric and Android product/network helpers, explicit span/outgoing-header helpers, and packaged `TraceCorrelation.kt` proof linking issue, log, action, span, metric, product action, network milestone, and outgoing `traceparent` without raw propagation/header/body/query/fragment capture or OkHttp/global HTTP patching. Local evidence: `bash scripts/check_kotlin_package.sh` with 21 tests plus installed trace payload proof, `bash scripts/check_kotlin_style.sh` with ktlint `1.8.0`, `bash scripts/real_user_kotlin_smoke.sh`, ShellCheck `0.11.0`, Python checker syntax, markdown links, confidentiality scan, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Honest gaps: Kotlin Android still lacks Android lifecycle spans, OkHttp/HttpURLConnection child spans, coroutine context propagation beyond explicit thread-local scopes, OpenTelemetry context ingestion, baggage/tracestate, rich span events/exceptions, and DB/cache/queue spans; next mobile/native targets are Unity, richer React Native active trace correlation, Kotlin/Swift/ObjC URLSession/OkHttp bridges, and Rust OTel interop.
- 2026-06-16: Unity trace correlation gap reduced after mandatory public source review of Sentry Unity (`getsentry/sentry-unity@4cda4392328f489a0b22490f0f0a2cc401cfc622`), Datadog Unity (`DataDog/dd-sdk-unity@309e84de7c20af6ec47c2ff3e6c6f8d339937e93`), and OpenTelemetry .NET (`open-telemetry/opentelemetry-dotnet@184b1cfd4a48f1fe6240a1b76f39c37efb38f465`). Exact source paths/functions studied are recorded in `docs/competitor-research/unity-trace-correlation-2026-06-16.md`. `co.logbrew.unity` now has dependency-free `LogBrewTraceContext` and thread-local `LogBrewTrace`; valid W3C `traceparent` continues with a fresh local span ID, malformed propagation falls back non-fatally, issue/log/action and Unity scene/log/exception helpers get authoritative active `traceId`/`spanId`/`parentSpanId`/`traceFlags`/`traceSampled` metadata, `LogBrewTrace.SpanAttributes(...)` emits correlated spans, and `OutgoingHeaders()` returns only normalized `traceparent` for app-owned requests. Packaged `examples/trace_correlation/TraceCorrelation.cs` plus `scripts/check_unity_trace_correlation_payload.py` prove one trace links issue, log, action, span, scene action, Unity log helper, exception helper, and outgoing propagation without raw propagation/header/body/query capture or `UnityWebRequest` patching. Local evidence: Unity package tests now 16 tests, `bash scripts/check_unity_package.sh`, `bash scripts/real_user_unity_smoke.sh`, ShellCheck `0.11.0`, Python checker syntax, markdown links, confidentiality scan, backend contract reports, release metadata, generated-artifact hygiene, and diff hygiene passed. Honest gaps: Unity still lacks lifecycle spans, `UnityWebRequest` child spans, coroutine context propagation, OpenTelemetry context ingestion, baggage/tracestate, rich span events/exceptions, and native crash/symbolication integration; next mobile/native priorities are richer React Native active trace correlation, Swift/Objective-C/Kotlin outbound child spans, Unity lifecycle/request helpers, and Rust OTel interop.
- 2026-06-16: React Native trace correlation gap reduced after mandatory public source review of Sentry React Native (`getsentry/sentry-react-native@580fb5c7bf39cc1a8caf7a30af9078c887eb40b9`), Datadog React Native (`DataDog/dd-sdk-reactnative@92462dccefd689815d87dabbad0d41572cd06cca`), and OpenTelemetry JS (`open-telemetry/opentelemetry-js@96619ed69e84613846e0ea94453711fc0cd02e18`). Exact source paths/functions studied are recorded in `docs/competitor-research/react-native-trace-correlation-2026-06-16.md`. `@logbrew/react-native` now has dependency-free `createReactNativeTraceContext(...)`, stack-backed `withLogBrewTrace(...)`/`bindLogBrewTrace(...)`, active trace metadata helpers, explicit span/header helpers, provider/hook trace propagation, helper-level trace metadata overwrite for spoofed IDs, and `createTraceparentFetch(...)` reuse of supplied or active trace context while retaining target-scoped propagation. Packaged `examples/trace-correlation.mjs` proves one W3C trace links screen, action, network milestone, handled error, explicit span, and outgoing `traceparent` without global fetch/XHR patching, payload/header capture, visual replay, or native bridge scope sync. Thermo review kept the active scope contract honest by avoiding async callbacks in verifier examples; async work after `await` should keep the trace object and pass it explicitly or use provider trace. Local evidence: `python3 scripts/check_js_sources.py js/logbrew-react-native`, `cd js/logbrew-react-native && npm test`, `bash scripts/real_user_react_native_smoke.sh` with React Native `0.86.0`/React `19.2.7`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh` with publint `0.3.21`, ShellCheck `0.11.0`, markdown links, confidentiality scan, release metadata, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Honest gaps: React Native still lacks automatic navigation/lifecycle spans, React Navigation route middleware, native bridge scope sync, fetch/XHR resource child spans, OpenTelemetry context ingestion, baggage/tracestate, rich span events/exceptions, and source-map/native symbolication parity with Sentry/Datadog.
- 2026-06-16: React Native navigation/resource span gap reduced after follow-up public source review of Sentry React Native `reactnavigation.ts`/`span.ts`, Datadog React Native `ResourceReporter.ts`/`RumResource.ts`, and OpenTelemetry JS `W3CTraceContextPropagator.ts`/`StackContextManager` tests; exact methods and commits are recorded in `docs/competitor-research/react-native-trace-correlation-2026-06-16.md`. `@logbrew/react-native` now adds explicit `createReactNavigationSpanListener(...)`, navigation span helpers, and resource span helpers. App-owned React Navigation refs can capture initial and route-change spans with previous route names, query/hash-free paths, opt-in route keys, W3C trace continuity, and primitive-only metadata; resource spans capture method, route template, duration, status, response size, screen, and session without global fetch/XHR patching or payload/header/full URL capture. Packaged `examples/navigation-resource-spans.mjs` proves one trace links initial navigation, route change, successful resource, and failed resource spans. Local evidence: `python3 scripts/check_js_sources.py js/logbrew-react-native`, `npm test --prefix js/logbrew-react-native`, `bash scripts/real_user_react_native_smoke.sh` with React Native `0.86.0`/React `19.2.7`, `bash scripts/check_js_lint.sh`, `bash scripts/check_js_package.sh` with publint `0.3.21`, `bash scripts/check_shell_static.sh` with ShellCheck `0.11.0`, markdown links, confidentiality scan, release metadata, backend contract reports, generated-artifact hygiene, diff hygiene, and thermo-nuclear review passed. Honest gaps: React Native still lacks automatic lifecycle/navigation spans, native bridge scope sync, automatic fetch/XHR resource spans, OTel context ingestion, baggage/tracestate, rich span events/exceptions, and source-map/native symbolication parity.
- 2026-06-16: React Native explicit fetch resource gap reduced after re-reading current Sentry React Native `options.ts`, Datadog React Native `XHRProxy.ts`/`ResourceReporter.ts`, and OpenTelemetry JS W3C/stack-context sources; HEADs still match recorded commits. Added `@logbrew/react-native/resource-fetch` with `createReactNativeResourceFetch(...)`, an app-owned fetch wrapper that resolves one trace context up front, reuses existing target-scoped `createTraceparentFetch(...)`, records success and thrown-error resource spans, strips query/hash route templates, keeps primitive metadata only, and avoids global `fetch`/XHR patching, bodies, arbitrary headers, GraphQL payload extraction, baggage, and tracestate. Packaged `examples/resource-fetch-spans.mjs` plus installed smoke prove matched API `traceparent`, unmatched target no-header behavior, sanitized success/failure spans, TypeScript/CJS/ESM subpath exports, and tarball contents. Thermo review caught and fixed a potential trace/header divergence by removing `traceparentFactory` from the wrapper and resolving a single trace object before propagation and span capture. Local evidence: `python3 scripts/check_js_sources.py js/logbrew-react-native`, `npm test --prefix js/logbrew-react-native`, `bash scripts/real_user_react_native_smoke.sh` with React Native `0.86.0`/React `19.2.7`, JS ESLint, publint `0.3.21`, ShellCheck `0.11.0`, markdown links, confidentiality scan, release metadata, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Remaining React Native gaps: automatic lifecycle/navigation spans, native bridge scope sync, automatic fetch/XHR instrumentation, OTel context ingestion, baggage/tracestate, rich span events/exceptions, and source-map/native symbolication parity.
- 2026-06-17: React Native explicit lifecycle span gap reduced after re-reading current Sentry React Native `packages/core/src/js/tracing/span.ts` (`startIdleSpan`) and `onSpanEndUtils.ts` (`cancelInBackground`), Datadog React Native Navigation `DdRumReactNativeNavigationTracking.tsx` (`startTracking`/`appStateListener`), and OpenTelemetry JS `StackContextManager.ts`/tests at commits recorded in `docs/competitor-research/react-native-trace-correlation-2026-06-16.md`. Added `@logbrew/react-native/lifecycle` with `createReactNativeLifecycleSpanEvent(...)`, `captureReactNativeLifecycleSpan(...)`, and `createAppStateLifecycleSpanListener(...)` to record AppState transitions such as `app_state:active->background` with previous-state duration, active/provider trace metadata, primitive-only metadata, and listener removal while avoiding native bridge sync, hidden view restarts, session-health derivation, or global RN patching. Packaged `examples/lifecycle-spans.mjs` and installed smoke validate initial/inactive/background/foreground spans, TypeScript/CJS/ESM subpath exports, tarball contents, and example launcher wiring. Thermo review moved lifecycle out of the main entry because the first draft pushed `index.js`/`index.cjs` past 1k lines; final core entries are 964/977 lines and lifecycle lives in focused 121/129-line ESM/CJS files. Local evidence: `python3 scripts/check_js_sources.py js/logbrew-react-native`, `npm test --prefix js/logbrew-react-native`, `bash scripts/real_user_react_native_smoke.sh` with React Native `0.86.0`/React `19.2.7`, JS ESLint, publint `0.3.21`, ShellCheck `0.11.0`, markdown links, confidentiality scan, release metadata, backend contract reports, generated-artifact hygiene, diff hygiene, line-count check, and thermo review passed. Remaining React Native gaps: automatic lifecycle/navigation instrumentation, native bridge scope sync, automatic fetch/XHR spans, OTel context ingestion, baggage/tracestate, rich span events/exceptions, and source-map/native symbolication parity.
- 2026-06-17: Unity explicit lifecycle span gap reduced after re-fetching current Sentry Unity (`getsentry/sentry-unity@4cda4392328f489a0b22490f0f0a2cc401cfc622`), Datadog Unity (`DataDog/dd-sdk-unity@309e84de7c20af6ec47c2ff3e6c6f8d339937e93`), and OpenTelemetry .NET (`open-telemetry/opentelemetry-dotnet@184b1cfd4a48f1fe6240a1b76f39c37efb38f465`) source snapshots and re-reading `SentryMonoBehaviour.UpdatePauseStatus(...)`/`OnApplicationPause(...)`/`OnApplicationFocus(...)`, `SceneManagerTracingAPI.LoadSceneAsyncByNameOrIndex(...)`, `DatadogTrackedWebRequest.SendWebRequest()`, and OTel `RuntimeContext`/`AsyncLocalRuntimeContextSlot<T>`. Added `LogBrewUnity.CaptureLifecycleSpan(...)` for app-owned transitions such as `active -> paused` with previous-state duration, active trace correlation, primitive Unity context metadata, and spoofed trace-key overwrite while avoiding hidden `MonoBehaviour` objects, global pause/focus subscriptions, session-health derivation, `UnityWebRequest` patching, payload/header/query capture, baggage, and tracestate. Packaged `examples/lifecycle_spans/LifecycleSpans.cs` plus `scripts/check_unity_lifecycle_payload.py` prove active/paused lifecycle spans reuse one local span ID and ship through installed UPM tarball examples. Local evidence: `bash scripts/check_unity_package.sh` with 17 tests, `bash scripts/real_user_unity_smoke.sh`, ShellCheck `0.11.0`, markdown links, confidentiality scan, release metadata, backend contract reports, generated-artifact hygiene, and diff hygiene passed. Remaining Unity gaps: automatic lifecycle instrumentation, `UnityWebRequest` child-span helpers, coroutine context propagation, OTel context ingestion, baggage/tracestate, rich span events/exceptions, and native crash/symbolication integration.
- 2026-06-17: Swift explicit URLSession child-span gap reduced after re-reading Sentry Cocoa (`getsentry/sentry-cocoa@4da7fcb73836342b63f1aca7449766c2c15f2822`) `SentryTracePropagation` header propagation, OpenTelemetry Swift (`open-telemetry/opentelemetry-swift@291fe3fff413ae9277ac36aec9fd9b51c1caa7e0`) `URLSessionLogger` request/span flow, and Datadog iOS (`DataDog/dd-sdk-ios@6462c0b81f5221072008443925d8bbf18aa5750b`) `URLSessionRUMResourcesHandler` resource tracking. Added dependency-free, app-owned `LogBrewTrace.startURLSessionSpan(for:routeTemplate:context:)` and `LogBrewClient.captureURLSessionSpan(...)`: callers get a copied `URLRequest` with only normalized `traceparent`, a fresh child span context, sanitized method/route metadata, and an explicit completion span without raw URL/query/hash/header/body/raw-traceparent capture or global URLSession patching. Packaged `TraceCorrelationExample` and `scripts/check_swift_trace_correlation_payload.py` now prove 9 correlated events including a `POST /api/checkout` 503 URLSession span parented to the active app span while preserving app-owned request headers outside telemetry. Local evidence: `bash scripts/check_swift_style.sh`, `bash scripts/check_swift_package.sh`, `bash scripts/real_user_swift_smoke.sh`, markdown links, confidentiality scan, release metadata, backend contract reports, ShellCheck `0.11.0`, generated-artifact hygiene, diff hygiene, and thermo review passed; thermo kept URLSession logic in focused `URLSessionTrace.swift` and reused existing ProductTimeline validators rather than duplicating network sanitization. Remaining Swift gaps: automatic URLSession instrumentation, SwiftUI/UIKit/AppKit lifecycle spans, OTel context ingestion, baggage/tracestate, rich span events/exceptions, URLSession metrics phase breakdown, and native symbolication parity.
- 2026-06-17: Objective-C explicit `NSURLSession` child-span gap reduced after re-reading Sentry Cocoa (`getsentry/sentry-cocoa@4da7fcb73836342b63f1aca7449766c2c15f2822`) `SentryTracePropagation`/`SentryTraceHeader`, OpenTelemetry Swift (`open-telemetry/opentelemetry-swift@291fe3fff413ae9277ac36aec9fd9b51c1caa7e0`) `URLSessionLogger`, and Datadog iOS (`DataDog/dd-sdk-ios@6462c0b81f5221072008443925d8bbf18aa5750b`) `URLSessionRUMResourcesHandler`. Added focused `LBWURLSessionSpan`, `LBWTrace startURLSessionSpanForRequest:...`, and `LBWClient captureURLSessionSpanWithID:...`: callers get a copied `NSURLRequest` with only normalized `traceparent`, a fresh child span context, normalized method, query/fragment-free route, and explicit completion span metadata without raw URL/query/fragment/header/body/raw-traceparent capture or global `NSURLSession` patching. Thermo review extracted `LogBrewNetworkValidation` so URLSession spans and network milestones share method, route, status, and duration rules instead of drifting. Packaged `trace_correlation.m` and `scripts/check_objc_trace_correlation_payload.py` now prove one trace links issue, log, product action, network milestone, metric, manual span, URLSession child span, and outgoing propagation while preserving app-owned request headers outside telemetry. Local evidence: `bash scripts/check_objc_package.sh`, `bash scripts/real_user_objc_smoke.sh`, Python checker syntax, markdown links, confidentiality scan, release metadata, backend reports, ShellCheck `0.11.0`, generated-artifact hygiene, and diff hygiene passed. Remaining Objective-C gaps: automatic `NSURLSession` instrumentation, UIKit/AppKit lifecycle spans, OTel context ingestion, baggage/tracestate, rich span events/exceptions, URLSession metrics phase breakdown, and native symbolication parity.
- 2026-06-17: Objective-C explicit lifecycle span gap reduced after source review of Sentry Cocoa app/session lifecycle (`SentryDefaultAppStateManager`, `SentryAutoSessionTrackingIntegration`, `SessionTracker`), Datadog iOS app/view lifecycle (`AppStateManager`, `UIViewControllerSwizzler`, `RUMViewsHandler`), and OTel Swift session instrumentation at commits recorded in `docs/competitor-research/objc-trace-correlation-2026-06-16.md`. Added focused `LogBrewLifecycle.m` with `LBWClient captureLifecycleSpanWithID:...` for app-owned transitions such as `active -> background`: it trims/validates states, validates previous-state duration, creates a child span under the active trace or a local root when unscoped, preserves primitive app context, and overwrites spoofed trace keys while avoiding notification observers, UIKit/AppKit swizzling, local session-health derivation, payload/header/query capture, baggage, and tracestate. Objective-C README build snippets now use `src/*.m` so fresh source installs do not miss focused SDK files. Packaged `trace_correlation.m` plus `scripts/check_objc_trace_correlation_payload.py` prove lifecycle span trace/error/log/timeline/resource correlation from installed artifacts. Local evidence: `bash scripts/check_objc_package.sh`, `bash scripts/real_user_objc_smoke.sh`, ShellCheck `0.11.0`, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, and diff hygiene passed. Remaining Objective-C gaps: automatic lifecycle/URLSession instrumentation, OTel context ingestion, baggage/tracestate, rich span events/exceptions, URLSession metrics phase breakdown, and native symbolication parity.
- 2026-06-17: Swift explicit lifecycle span gap reduced after source review of Sentry Cocoa lifecycle/session tracking (`SentryDefaultAppStateManager`, Swift `SessionTracker`), Datadog iOS app/view lifecycle (`AppStateManager`, `UIViewControllerSwizzler`), and OpenTelemetry Swift sessions (`SessionManager`) at commits recorded in `docs/competitor-research/swift-trace-correlation-2026-06-16.md`. Added focused `LifecycleTrace.swift` with `LogBrewClient.captureLifecycleSpan(...)` for app-owned transitions such as `active -> background`: it validates states/duration, creates a child span under the active trace or local root, preserves primitive context/metadata, and overwrites spoofed trace keys while avoiding notification observers, SwiftUI/UIKit/AppKit swizzling, local session-health derivation, raw propagation/header/body capture, baggage, and tracestate. Packaged `TraceCorrelationExample` now proves 10 correlated events including URLSession and lifecycle child spans. Local evidence: `bash scripts/check_swift_style.sh`, `bash scripts/check_swift_package.sh`, `bash scripts/real_user_swift_smoke.sh`, markdown links, confidentiality scan, release metadata, backend reports, ShellCheck `0.11.0`, generated-artifact hygiene, and diff hygiene passed. Remaining Swift gaps: automatic URLSession/lifecycle instrumentation, OTel context ingestion, baggage/tracestate, rich span events/exceptions, URLSession phase timing, and native symbolication parity.
- 2026-06-17: Unity explicit request child-span gap reduced after re-reading Sentry Unity lifecycle/scene tracing, Datadog Unity `ResourceTrackingHelper.GenerateTracingHeaders(...)`/`TraceContext.InjectHeaders(...)`/`DatadogTrackedWebRequest.SendWebRequest()`, and OpenTelemetry .NET context propagation at commits recorded in `docs/competitor-research/unity-trace-correlation-2026-06-16.md`. Added focused `UnityRequestTrace.cs` with `UnityRequestSpan`, `LogBrewUnity.StartRequestSpan(...)`, and `LogBrewUnity.CaptureRequestSpan(...)`: app-owned request clients such as `UnityWebRequest` get a child trace context plus exactly one `traceparent` header, sanitized method/route metadata, status/duration/error capture, primitive Unity context, and spoofed trace/traceparent overwrite while avoiding global request patching, payload/header/full-URL/query/hash capture, baggage, and tracestate. Packaged `trace_correlation` proof now includes a request child span plus active/request outgoing traceparents. Local evidence: `bash scripts/check_unity_package.sh` with 18 tests, `bash scripts/real_user_unity_smoke.sh`, `bash scripts/check_shell_static.sh` with ShellCheck `0.11.0`, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Unity gaps: automatic lifecycle/UnityWebRequest instrumentation, coroutine context propagation, OTel context ingestion, baggage/tracestate, rich span events/exceptions, URL/request phase timings, and native crash/symbolication integration.
- 2026-06-17: Unity explicit coroutine trace propagation gap reduced after re-reading Sentry Unity `SentryMonoBehaviour.QueueCoroutine(...)`/`Update()`/`StartCoroutine(...)`, OpenTelemetry .NET `RuntimeContext`/`AsyncLocalRuntimeContextSlot<T>`, and Datadog Unity `DdUnityLogHandler` at commits recorded in `docs/competitor-research/unity-trace-correlation-2026-06-16.md`. Added focused `UnityCoroutineTrace.cs` with `UnityTraceCoroutine` and `LogBrewUnity.TraceCoroutine(...)`: apps wrap an owned `IEnumerator` while a trace is active, then each `MoveNext()`/`Reset()` temporarily reactivates that captured trace so post-yield telemetry keeps trace-log-error-span correlation. It deliberately avoids hidden `MonoBehaviour` creation, automatic coroutine/lifecycle/resource instrumentation, ambient async context assumptions, payload/header/query capture, baggage, and tracestate. Packaged `trace_correlation` proof now includes a coroutine resume action under the same active trace. Local evidence: `dotnet run --project unity/logbrew-unity/tests/LogBrew.Unity.Tests/LogBrew.Unity.Tests.csproj --configuration Release` with 19 tests, `bash scripts/check_unity_package.sh`, `bash scripts/real_user_unity_smoke.sh`, `bash scripts/check_shell_static.sh` with ShellCheck `0.11.0`, `python3 -m py_compile scripts/check_unity_trace_correlation_payload.py`, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Unity gaps: automatic lifecycle/UnityWebRequest/coroutine instrumentation, OTel context ingestion, baggage/tracestate, rich span events/exceptions, URL/request phase timings, and native crash/symbolication integration.
- 2026-06-17: Kotlin Android explicit outbound request child-span gap reduced after re-reading Sentry OkHttp `SentryOkHttpInterceptor.intercept(...)` at `getsentry/sentry-java@6dff1c9970ad612ac431980c08abb138218465e0`, OpenTelemetry OkHttp `TracingInterceptor.intercept(...)`/`RequestHeaderSetter.set(...)` at `open-telemetry/opentelemetry-java-instrumentation@2f94c787cb511ddd80c6e337cd89999798a2da2b`, and Datadog Android OkHttp `TracingInterceptor.interceptAndTrace(...)`/`updateRequest(...)`/`handleResponse(...)`/`handleThrowable(...)` plus `TraceContextInjection` at `DataDog/dd-sdk-android@e07c4cc6a23b51d4a45602787cea3f3f1db7b8b0`; exact notes are in `docs/competitor-research/kotlin-android-trace-correlation-2026-06-16.md`. Added `AndroidRequestSpan`, `LogBrewTrace.childContext(...)`, `LogBrewAndroid.startRequestSpan(...)`, and `captureRequestSpan(...)`: app-owned OkHttp/HttpURLConnection clients get exactly one normalized `traceparent`, a child span under the supplied/active trace, sanitized method/route/status/duration/error metadata, primitive Android context, and spoofed trace-key overwrite without interceptors, global HTTP patching, payload/header/full-URL/query/hash capture, baggage, or tracestate. Packaged trace proof now validates active and request traceparents separately. Local evidence: `bash scripts/check_kotlin_style.sh` with ktlint 1.8.0, `bash scripts/check_kotlin_package.sh` with 22 tests, `bash scripts/real_user_kotlin_smoke.sh`, `python3 -m py_compile scripts/check_kotlin_trace_correlation_payload.py`, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Kotlin gaps: automatic Android lifecycle/OkHttp/HttpURLConnection instrumentation, coroutine context propagation beyond explicit thread-local scopes, OTel context ingestion, baggage/tracestate, rich span events/exceptions, DB/cache/queue spans, and native crash/symbolication integration.
- 2026-06-17: Rust OpenTelemetry SpanContext interop gap reduced after source review of OpenTelemetry Rust `SpanContext`/`TraceContextExt` at `open-telemetry/opentelemetry-rust@88821497a893ff6dd4dd916621a2224394ebb0a4`, `tracing-opentelemetry` `OpenTelemetrySpanExt` at `tokio-rs/tracing-opentelemetry@1d5422f1f37932fd65e434da618b305d4c94ee9c`, Sentry Rust `sentry-opentelemetry` converters/processor/propagator at `getsentry/sentry-rust@e33b7ff20eb5bf948eacf89d7eecdcc59b31d4f3`, and Datadog `dd-trace-rs` OTel mappings/propagation at `DataDog/dd-trace-rs@0d1d982f0464318f5c1a21c2db1c84b58ff2c95c`; exact paths/functions are in `docs/competitor-research/rust-tracing-bridge-2026-06-15.md`. Added dependency-free `OpenTelemetrySpanContext`, `Traceparent::create_headers_from_opentelemetry_context(...)`, `span_attributes_from_opentelemetry_context(...)`, and `context_from_opentelemetry_context(...)`; apps can copy trace ID/span ID/trace flags from an owned OTel span context, create one normalized `traceparent`, and queue a LogBrew child span with the OTel span as parent without adding OTel dependencies, processors/exporters, tracestate/baggage ingestion, global HTTP patching, payload/header/raw URL capture, or raw propagation metadata. Packaged `first_useful_telemetry` now exercises the OTel-compatible helper with unchanged 7-event output. Local evidence: `cargo fmt --check && cargo test && cargo test --all-features && cargo clippy --all-targets --all-features -- -D warnings`, `bash scripts/real_user_rust_smoke.sh` packaged 25 files / 238.5 KiB / 49.1 KiB and passed installed-app/rustdoc proof, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Rust gaps: automatic `tracing-opentelemetry` context extraction/processor interop, rich span events/exceptions, links, tracestate/baggage, Rocket example, and native symbolication parity.
- 2026-06-17: Kotlin Android OpenTelemetry SpanContext interop gap reduced after source re-read of OpenTelemetry Java `SpanContext`/`TraceFlags`/`Span`/`W3CTraceContextPropagator` at `open-telemetry/opentelemetry-java@a00536a2c7b3a3f7a0952dbea12e9249da18611d`, Sentry Java `PropagationContext`/`SentryTraceHeader` at `getsentry/sentry-java@6dff1c9970ad612ac431980c08abb138218465e0`, and Datadog Android `TraceContext`/`TraceContextInjection` at `DataDog/dd-sdk-android@e07c4cc6a23b51d4a45602787cea3f3f1db7b8b0`; exact paths/functions are recorded in `docs/competitor-research/kotlin-android-trace-correlation-2026-06-16.md`. Added dependency-free `LogBrewOpenTelemetrySpanContext`, `LogBrewTrace.fromOpenTelemetrySpanContext(...)`, and `spanAttributesFromOpenTelemetrySpanContext(...)`: apps can copy only OTel trace ID/span ID/trace flags into a fresh LogBrew child context/span without adding OTel dependencies, processors/exporters, tracestate/baggage, global HTTP patching, payload/header/raw URL capture, or raw propagation metadata. Packaged `TraceCorrelation.kt` now starts from a copied OTel-compatible parent while preserving the same trace-log-error-action-metric-network-request proof. Local evidence: `bash scripts/check_kotlin_style.sh` with ktlint 1.8.0, `bash scripts/check_kotlin_package.sh` with 23 tests, `bash scripts/real_user_kotlin_smoke.sh`, `python3 -m py_compile scripts/check_kotlin_trace_correlation_payload.py`, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, and diff hygiene passed. Remaining Kotlin gaps: automatic Android lifecycle/OkHttp/HttpURLConnection instrumentation, coroutine context propagation beyond explicit thread-local scopes, richer OTel `Context`/`Span` extraction, baggage/tracestate, rich span events/exceptions, DB/cache/queue spans, and native crash/symbolication parity.
- 2026-06-17: C++ OpenTelemetry SpanContext interop gap reduced after source re-read of OpenTelemetry C++ `SpanContext`/`TraceId`/`SpanId`/`TraceFlags`/`HttpTraceContext` at `open-telemetry/opentelemetry-cpp@de26178fe5275a632a749792b4a72b625422d2ff`, Sentry Native `sentry_set_trace_n`/outgoing `traceparent` at `getsentry/sentry-native@10a29ab2e594944d1dfabb4fd261e7f314dbd8d7`, and Datadog C++ `Tracer::extract_span`/`TraceSegment::inject`/W3C tests at `DataDog/dd-trace-cpp@0a1dc56b418262cee758c33fa6f488e9e44ab6ed`; exact paths/functions are recorded in `docs/competitor-research/cpp-trace-correlation-2026-06-16.md`. Added dependency-free `OpenTelemetrySpanContext`, `open_telemetry_span_context(...)`, `open_telemetry_span_context_from_sampled(...)`, `trace_context_from_opentelemetry_span_context(...)`, and `trace_span_attributes_from_opentelemetry_span_context(...)`: apps can copy only OTel trace ID/span ID/trace flags into a fresh LogBrew child context/span without adding OTel dependencies, processors/exporters, tracestate/baggage, global HTTP patching, payload/header/raw URL capture, or raw propagation metadata. Packaged `trace_correlation.cpp` now starts from a copied OTel-compatible parent while preserving the same C++ trace-log-error-action-metric-network proof. Local evidence: `bash scripts/check_cpp_package.sh` with Apple clang 21.0.0 and 108 checks, `bash scripts/real_user_cpp_smoke.sh` with source archive install/remove/reinstall and local HTTP 503-to-202 proof, and packaged README/header/example assertions for the OTel bridge passed. Remaining C++ gaps: live OTel `Context`/`Span` extraction, baggage/tracestate, rich span events/exceptions/links, automatic HTTP/lifecycle/DB/cache/queue instrumentation, URL/request phase timings, and native crash/symbolication parity.
- 2026-06-17: Swift OpenTelemetry SpanContext interop gap reduced after source re-read of OpenTelemetry Swift Core `SpanContext`/`TraceId`/`SpanId`/`TraceFlags`/`W3CTraceContextPropagator` at `open-telemetry/opentelemetry-swift-core@4f85f2a5c8138a72384be3edd1dcfc2cc97b297f`, Sentry Cocoa `SentryTracePropagation`/`SentryScope` at `getsentry/sentry-cocoa@4da7fcb73836342b63f1aca7449766c2c15f2822`, and Datadog iOS `TraceContext`/`HTTPHeadersWriter`/`URLSessionRUMResourcesHandler`/`RemoteLogger` at `DataDog/dd-sdk-ios@6462c0b81f5221072008443925d8bbf18aa5750b`; exact paths/functions are recorded in `docs/competitor-research/swift-trace-correlation-2026-06-16.md`. Added dependency-free `LogBrewOpenTelemetrySpanContext`, `LogBrewTrace.openTelemetrySpanContext(...)`, non-throwing `context(fromOpenTelemetrySpanContext:)`, and `spanAttributesFromOpenTelemetrySpanContext(...)`: apps can copy only OTel trace ID/span ID/trace flags into a fresh LogBrew child context/span without OTel dependencies/exporters/processors, tracestate/baggage, global URLSession patching, payload/header/raw URL capture, or raw propagation metadata. Packaged `TraceCorrelationExample` and installed consumer smoke now start from an OTel-compatible parent while preserving 10-event Swift trace/error/log/action/network/metric/URLSession/lifecycle proof. Local evidence: `bash scripts/check_swift_style.sh`, `bash scripts/check_swift_package.sh`, `bash scripts/real_user_swift_smoke.sh`, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Swift gaps: automatic URLSession/lifecycle instrumentation, live OTel `Context`/`Span` extraction, baggage/tracestate, rich span events/exceptions, URLSession phase timings, and native symbolication parity.
- 2026-06-17: Objective-C OpenTelemetry SpanContext interop gap reduced after source re-read of OpenTelemetry Swift Core `SpanContext`/`W3CTraceContextPropagator` at `open-telemetry/opentelemetry-swift-core@4f85f2a5c8138a72384be3edd1dcfc2cc97b297f`, Sentry Cocoa `SentryTracePropagation`/`SentryScope` at `getsentry/sentry-cocoa@4da7fcb73836342b63f1aca7449766c2c15f2822`, and Datadog iOS `TraceContext`/`HTTPHeadersWriter` at `DataDog/dd-sdk-ios@6462c0b81f5221072008443925d8bbf18aa5750b`; exact paths/functions are recorded in `docs/competitor-research/objc-trace-correlation-2026-06-16.md`. Added dependency-free `LBWOpenTelemetrySpanContext`, `LBWTrace openTelemetrySpanContextWithTraceID:...`, `contextFromOpenTelemetrySpanContext:`, and `spanAttributesFromOpenTelemetrySpanContext:...`: apps can copy only OTel trace ID/span ID/trace flags into a fresh LogBrew child context/span without OTel dependencies/exporters/processors, tracestate/baggage, global `NSURLSession` patching, payload/header/raw URL capture, or raw propagation metadata. Packaged `trace_correlation.m` and installed source smoke now start from an OTel-compatible parent while preserving Objective-C trace/error/log/action/network/metric/URLSession/lifecycle proof. Local evidence: `bash scripts/check_objc_package.sh`, `bash scripts/real_user_objc_smoke.sh`, `python3 -m py_compile scripts/check_objc_trace_correlation_payload.py`, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Objective-C gaps: automatic lifecycle/URLSession instrumentation, live OTel `Context`/`Span` extraction, baggage/tracestate, rich span events/exceptions, URLSession phase timings, and native symbolication parity.
- 2026-06-17: Rust Rocket request tracing gap reduced after source reading Rocket `rwf2/Rocket@v0.5.1` `AdHoc::on_request`/`on_response`, `Request::headers`/`route`/`local_cache`, and `Route::uri`, plus Sentry Rust `sentry-actix`/`sentry-tower` request integration patterns at `getsentry/sentry-rust@0.48.2`; exact notes are in `docs/competitor-research/rust-tracing-bridge-2026-06-15.md`. Added packaged `examples/rocket_request_fairing.rs`, README guidance, shared `scripts/check_rust_framework_request_payload.py` plus Rocket wrapper, `scripts/real_user_rust_rocket_smoke.sh`, and CI/release/public-verifier wiring. The Rocket example uses app-owned fairings, records timing in `on_request`, builds telemetry in `on_response` after route matching, emits a request span plus `http.server.duration`, continues incoming W3C `traceparent`, returns one outgoing `traceparent`, disables Rocket logs for parseable stdout, and avoids default LogBrew dependency weight, global HTTP patching, payload/header/raw URI/query/hash capture, baggage, and tracestate. Local evidence: direct Rocket example checker, installed Axum/Actix/Rocket smokes with shared validator, Rust fmt/default tests/all-feature tests/clippy all-targets/all-features, broad Rust smoke package proof at 26 files / 265.3 KiB / 55.0 KiB, public verifier contract tests, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, and diff hygiene passed. Remaining Rust gaps: automatic `tracing-opentelemetry` extraction/processor interop, rich span events/exceptions, links, tracestate/baggage, automatic framework/outbound/DB/cache/queue spans, and native symbolication parity.
- 2026-06-17: React Native app-owned native bridge scope sync gap reduced after reusing source review of Sentry React Native `scopeSync.ts`/`wrapper.ts`/`reactnavigation.ts` at `getsentry/sentry-react-native@580fb5c7bf39cc1a8caf7a30af9078c887eb40b9` and Datadog React Native resource tracing/headers at `DataDog/dd-sdk-reactnative@92462dccefd689815d87dabbad0d41572cd06cca`; exact notes are in `docs/competitor-research/react-native-trace-correlation-2026-06-16.md`. Added explicit `@logbrew/react-native/native-bridge` with `createLogBrewNativeBridgeScope(...)`, `syncLogBrewNativeBridgeScope(...)`, `clearLogBrewNativeBridgeScope(...)`, and `withLogBrewNativeBridgeScope(...)`: apps can pass primitive trace/log scope into their own native bridge callback or `setLogBrewScope(...)` adapter, keep it active through async native calls, and clear afterward without installing a native module, global scope sync, native SDK state ownership, bridge argument inspection, user/session identity sync, payload/header capture, baggage, or tracestate. Packaged `native-bridge-scope.mjs` plus installed smoke prove primitive metadata dropping, trace/span propagation, async clear, CJS/ESM/type exports, package tarball contents, launcher/npm-helper discovery, and React Native 0.86.0/React 19.2.7 install; `npm pack --json` proof is 28 files / 176,350 bytes unpacked / 31,288 bytes compressed. Local evidence: `npm test`, `python3 scripts/check_js_sources.py js/logbrew-react-native`, `bash scripts/real_user_react_native_smoke.sh`, JS ESLint, publint 0.3.21, markdown links, confidentiality scan, release metadata, backend reports, ShellCheck 0.11.0, generated-artifact hygiene, and diff hygiene passed. Remaining React Native gaps: automatic navigation/lifecycle/resource/native-bridge instrumentation, automatic fetch/XHR patching, OTel context ingestion, baggage/tracestate, rich span events/exceptions, source maps, and native symbolication parity.
- 2026-06-17: React Native reversible instrumentation setup gap reduced after refreshing public source reads: Sentry React Native `reactnavigation.ts`/`scopeSync.ts` at `getsentry/sentry-react-native@580fb5c7bf39cc1a8caf7a30af9078c887eb40b9`, Datadog React Native `XHRProxy.ts` at `DataDog/dd-sdk-reactnative@92462dccefd689815d87dabbad0d41572cd06cca`, and OpenTelemetry JS `StackContextManager.ts` at `open-telemetry/opentelemetry-js@e8b0b7618beaf29b79e2b8ab6e0235e0dfd60cfc`; exact notes are in `docs/competitor-research/react-native-trace-correlation-2026-06-16.md`. Added explicit `@logbrew/react-native/instrumentation` with `createLogBrewReactNativeInstrumentation(...)`: a removable app-owned setup handle that composes AppState lifecycle spans, React Navigation spans, target-scoped resource fetch spans, and native bridge scope sync while returning `trace`, `resourceFetch(...)`, `syncNativeBridgeScope(...)`, `withNativeBridgeScope(...)`, `remove()`, and `stop()`. Packaged `instrumentation-kit.mjs` validates shared trace correlation, listener removal, native bridge clear, target-scoped `traceparent`, primitive metadata dropping, CJS/ESM/type exports, launcher/npm-helper discovery, and React Native 0.86.0/React 19.2.7 install. `npm pack --json --dry-run` package size is 33 files / 199,844 bytes unpacked / 34,537 bytes compressed. Local evidence: `npm test`, `python3 scripts/check_js_sources.py js/logbrew-react-native`, `bash scripts/real_user_react_native_smoke.sh`, JS ESLint, publint 0.3.21, markdown links, confidentiality scan, release metadata, backend reports, ShellCheck 0.11.0, generated-artifact hygiene, and diff hygiene passed. Remaining RN gaps: true hidden automatic instrumentation/global fetch/XHR patching, OTel context ingestion, baggage/tracestate, rich span events/exceptions, source maps, and native symbolication parity; keep LogBrew default path explicit/reversible unless a framework package owns heavier patching.
- 2026-06-17: Rust tracing rich span-event gap reduced after re-reading Sentry Rust `sentry-tracing` event conversion at `getsentry/sentry-rust@e33b7ff20eb5bf948eacf89d7eecdcc59b31d4f3`, Tokio `tracing-opentelemetry` span event/error mapping at `tokio-rs/tracing-opentelemetry@1d5422f1f37932fd65e434da618b305d4c94ee9c`, and Datadog Rust OTel span event/link mapping at `DataDog/dd-trace-rs@0d1d982f0464318f5c1a21c2db1c84b58ff2c95c`; exact notes are in `docs/competitor-research/rust-tracing-bridge-2026-06-15.md`. `LogBrewTracingLayer::with_span_events()` now adds privacy-bounded closed-span summaries: `tracingSpanEventCount`, `tracingSpanErrorEventCount`, `tracingLastErrorLevel`, and `tracingLastErrorTarget`, marks only the current span as error, and still records the actual tracing event as a LogBrew log without copying error messages, stacks, payloads, headers, arbitrary event fields, full URLs, baggage, or tracestate into span metadata. Local evidence: Rust fmt/default tests/all-feature tracing-layer tests/all-feature tests/clippy all-targets-all-features, `bash scripts/real_user_rust_tracing_smoke.sh`, `bash scripts/real_user_rust_smoke.sh` with package proof 26 files / 268.0 KiB / 55.5 KiB, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Rust gaps: automatic `tracing-opentelemetry` extraction/processor interop, full span event arrays/exceptions/links, tracestate/baggage, automatic outbound/DB/cache/queue spans, and native symbolication parity.
- 2026-06-17: Unity automatic lifecycle gap reduced after re-checking current Sentry Unity `SentryMonoBehaviour.UpdatePauseStatus(...)`/`OnApplicationPause(...)`/`OnApplicationFocus(...)`, Datadog Unity `DdUnityLogHandler` attach/detach forwarding, and OpenTelemetry .NET `RuntimeContext`/`AsyncLocalRuntimeContextSlot<T>` at the commits recorded in `docs/competitor-research/unity-trace-correlation-2026-06-16.md`. Added source-only `UnityLifecycleTracker`: apps instantiate it in their own `MonoBehaviour`, wire app-owned pause/focus callbacks, get deduplicated lifecycle transition spans with previous-state duration, default plus per-transition primitive Unity metadata, and active trace correlation through the existing lifecycle span path. It avoids hidden GameObjects, global lifecycle subscriptions, local session-health derivation, Unity API patching, payload/header/query capture, baggage, and tracestate. Packaged `examples/lifecycle_tracker/LifecycleTracker.cs` proves source and installed UPM tarball output with the existing lifecycle payload checker. Local evidence: `dotnet run --project unity/logbrew-unity/tests/LogBrew.Unity.Tests/LogBrew.Unity.Tests.csproj --configuration Release` with 20 tests, `bash scripts/check_unity_package.sh`, `bash scripts/real_user_unity_smoke.sh`, generated-artifact hygiene, and initial diff hygiene passed. Remaining Unity gaps: `UnityWebRequest` convenience instrumentation, coroutine convenience instrumentation beyond explicit wrapping, OTel context ingestion, rich span events/exceptions, URL/request phase timings, and native crash/symbolication parity.
- 2026-06-17: UnityWebRequest convenience gap reduced after re-reading Sentry Unity `UnityWebRequestTransport.SendEnvelopeAsync(...)`/`CreateWebRequest(...)`/`GetResponse(...)`, Datadog Unity `DatadogTrackedWebRequest.SendWebRequest(...)` plus `ResourceTrackingHelperTest` manual/header paths, and OpenTelemetry .NET `TraceContextPropagator.Inject(...)` at the commits recorded in `docs/competitor-research/unity-trace-correlation-2026-06-16.md`. Added source-only `UnityRequestTracker`: apps pass an app-owned header setter such as `request.SetRequestHeader`, the tracker writes exactly one normalized `traceparent`, computes duration from an app-supplied realtime clock, merges default plus per-request primitive Unity metadata, and emits through the existing sanitized request span path. It avoids `UnityEngine.Networking` runtime dependency, Datadog-style wrapper ownership, hidden/global request instrumentation, request disposal/completion ownership, payload/header/full URL/query/hash capture, baggage, and tracestate. Packaged `examples/request_tracker/RequestTracker.cs` plus `scripts/check_unity_request_tracker_payload.py` prove source and installed UPM tarball output. Local evidence: Unity package tests now 21 tests, `bash scripts/check_unity_package.sh`, `bash scripts/real_user_unity_smoke.sh`, `python3 scripts/check_python_sources.py`, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, and diff hygiene passed. Remaining Unity gaps: coroutine convenience instrumentation beyond explicit wrapping, OTel context ingestion, rich span events/exceptions, URL/request phase timings, and native crash/symbolication parity.
- 2026-06-17: Rust `tracing-opentelemetry` active-context gap reduced after re-reading Tokio `tracing-opentelemetry` `OpenTelemetrySpanExt::context`/private `OtelData` layer path, OpenTelemetry Rust `TraceContextExt`/`SpanContext`/`TraceFlags`, Sentry Rust `SentrySpanProcessor`, and Datadog Rust `SdkSpan` at commits already recorded in `docs/competitor-research/rust-tracing-bridge-2026-06-15.md`. Added opt-in `tracing-opentelemetry` feature with `opentelemetry_span_context_from_current_tracing_span()` and `opentelemetry_span_context_from_tracing_span(...)`: apps that already install a `tracing_opentelemetry` layer can copy only valid active OTel trace ID/span ID/trace flags into LogBrew's dependency-free `OpenTelemetrySpanContext`, returning `None` without an OTel layer and without reading tracestate, baggage, attributes, events, links, exporters, payloads, headers, raw URLs, or raw propagation metadata. Packaged `examples/tracing_opentelemetry_bridge.rs` plus installed `tracing-otel-app` smoke prove direct `opentelemetry@0.32`/`tracing-opentelemetry@0.33` app dependencies, one LogBrew child span under an OTel parent, one generated downstream trace header not emitted in metadata, and unchanged default/`tracing` feature installs; package proof is 27 files / 281.4 KiB / 58.1 KiB. Local evidence: Rust fmt, default tests, all-feature tests with 6 tracing-layer tests, all-target/all-feature clippy, `bash scripts/real_user_rust_tracing_smoke.sh`, Python source syntax, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, and diff hygiene passed. Remaining Rust gaps: full OTel span processor/exporter interop, event arrays/exceptions/links, tracestate/baggage, automatic outbound/DB/cache/queue spans, and native symbolication parity.
- 2026-06-17: Unity coroutine convenience gap reduced after re-reading Sentry Unity `SentryMonoBehaviour.QueueCoroutine(...)`/`Update()`/`StartCoroutine(...)`/lifecycle auto-span hooks at `getsentry/sentry-unity@4cda4392328f489a0b22490f0f0a2cc401cfc622`, Datadog Unity `DdUnityLogHandler.Attach(...)`/`Detach(...)`/`LogException(...)`/`LogFormat(...)` at `DataDog/dd-sdk-unity@309e84de7c20af6ec47c2ff3e6c6f8d339937e93`, and OpenTelemetry .NET `RuntimeContext`/`AsyncLocalRuntimeContextSlot<T>` at `open-telemetry/opentelemetry-dotnet@184b1cfd4a48f1fe6240a1b76f39c37efb38f465`; exact notes are in `docs/competitor-research/unity-trace-correlation-2026-06-16.md`. Added source-only `UnityCoroutineTracker`: apps pass an owned `IEnumerator`, app-provided ID/timestamp/realtime clock, and optional Unity context; LogBrew creates a child trace context under the supplied/active trace, reactivates it per `MoveNext()`/`Reset()`, and emits one `unity.coroutine:<name>` completion or exception-type-only failure span with primitive metadata. It avoids hidden `MonoBehaviour` objects, global coroutine scheduling/patching, ambient async context, yielded-value capture, exception messages/stacks, payload/header/full URL/query capture, baggage, and tracestate. Local evidence: Unity tests now 22 tests, `bash scripts/check_unity_package.sh`, `bash scripts/real_user_unity_smoke.sh`, Python source syntax, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, and diff hygiene passed. Remaining Unity gaps: OTel context ingestion, richer span events/exceptions beyond bounded fields, URL/request phase timings, optional heavier hidden/global instrumentation for teams that explicitly want it, and native crash/symbolication parity.
- 2026-06-17: Kotlin live OpenTelemetry Span/Context extraction gap reduced after refreshing source reads: OpenTelemetry Java `Span.current()`/`Span.fromContext(...)`/`Span.wrap(...)`/`SpanContext`/`Context.current()` at `open-telemetry/opentelemetry-java@a00536a2c7b3a3f7a0952dbea12e9249da18611d`, Sentry Java `PropagationContext`/`SpanContext` at `getsentry/sentry-java@ba010111864967003758a5e4d750dfe04f995c18`, and Datadog Android `TraceContext` at `DataDog/dd-sdk-android@e07c4cc6a23b51d4a45602787cea3f3f1db7b8b0`; exact paths/functions are recorded in `docs/competitor-research/kotlin-android-trace-correlation-2026-06-16.md`. Added dependency-free `LogBrewOpenTelemetry` exact-type reflection bridge with `spanContextFromCurrentSpan()`, `traceContextFromCurrentSpan()`, `spanContextFromSpan(...)`, `traceContextFromSpan(...)`, `spanContextFromContext(...)`, and `traceContextFromContext(...)`: apps that already install `io.opentelemetry:opentelemetry-api` can copy only valid trace ID/span ID/trace flags from live OTel `Span.current()`, explicit `Span`, or `Context` into a fresh LogBrew child without adding OTel to default LogBrew installs. It returns `null` when OTel is absent or invalid and avoids OTel exporters/processors, attributes, links, events, tracestate, baggage, payloads, headers, raw propagation metadata, and global HTTP patching. Local evidence: `bash scripts/check_kotlin_style.sh` with ktlint 1.8.0, compiled Kotlin tests now 24 tests, `bash scripts/check_kotlin_package.sh`, `bash scripts/real_user_kotlin_smoke.sh` with installed Gradle app using `io.opentelemetry:opentelemetry-api:1.63.0`/`opentelemetry-context:1.63.0`, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, and diff hygiene passed. Remaining Kotlin gaps: automatic Android lifecycle/OkHttp/HttpURLConnection instrumentation, coroutine context propagation beyond explicit thread-local scopes, baggage/tracestate, rich span events/exceptions, DB/cache/queue spans, and native crash/symbolication parity.
- 2026-06-17: Rejected Swift live OpenTelemetry bridge design after a focused `LogBrewOpenTelemetrySupport` SwiftPM product/test passed locally but a default `LogBrew` consumer still resolved `opentelemetry-swift-core@2.5.0` and `swift-atomics@1.3.0` at package level. Do not add optional SwiftPM products with OTel dependencies inside `swift/logbrew-swift` unless SwiftPM package traits or another approved packaging boundary can prove default installs stay dependency-free; future Swift live OTel extraction needs a separate approved package or another dependency-free design. Evidence: `swift test --package-path swift/logbrew-swift --scratch-path /tmp/logbrew-swift-otel-test` passed before revert, then `swift package show-dependencies` in a default `LogBrew` consumer showed the unwanted OTel/Atomics dependency graph.
- 2026-06-17: C++ live OpenTelemetry span-copy gap reduced after refreshing source reads: OpenTelemetry C++ `SpanContext`/`Span::GetContext`/`GetSpan(context)`/`Tracer::GetCurrentSpan`/ID `ToLowerBase16` helpers at `open-telemetry/opentelemetry-cpp@d6035a817b363db74f02a401cfbe396831b60109`, Sentry Native trace propagation at `getsentry/sentry-native@fd19f353875b7043613b7c08f565aebbf8490e16`, and Datadog C++ W3C extraction/injection at `DataDog/dd-trace-cpp@0a1dc56b418262cee758c33fa6f488e9e44ab6ed`; exact paths/functions are recorded in `docs/competitor-research/cpp-trace-correlation-2026-06-16.md`. Added header-only `try_open_telemetry_span_context_from_span_context(...)`, `open_telemetry_span_context_from_span_context(...)`, `try_open_telemetry_span_context_from_span(...)`, `open_telemetry_span_context_from_span(...)`, `try_open_telemetry_span_context_from_span_pointer(...)`, and `open_telemetry_span_context_from_span_pointer(...)`: apps that already include OpenTelemetry C++ can copy only valid trace ID/span ID/trace flags from live OTel spans/current span pointers into LogBrew child contexts without adding OTel headers/libs to default LogBrew source builds. It avoids exporters/processors, baggage/tracestate, attributes/events/links, HTTP patching, payload/header/full-URL capture, and raw propagation metadata. Local evidence: `bash scripts/check_cpp_package.sh` with 119 checks, `bash scripts/real_user_cpp_smoke.sh`, a throwaway compile/run against real OpenTelemetry C++ headers proving `SpanContext`, `DefaultSpan`, and `nostd::shared_ptr<Span>` adapter compatibility, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining C++ gaps: named OTel `Context` convenience overloads, full OTel processor/exporter interop, baggage/tracestate, rich events/exceptions/links, automatic outbound/DB/cache/queue spans, URL/request phase timings, lifecycle spans, and native symbolication parity.
- 2026-06-17: Objective-C live OpenTelemetry-compatible object extraction gap reduced after refreshing source reads: OpenTelemetry Swift `opentelemetry-swift@291fe3fff413ae9277ac36aec9fd9b51c1caa7e0`, OpenTelemetry Swift Core `opentelemetry-swift-core@4f85f2a5c8138a72384be3edd1dcfc2cc97b297f`, Sentry Cocoa `sentry-cocoa@5804f3336b7be802acced20d716d7c092d0d8a6b`, and Datadog iOS `dd-sdk-ios@f5464fe3c6ebec40a80bce262d64a8381c800261`; exact paths/functions are recorded in `docs/competitor-research/objc-trace-correlation-2026-06-16.md`. Added dependency-free `LBWTrace openTelemetrySpanContextFromSpanContextObject:error:`, `openTelemetrySpanContextFromSpanObject:error:`, `contextFromOpenTelemetrySpanObject:error:`, and `spanAttributesFromOpenTelemetrySpanObject:...`: Objective-C-compatible span/context objects or app-owned NSObject adapters around Swift OTel can expose `context`/`spanContext`, `traceId`/`traceID`, `spanId`/`spanID`, and `traceFlags`/`isSampled`; LogBrew copies only validated W3C trace ID/span ID/flags into a child context. Nil or explicitly invalid live spans return nil without error; malformed non-empty IDs/flags return redacted `validation_error`. It avoids installing `opentelemetry-swift-core`, reading Swift-only value types directly, global context hooks, exporters/processors, baggage/tracestate, attributes/events/links, `NSURLSession` patching, payload/header/full-URL capture, and raw propagation metadata. Local evidence: `bash scripts/check_objc_package.sh`, `bash scripts/real_user_objc_smoke.sh` with extracted installed consumer object-bridge proof, ShellCheck 0.11.0, markdown links, confidentiality scan, release metadata, backend reports, generated-artifact hygiene, diff hygiene, and thermo review passed. Remaining Objective-C gaps: direct Swift-only OTel extraction without an app-owned adapter, automatic UIKit/AppKit lifecycle instrumentation, automatic URLSession instrumentation, baggage/tracestate, rich span events/exceptions, URLSession phase timings, and native symbolication parity.
- 2026-06-17: React Native release-artifact/source-map bundle gap reduced after public source reads of Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` React Native `injectDebugId.ts`/`upload.ts`/fixtures and Sentry React Native `getsentry/sentry-react-native@580fb5c7bf39cc1a8caf7a30af9078c887eb40b9` Gradle/Xcode/debug-ID scripts; exact paths are in `docs/competitor-research/source-maps-debug-symbols-2026-06-13.md`. LogBrew JS release-artifact manifest/debug-ID prep now scans `.js`, `.mjs`, `.bundle`, and `.jsbundle`, preserves `app:///` prefixes, and proves React Native-style `main.jsbundle`/`index.android.bundle` sibling `.map` flows without upload, backend symbolication claims, raw source content, payload/header capture, or runtime dependencies. Local evidence: focused unit tests, `bash scripts/real_user_js_release_artifact_smoke.sh`, Python syntax, ShellCheck, markdown links, backend contract reports, confidentiality scan, release metadata, generated-artifact hygiene, diff hygiene, and thermo review. Remaining gaps: backend upload/lookup/symbolication contract, Vite/Next/React Native real app minified-error proof, source-map upload auth/failure/retry handling, and native dSYM/ProGuard/PDB/Unity symbol workflows.
- 2026-06-17: Native/mobile release-artifact dry-run gap reduced after public source reads of Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` ProGuard/debug-file upload paths, Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@a5a55f8448cddd9e0282a89c436b339704b30ab3` ProGuard/native-symbol tasks, and Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` dSYM/ELF/Unity symbol upload commands. Added dependency-free `scripts/create_native_release_artifact_manifest.py` for `ios_dsym` and `android_proguard_mapping` dry-run manifests with relative paths, content-derived artifact IDs, dSYM structure checks, ProGuard/R8 class mapping validation, artifact-root confinement, symlink blocking, and no symbol bytes or mapping class names in output. Added `scripts/real_user_native_release_artifact_smoke.sh`, focused unit tests, CI/release-readiness/public-verifier/checklist gates, and updated backend contract/competitor docs. Local evidence: 16 focused release-artifact/verifier-gate tests, native smoke, Python syntax, ShellCheck 0.11.0, workflow YAML parse, markdown links, backend contract reports, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. Remaining gaps: backend upload/lookup/symbolication contract, dSYM UUID/build-id extraction, Android native symbols, PDB/ELF/Breakpad/Unity symbols, fake-intake upload auth/failure/retry proof, and real minified/native symbolication proof.
- 2026-06-17: Android native `.so` release-artifact dry-run gap reduced after re-reading Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@a5a55f8448cddd9e0282a89c436b339704b30ab3` `SentryUploadNativeSymbolsTask.kt`, Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/debug_files/upload.rs`, and Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` `packages/base/src/commands/elf-symbols/elf.ts`, `upload.ts`, `interfaces.ts`, and `elf-constants.ts`. Added dependency-free `android_native_symbols` support to `scripts/create_native_release_artifact_manifest.py`: it parses ELF32/ELF64 headers and section names, maps Android ABIs, extracts GNU/Go build IDs, reports `debug_info`/`symbol_table`/`dynamic_symbol_table`, falls back to a file hash only when `.text` exists, blocks non-ELF/unsupported/no-ID/no-symbol files, and keeps symbol/debug-section bytes out of manifests. Focused evidence so far: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests/test_native_release_artifact_manifest.py` and `bash scripts/real_user_native_release_artifact_smoke.sh` passed. Remaining gaps: backend upload/lookup/symbolication contract, dSYM UUID extraction, PDB/Breakpad/Unity symbol dry-runs, Android native size/count limits, fake-intake upload auth/failure/retry proof, and real minified/native symbolication proof.
- 2026-06-17: dSYM UUID dry-run gap reduced after re-reading Datadog CI `DataDog/datadog-ci@74ed439f292a09a44d38de1f4f5ac092e9528b75` `packages/base/src/commands/dsyms/upload.ts`, `dsyms/utils.ts`, and `dsyms/interfaces.ts`, plus Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs`. Datadog's pattern is `dwarfdump --uuid` parsing, `lipo` thinning, UUID-named archives, and UUID metadata; Sentry's pattern is `symbolic` debug-file parsing and `Object::debug_id()` identity. LogBrew now extracts Mach-O/fat Mach-O `LC_UUID` values from dSYM DWARF files in `scripts/create_native_release_artifact_manifest.py`, records normalized UUID/arch metadata, warns without blocking when UUIDs are missing, and keeps Mach-O payload bytes out of manifests. Local evidence: 21 release-artifact/verifier tests, `bash scripts/real_user_native_release_artifact_smoke.sh`, Python syntax, ShellCheck 0.11.0, workflow YAML parse, markdown links, backend contract reports, confidentiality scan, release metadata, generated-artifact hygiene, diff check, automation TOML parse/prompt-size check, and thermo review passed. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, PDB/Breakpad/Unity symbol dry-runs, Android native size/count limits, fake-intake upload auth/failure/retry proof, real minified/native symbolication proof, and fuller multi-toolchain dSYM edge cases.
- 2026-06-17: Breakpad `.sym` release-artifact dry-run gap reduced after refreshing current Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/breakpad.ts`, `upload.ts`, and `__tests__/pe.test.ts`, plus Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif.rs`. Datadog parses ASCII `MODULE` headers, normalizes GUID/age, maps CPU, infers `debug_info` from early `FILE` records, and uploads `.sym` as a PE-symbol source; Sentry treats Breakpad as a debug-info format through `symbolic` with debug IDs/features. LogBrew now adds dependency-free `breakpad_symbols` dry-run validation to `scripts/create_native_release_artifact_manifest.py`: it discovers `.sym` files, validates MODULE identifiers, records GUID/age/module OS/architecture/source quality with basename-only module names, warns on unknown CPUs, and keeps FILE/FUNC/PUBLIC lines, local module directories, source paths, function names, and symbol payload bytes out of manifests. Local evidence: 25 release-artifact/verifier tests, `bash scripts/real_user_native_release_artifact_smoke.sh`, Python source syntax, ShellCheck 0.11.0, workflow YAML parse via Ruby, markdown links, backend contract reports, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. `scripts/create_native_release_artifact_manifest.py` is now 982 lines; decompose native artifact validators before adding another large symbol format. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, PDB/Unity symbol dry-runs, Android native size/count limits, fake-intake upload auth/failure/retry proof, real minified/native symbolication proof, and richer Breakpad duplicate/richness handling.
- 2026-06-17: PE/PDB release-artifact dry-run gap reduced after refreshing current Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/pe.ts`, `pe-constants.ts`, `upload.ts`, and `__tests__/pe.test.ts`, plus Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif.rs` and `src/utils/dif_upload/mod.rs`. Datadog parses PE `MZ`/`PE` headers, machine architecture, CodeView `RSDS` GUID/age/PDB filename, and associated sibling `.pdb`; Sentry treats PDB/Portable PDB as debug-info formats and fixes PDB age from PE metadata. LogBrew now adds `dotnet_pdb` dry-run validation and moves PE/Breakpad parsing into `scripts/native_release_artifact_pe.py`: it discovers `.dll`/`.exe`, extracts CodeView GUID/age, requires a non-empty sibling `.pdb`, records basename-only PDB names/relative paths, and keeps PDB payload bytes plus embedded developer-machine PDB directory strings out of manifests. Local evidence: 27 release-artifact/verifier tests, `bash scripts/real_user_native_release_artifact_smoke.sh`, Python source syntax over 66 files, ShellCheck 0.11.0, Ruby YAML parse, markdown links, backend reports, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. `scripts/create_native_release_artifact_manifest.py` is 988 lines after extraction and dispatcher simplification; keep decomposing validators before adding Unity/PDB-richness work. Remaining gaps: backend upload/lookup/symbolication contract, Unity symbol dry-run, Android native size/count limits, fake-intake upload auth/failure/retry proof, real minified/native symbolication proof, PE/PDB duplicate/richness handling, Portable PDB direct parsing, and PDB payload validation beyond PE CodeView identity.
- 2026-06-17: Native release-artifact limit and duplicate handling gap reduced after refreshing current Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/elf-symbols/upload.ts` and `packages/base/src/commands/pe-symbols/upload.ts`, Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs`, `src/utils/dif_upload/error.rs`, and `src/constants.rs`, and Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@f62e071eea2e6db2b0e646fcbd4f0dddd17fffb5` `SentryUploadNativeSymbolsTask.kt` plus `SentryPluginExtension.kt`. Functions/classes read: Datadog `ElfSymbolsUploadCommand.getElfSymbolFiles`, `removeBuildIdDuplicates`, `performElfSymbolsUpload`, `PESymbolsUploadCommand.getPESymbolFiles`, `removeBuildIdDuplicates`, and `performPESymbolsUpload`; Sentry `DifUpload::new`, `valid_size`, `validate_dif`, `filter_bad_sources`, and `ValidationError::TooLarge`; Sentry Android `SentryUploadNativeSymbolsTask.getArguments` and native-symbol extension options. LogBrew now emits explicit manifest `limits`, defaults to 500 candidate symbol files and 2GB per symbol file/artifact with CLI overrides, blocks over-count and oversized native/mobile dry runs before parsing large files, dedupes Android native `.so` identities by keeping the richer symbol source, moved ELF parsing plus Android duplicate selection into `scripts/native_release_artifact_elf.py`, and centralized binary I/O helpers in `scripts/native_release_artifact_io.py` so `scripts/create_native_release_artifact_manifest.py` shrank to 858 lines. This stays SDK/tooling-side only: no upload, auth, storage, lookup, or symbolication claim. Local evidence: 35 focused release-artifact/verifier/backend-report tests, `bash scripts/real_user_native_release_artifact_smoke.sh`, Python source syntax over 67 files, ShellCheck 0.11.0, Ruby YAML parse, backend contract reports, markdown links, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, Unity symbol dry-run, fake-intake upload auth/failure/retry proof, real Vite/Next/React Native minified-error proof, PE/PDB and Breakpad duplicate/richness handling beyond Android ELF, Portable PDB direct parsing, PDB payload validation beyond PE CodeView identity, and Unity/native symbolication parity.
- 2026-06-17: Unity release-artifact dry-run gap reduced after refreshing current Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/unity-symbols/upload.ts`, `helpers.ts`, `interfaces.ts`, `README.md`, and `__tests__/upload.test.ts`; Sentry Unity `getsentry/sentry-unity@4cda4392328f489a0b22490f0f0a2cc401cfc622` `src/Sentry.Unity.Editor/Android/DebugSymbolUpload.cs` and `test/Sentry.Unity.Editor.Tests/Android/DebugSymbolUploadTests.cs`; and Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` plus `src/constants.rs`. Functions/classes read: Datadog `UnitySymbolsUploadCommand.verifyParameters`, `getBuildId`, `performSoUpload`, `performIl2CppMappingUpload`, and `getMappingMetadata`; Sentry Unity `AppendUploadToGradleFile`, `TryCopySymbolsToGradleProject`, `GetSymbolUploadPaths`, and `GetMappingFilePath`; Sentry CLI `walk_difs_zip`, `walk_difs_directory`, `create_il2cpp_mappings`, `valid_size`, and `validate_dif`. LogBrew now supports dependency-free `unity_symbols` manifests: a safe `build_id` is required, Unity `.so`/`.dbg.so`/`.sym.so` files reuse shared ELF validation and duplicate/richer-symbol handling, optional `LineNumberMappings.json` is metadata-only, missing managed mappings warn without blocking native symbols, and manifests omit source paths, method names, symbol bytes, query strings, and upload-only auth material. Main native manifest script remains under 1k lines at 908 after isolating Unity helpers in `scripts/native_release_artifact_unity.py` and reusing `validated_elf_symbol_file(...)`. This is still not upload/storage/lookup/symbolication support. Local evidence: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests` ran 118 tests, focused native manifest tests ran 24 tests, `bash scripts/real_user_native_release_artifact_smoke.sh`, Python source syntax over 69 files, ShellCheck 0.11.0, Ruby workflow YAML parse, backend report checker, markdown links, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. `automation_update` was unavailable, so the automation TOML fallback was updated/validated. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, fake-intake upload auth/failure/retry proof, real Vite/Next/React Native minified-error proof, PE/PDB and Breakpad duplicate/richness handling beyond Android ELF, Portable PDB direct parsing, PDB payload validation beyond PE CodeView identity, Unity ZIP/archive support, and Unity/native symbolication parity.
- 2026-06-18: PE/PDB and Breakpad duplicate/richness gap reduced after re-reading Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/pe-symbols/upload.ts`, `breakpad.ts`, and `pe.ts`, plus Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs` and `src/utils/dif.rs`. Functions/classes read: Datadog `PESymbolsUploadCommand.getPESymbolFiles`, `removeBuildIdDuplicates`, `getSymbolSourcePriority`, `performPESymbolsUpload`, `getBreakpadSymMetadata`, `getPEFileMetadata`, and `getBuildId`; Sentry `DifMatch.debug_id`, `fix_pdb_ages`, `extract_embedded_ppdb`, `validate_dif`, and `DifFile::try_open`. LogBrew now dedupes `breakpad_symbols` and `dotnet_pdb` dry-run manifests by PDB GUID plus age: `debug_info` beats `symbol_table`, same-quality duplicates keep the first relative path, and warnings explain the selected identity without serializing source paths, function names, PDB payload bytes, or symbol bytes. Main native manifest script remains under 1k lines at 911 by keeping the identity/richness logic in `scripts/native_release_artifact_pe.py`. Local evidence: focused native manifest tests ran 26 tests, `bash scripts/real_user_native_release_artifact_smoke.sh`, full `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests` ran 120 tests, Python source syntax over 69 files, ShellCheck 0.11.0, Ruby workflow YAML parse, backend report checker, markdown links, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, fake-intake upload auth/failure/retry proof, real Vite/Next/React Native minified-error proof, Portable PDB direct parsing, PDB payload validation beyond PE CodeView identity, Unity ZIP/archive support, richer PE/Breakpad edge cases, and Unity/native symbolication parity.
- 2026-06-18: Vite source-map build-output gap reduced after re-reading Sentry JavaScript `getsentry/sentry-javascript@ccf5571c78c1116ba15fa7186234c86dcf57db5a` `packages/sveltekit/src/vite/sourceMaps.ts` and `dev-packages/test-utils/src/sourcemap-upload-utils.ts`, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/upload.ts`, `validation.ts`, `interfaces.ts`, and `helpers/git/format-git-sourcemaps-data.ts`. Functions/classes read: Sentry `makeCustomSentryVitePlugins`, `_getUpdatedSourceMapSettings`, custom `closeBundle`, `getFiles`, `detectSentryRelease`, `getArtifactBundles`, `getDebugIdPairs`, and `getSourcemaps`; Datadog `SourcemapsUploadCommand.execute`, `getMatchingSourcemapFiles`, `getLegacyMatchingSourcemapFiles`, `getMinifiedURLAndRelativePath`, `getPayloadsToUpload`, `upload`, `Sourcemap.asMultipartPayload`, `getMetadataPayload`, and `validatePayload`. LogBrew Debug ID prep now supports explicit `--strip-sources-content` dry-runs/writes so Vite source maps can become manifest-ready without `--allow-sources-content`; Debug IDs are generated from the post-strip map payload for stability. `scripts/real_user_vite_release_artifact_smoke.sh` builds a real `vite@8.0.16` app with isolated npm cache and explicit `esbuild@0.28.1`, injects matching Debug IDs, strips raw source content, verifies query/hash-free minified URLs, and proves a generated source-map position resolves back to `src/main.js` without serializing raw source. Local evidence: focused JS artifact/verifier tests ran 19 tests, full unittest discovery ran 122 tests, `bash scripts/real_user_js_release_artifact_smoke.sh`, `bash scripts/real_user_vite_release_artifact_smoke.sh`, Python source syntax over 69 files, ShellCheck 0.11.0, Ruby workflow YAML parse, markdown links, backend reports, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, fake-intake upload auth/failure/retry proof, backend-unminified Vite/Next/React Native error proof, Next and React Native real framework build proof, Portable PDB direct parsing, PDB payload validation beyond PE CodeView identity, Unity ZIP/archive support, and native symbolication parity.
- 2026-06-18: Next.js source-map build-output gap reduced after re-reading Sentry JavaScript `getsentry/sentry-javascript@839391fecb10be6765f55c418197cad541a1784b` `packages/nextjs/src/config/getBuildPluginOptions.ts`, `packages/nextjs/src/config/handleRunAfterProductionCompile.ts`, `packages/nextjs/src/client/clientNormalizationIntegration.ts`, `packages/nextjs/src/config/webpack.ts`, and `dev-packages/test-utils/src/sourcemap-upload-utils.ts`, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/upload.ts`, `validation.ts`, `interfaces.ts`, and `helpers/git/format-git-sourcemaps-data.ts`. Functions/classes read: Sentry `createSourcemapUploadAssetPatterns`, `createSourcemapUploadIgnorePattern`, `createFilesToDeleteAfterUploadPattern`, `shouldSkipSourcemapUpload`, `getBuildPluginOptions`, `handleRunAfterProductionCompile`, `stripSourceMappingURLComments`, `nextjsClientStackFrameNormalizationIntegration`, `getDebugIdPairs`, and `getSourcemaps`; Datadog `SourcemapsUploadCommand.execute`, `addRepositoryDataToPayloads`, `validatePayload`, `Sourcemap.asMultipartPayload`, `getMetadataPayload`, and `normalizeSources`. LogBrew now has `scripts/real_user_next_release_artifact_smoke.sh`: it builds real `next@16.2.9` App Router output with isolated npm cache, `productionBrowserSourceMaps`, React/React DOM 19.2.7, and `@jridgewell/trace-mapping@0.3.31`; injects matching Debug IDs; strips all Next `sourcesContent`; proves the manifest is ready without source-content opt-in; normalizes `app:///_next/static/chunks` URLs without query/hash text; and proves a generated source-map position resolves back to `components/CheckoutProbe.jsx` without serializing raw source. The smoke is wired into CI, release-readiness, public verifier labels, and the SDK readiness checklist. Local evidence: focused release-artifact/verifier tests ran 25 tests, full unittest discovery ran 122 tests, `bash scripts/real_user_js_release_artifact_smoke.sh && bash scripts/real_user_vite_release_artifact_smoke.sh && bash scripts/real_user_next_release_artifact_smoke.sh && bash scripts/real_user_native_release_artifact_smoke.sh`, Python source syntax over 69 files, ShellCheck 0.11.0, Ruby workflow YAML parse, markdown links, backend reports, confidentiality scan, release metadata, generated-artifact hygiene, diff check, and thermo review passed. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, fake-intake upload auth/failure/retry proof, backend-unminified Vite/Next/React Native error proof, React Native real framework build proof/native symbolication, Portable PDB direct parsing, PDB payload validation beyond PE CodeView identity, Unity ZIP/archive support, and native symbolication parity.
- 2026-06-18: React Native source-map build-output gap reduced after re-reading Sentry React Native `getsentry/sentry-react-native@78878471dc46a38eb72268f452a8abac8c1813a2` `packages/core/sentry.gradle.kts`, `sentry-xcode.sh`, `copy-debugid.js`, `has-sourcemap-debugid.js`, and Expo upload CLI logic, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/react-native/injectDebugId.ts`, `upload.ts`, `interfaces.ts`, `utils.ts`, and `validation.ts`. Functions/classes read include Sentry `extractBundleTaskArguments`, `extractBundleTaskArgumentsLegacy`, `forceSourceMapOutputFromBundleTask`, Gradle upload task setup, and Datadog `ReactNativeInjectDebugIdCommand.execute`, `injectDebugIds`, `injectDebugIdIntoFiles`, `injectDebugIdIntoBundle`, `injectDebugIdIntoSourceMap`, `ReactNativeUploadCommand.execute`, `RNSourcemap.asMultipartPayload`, `removeSourcesContentFromSourceMap`, `getBundleName`, and `validatePayload`. LogBrew now has `scripts/real_user_react_native_release_artifact_smoke.sh`: it builds a real `react-native@0.86.0` Android Metro bundle with explicit `@react-native-community/cli@20.1.3`, `@react-native/metro-config@0.86.0`, `react@19.2.7`, Metro 0.84.4, and `@jridgewell/trace-mapping@0.3.31`; injects matching Debug IDs; strips Metro `sourcesContent`; rewrites absolute source-map sources under the app root to relative paths via new `prepare_js_release_artifact_debug_ids.py --strip-source-prefix`; proves the manifest is ready without source-content opt-in; normalizes `app:///react-native` URLs without query/hash text; and proves a generated source-map position resolves back to `index.js` without serializing raw source or temp paths. Wired into CI, release-readiness, public verifier labels, and SDK checklist. Local evidence: `bash scripts/real_user_react_native_release_artifact_smoke.sh`, focused artifact/verifier tests ran 20 tests, full unittest discovery ran 123 tests, JS+Vite+Next+React Native+native release-artifact smokes passed, Python source syntax over 69 files, ShellCheck 0.11.0, Ruby workflow YAML parse, markdown links, backend reports, confidentiality scan, release metadata, generated-artifact hygiene, diff check, automation TOML parse/prompt-size check, and thermo review passed. Remaining gaps: backend release-artifact upload/lookup/symbolication contract, fake-intake upload auth/failure/retry proof, backend-unminified Vite/Next/React Native error proof, React Native native symbolication/optional heavier automatic instrumentation, Portable PDB direct parsing, PDB payload validation beyond PE CodeView identity, Unity ZIP/archive support, and native symbolication parity.
- 2026-06-18: JavaScript release-artifact upload transport gap reduced after re-reading Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/helpers/upload.ts`, `helpers/retry.ts`, `commands/sourcemaps/upload.ts`, `interfaces.ts`, and `validation.ts`; Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/sourcemaps/upload.rs`, `src/utils/dif_upload/mod.rs`, `src/utils/chunks/upload.rs`, `src/api/mod.rs`, and `src/api/errors/api_error.rs`; and Sentry JavaScript `getsentry/sentry-javascript@4a94311d708efffc407535ddcb5d206cda95eee9` `packages/sveltekit/src/vite/sourceMaps.ts`, `packages/gatsby/gatsby-node.js`, and `packages/tanstackstart-react/test/vite/sourceMaps.test.ts`. Competitor pattern: validate local artifacts first, send manifest/metadata and files as multipart parts, keep upload auth material out of runtime SDK config, use bounded retries for server/network failures, do not retry auth or validation failures, support dry-run/no-upload paths, and avoid auth-material echoing. LogBrew now adds loopback-only `scripts/upload_js_release_artifacts.py` plus `scripts/real_user_js_release_artifact_upload_smoke.sh`; it revalidates ready manifests, build-root confinement, artifact byte sizes/SHA-256s, redacted endpoints, multipart manifest/minified/source-map parts, auth failure, validation failure, retryable 503-to-202 recovery, dry-run, and local tamper rejection without sending fake auth material, query text, temp paths, or raw `sourcesContent` in reports or multipart metadata. Wired into CI, release-readiness, public verifier labels, and SDK readiness checklist; docs/backend handoff now say this is still not public backend upload/storage/lookup/symbolication. Local evidence this cycle: 15 focused upload/gate tests; JS upload, base JS, Vite 8.0.16, Next 16.2.9, React Native 0.86.0, and native release-artifact smokes; full unittest discovery 128 tests; Python source syntax over 71 files; ShellCheck 0.11.0; workflow YAML parse; markdown links; backend reports; confidentiality scan; release metadata; generated-artifact hygiene; and diff check passed. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication contract, backend-unminified Vite/Next/React Native error proof, native upload fake-intake proof, public build-plugin/tool packaging, Portable PDB direct parsing, PDB payload validation, Unity ZIP/archive support, and native/mobile symbolication parity.
- 2026-06-18: Native release-artifact upload transport gap reduced after re-reading Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/helpers/upload.ts`, `helpers/retry.ts`, `commands/dsyms/upload.ts`, `commands/elf-symbols/upload.ts`, `commands/pe-symbols/upload.ts`, and `commands/unity-symbols/upload.ts`; Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/debug_files/upload.rs` and `src/utils/dif_upload/mod.rs`; Sentry Android Gradle Plugin `getsentry/sentry-android-gradle-plugin@167c79e7b0a70d37af9ee82c67b9ed764f2b735e` `SentryUploadNativeSymbolsTask.kt`; and Sentry Unity `getsentry/sentry-unity@a9bebf56a8808b866ff11330bf42030685701cf9` `DebugSymbolUpload.cs`. Competitor pattern: re-walk and validate local debug artifacts, support dry-run/no-upload, send metadata/files as multipart upload payloads, retry transient server/network failures, and leave storage/lookup/symbolication to backend services. LogBrew now extracts shared loopback upload primitives into `scripts/release_artifact_upload_common.py`, refactors `scripts/upload_js_release_artifacts.py` to use them, and adds loopback-only `scripts/upload_native_release_artifacts.py` plus `scripts/real_user_native_release_artifact_upload_smoke.sh`. The native uploader revalidates ready manifests, artifact-root confinement, symlink-free paths, file/directory byte sizes, SHA-256s, directory tree hashes, file counts, basename multipart names, dry-run, auth failure, validation failure, 503-to-202 retry, and tamper-before-network failure without echoing sample auth values, endpoint query text, temp paths, source paths, function names, or symbol bytes. Wired into CI, release-readiness, `scripts/check_public_sdks.sh`, SDK readiness checklist, competitor research, and backend handoff docs. This is still not public backend upload/storage/lookup/symbolication. Local evidence: 20 focused upload/gate tests; JS upload, native upload, and native dry-run smokes; Python syntax over touched scripts/tests; ShellCheck 0.11.0; workflow YAML parse; markdown links; backend reports; confidentiality scan; release metadata; generated-artifact hygiene; diff check; and thermo review passed. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication contract, backend-unminified Vite/Next/React Native error proof, public build-plugin/tool packaging, Portable PDB direct parsing, PDB payload validation, Unity ZIP/archive support, React Native native symbolication/optional heavier automatic instrumentation, Unity OTel/rich events/URL timings, Swift/Objective-C automatic instrumentation, Kotlin Android lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper Ruby/PHP/.NET/Java/Go/Python framework/outbound/DB/cache/queue spans.
- 2026-06-18: JavaScript local minified-frame source-map proof gap reduced after refreshing Sentry JavaScript `getsentry/sentry-javascript@e856eb4acb67057861b96f924347af6c8b86edfb` `packages/core/src/utils/stacktrace.ts`, `packages/core/src/types/debugMeta.ts`, `dev-packages/test-utils/src/sourcemap-upload-utils.ts`, Next build source-map handlers, and SvelteKit Vite plugin paths, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/validation.ts` and `upload.ts`. Functions/classes read include Sentry `createStackParser`, `stripSentryFramesAndReverse`, `normalizeStackTracePath`, debug-image metadata types, `getArtifactBundles`, `getDebugIdPairs`, `getSourcemaps`, `handleRunAfterProductionCompile`, and source-map upload path option builders; Datadog `validatePayload`, `SourcemapsUploadCommand.execute`, and `getPayloadsToUpload`. Added dependency-free `scripts/verify_js_release_artifact_symbolication.py` with base64-VLQ source-map mapping resolution, ready-manifest/debug-ID checks, sanitized URL/path/local-frame matching, `sourcesContent` plus absolute-source-path rejection, and privacy-bounded reports. Vite smoke now captures an actual thrown V8 frame from built `vite@8.0.16`; Next and React Native smokes generate V8-style frames from real minified `next@16.2.9` and `react-native@0.86.0` outputs; all resolve without `@jridgewell/trace-mapping`, raw source content, query/hash text, or temp paths. Local evidence: focused symbolication/upload/gate tests ran 29 tests, full unittest discovery ran 139 tests, Vite/Next/React Native release-artifact smokes passed, Python AST parse, ShellCheck 0.11.0, markdown links, backend reports, confidentiality scan, release metadata, workflow YAML parse, generated-artifact hygiene, diff check, automation TOML prompt-size check, and thermo review passed. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication contract, backend-symbolicated Vite/Next/React Native error proof, public build-plugin/tool packaging, Portable PDB direct parsing, PDB payload validation, Unity ZIP/archive support, React Native native symbolication/optional heavier automatic instrumentation, Unity OTel/rich events/URL timings, Swift/Objective-C automatic instrumentation, Kotlin Android lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper Ruby/PHP/.NET/Java/Go/Python framework/outbound/DB/cache/queue spans.
- 2026-06-18: JavaScript installed release-artifact CLI packaging gap reduced after reading Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `package.json`, `src/commands/sourcemaps/mod.rs`, and `src/commands/sourcemaps/upload.rs`; Sentry JavaScript `getsentry/sentry-javascript@e856eb4acb67057861b96f924347af6c8b86edfb` `packages/nextjs/src/config/handleRunAfterProductionCompile.ts`; and Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/package.json`, `src/cli.ts`, `commands/sourcemaps/cli.ts`, `commands/sourcemaps/upload.ts`, and `commands/sourcemaps/validation.ts`. Competitor pattern: release-artifact workflows are reachable from installed package bins or framework build hooks, validate local artifacts before upload, keep auth/upload outside runtime SDK config, and preserve dry-run/no-upload paths. LogBrew now ships dependency-free `@logbrew/sdk` bin `logbrew-release-artifacts` with local `prepare-js` and `manifest-js`: it injects matching Debug IDs, optionally strips `sourcesContent` and source prefixes, validates manifests, strips query/hash text, and performs no upload, support-case creation, account/session API value use, or backend symbolication claim. Local evidence so far: JS release-artifact CLI tests, full JS package tests, installed npm-pack CLI smoke, JS package metadata check, JS source syntax, JS package/publint check, ESLint, release metadata, focused verifier tests, base JS smoke, ShellCheck 0.11.0, markdown links, and backend reports passed. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication, backend-symbolicated Vite/Next/React Native error proof, public framework build plugins, native package-facing artifact tooling, React Native native symbolication, Unity OTel/rich URL timings/native symbolication, Swift/Objective-C automatic instrumentation, Kotlin Android lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: Backend coordination update: Agent Support Ticket API contract is defined for future implementation but routes are not live; do not call `POST /api/support/tickets`, `GET /api/support/tickets`, or `GET /api/support/tickets/{ticket_id}` until backend reports deployed support storage/routes. Planned create payload fields are `project_id?`, `source`, `category`, `title`, `description`, `environment?`, `runtime?`, `framework?`, `sdk_package?`, `sdk_version?`, `release?`, `trace_id?`, `event_id?`, and sensitive-value-free structured `diagnostics?`; source enum is `cli|sdk|website|docs|mobile`; category enum is `sdk_install_failure|ingest_failure|auth_failure|project_setup|dashboard_issue|docs_confusion|cli_issue|mobile_issue|billing_question|other`. SDKs may add explicit diagnostics helpers, but must never silently open support tickets; any SDK UI/API must require user or agent action, redact auth values, cookies, and sensitive strings, and report backend contract drift back to backend automation. Routing: `sdk_install_failure` routes to SDK; `ingest_failure`, `auth_failure`, and `billing_question` stay backend-owned; `project_setup` and `other` route by source. Backend handoff format is `{ticket_id, source, category, expected_owner, title, user_impact, diagnostics_summary, requested_action}`.
- 2026-06-18: JavaScript installed frame-resolution gap reduced after reading Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/commands/sourcemaps/resolve.rs`/`mod.rs` (`make_command`, `lookup_pos`, `execute`, location helpers) and Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/validation.ts`/`upload.ts` (`validatePayload`, `SourcemapsUploadCommand.execute`, `getMatchingSourcemapFiles`). Competitor pattern: installed tooling lets developers resolve or validate source-map inputs before upload. LogBrew now ships `logbrew-release-artifacts symbolicate-js` from installed `@logbrew/sdk`; it resolves one V8-style generated frame through a ready manifest, checks Debug ID consistency, rejects `sourcesContent` and machine-specific source identifiers, confines source maps to the build directory, and reports only generated/original positions. Package split keeps `release-artifacts.js` 759 lines and resolver module 314 lines. Local evidence: `node --check` for both CLI modules, `cd js/logbrew-js && npm test` ran 56 tests, installed npm-pack CLI smoke proves `prepare-js`, `manifest-js`, `symbolicate-js`, package file inclusion, source/content prefix stripping, query/hash stripping, and no marker/query leakage. Still not backend upload/storage/lookup/symbolication; next priorities remain backend release-artifact contract, public build-plugin/tool packaging, native package-facing artifact tooling, and React Native/native/Unity symbolication gaps.
- 2026-06-18: JavaScript Vite build-plugin ergonomics gap reduced after reading Sentry bundler plugins `getsentry/sentry-javascript-bundler-plugins@988efd30691e08c059eb577e499d0b4346434f3c` `packages/bundler-plugins/src/rollup/index.ts`/`vite/index.ts` (`_rollupPluginInternal`, `renderChunk`, `writeBundle`, `sentryVitePlugin`), Sentry framework source-map wiring `getsentry/sentry-javascript@c9730ce3a3635f83ab629559bddbd7345fae9917` `packages/sveltekit/src/vite/sourceMaps.ts`, `packages/nuxt/src/vite/sourceMaps.ts`, and `packages/nextjs/src/config/handleRunAfterProductionCompile.ts`, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `commands/sourcemaps/upload.ts`/`validation.ts`. Pattern: framework/build plugins enable or preserve source maps, wait until output exists, validate release/service/path-prefix metadata, then keep upload separate from runtime SDK config. LogBrew now ships dependency-free `@logbrew/sdk/vite-release-artifacts` with ESM/CJS/types; it sets hidden Vite source maps only when unset, runs installed local `prepare-js --write` and `manifest-js`, strips `sourcesContent` by default, strips app-root source prefixes, writes a ready manifest, and never uploads, opens support tickets, uses account/session API values, or claims backend symbolication. Local evidence: focused TDD red/green, `node --check` for release-artifact modules, `cd js/logbrew-js && npm test` ran 57 tests, JS source syntax, release metadata, publint 0.3.21, ESLint, installed CLI smoke, and installed Vite plugin smoke proving packed subpath ESM/CJS imports, plugin hidden source maps, manifest readiness, query/hash stripping, no raw source content/temp-path leakage, and actual thrown Vite frame resolution through installed `symbolicate-js`. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication, backend-symbolicated Vite/Next/React Native proof, Next/React Native package-facing plugins, native package-facing artifact tooling, React Native native symbolication, Unity native/OTel/rich URL timing gaps, Swift/Objective-C automatic instrumentation, Kotlin Android lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: JavaScript Next build-helper ergonomics gap reduced after reading Sentry JavaScript `getsentry/sentry-javascript@c9730ce3a3635f83ab629559bddbd7345fae9917` `packages/nextjs/src/config/withSentryConfig/index.ts`, `getFinalConfigObject.ts`, `getFinalConfigObjectBundlerUtils.ts`, `handleRunAfterProductionCompile.ts`, `getBuildPluginOptions.ts`, and `webpack.ts`, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/sourcemaps/upload.ts`. Pattern: mature Next tooling wraps object/function config, preserves explicit source-map settings, chains production compile hooks, scopes `.next/static/chunks` artifacts, and keeps release/service/path-prefix validation separate from runtime SDK config. LogBrew now ships `@logbrew/next/release-artifacts` with ESM/CJS/types; it sets `productionBrowserSourceMaps` only when unset, wraps sync/async config functions, preserves app-owned `compiler.runAfterProductionCompile`, runs installed `@logbrew/sdk` `prepare-js --write` and `manifest-js`, strips `sourcesContent` by default, writes `.next/logbrew-release-artifacts.json`, defaults minified URLs to `app:///_next/static/chunks`, and never uploads, opens support tickets, uses account/session API values, or claims backend symbolication. Local evidence: focused TDD red/green, `cd js/logbrew-next && npm test` ran 2 tests plus syntax checks, release metadata gate, smoke-gate unittest, and installed `scripts/real_user_next_release_artifact_smoke.sh` proving packed `@logbrew/sdk` + `@logbrew/next` subpath ESM/CJS imports, Next 16.2.9 build-hook manifest generation, source-content stripping, query/hash stripping, and actual minified-frame resolution through installed `symbolicate-js`. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication, backend-symbolicated Vite/Next/React Native proof, React Native package-facing artifact helper, native package-facing artifact tooling, React Native native symbolication, Unity native/OTel/rich URL timing gaps, Swift/Objective-C automatic instrumentation, Kotlin Android lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: React Native package-facing release-artifact ergonomics gap reduced after refreshing Sentry React Native `getsentry/sentry-react-native@20fbd51c4eaf2bfb50fcbd86fa4538cd0df89213` `packages/core/sentry.gradle.kts`, `sentry-xcode.sh`, `copy-debugid.js`, `has-sourcemap-debugid.js`, and Expo upload CLI, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/react-native/injectDebugId.ts`, `upload.ts`, `validation.ts`, and `utils.ts`. Pattern: Sentry uses heavier Gradle/Xcode upload integration; Datadog exposes explicit bundle/map commands with platform/release/service/build metadata. LogBrew now ships the lighter local subset as `@logbrew/react-native/release-artifacts`: `prepareLogBrewReactNativeReleaseArtifacts({ bundle, sourcemap?, platform, release, environment, service, ... })` runs installed `@logbrew/sdk` `prepare-js --write` and `manifest-js`, strips `sourcesContent` and app-root source prefixes by default, defaults minified URLs to `app:///react-native/<platform>`, supports ESM/CJS/types, and never patches Gradle/Xcode/Metro, uploads, opens support tickets, uses account/session API values, or claims backend symbolication. Local evidence: focused TDD red/green with `cd js/logbrew-react-native && npm test`, JS source syntax, release metadata gate, smoke-gate unittest, full unittest discovery, JS package/publint 0.3.21, ESLint, ShellCheck 0.11.0, markdown links, backend reports, confidentiality scan, generated-artifact hygiene, diff check, thermo review, and installed `bash scripts/real_user_react_native_release_artifact_smoke.sh` proving packed `@logbrew/sdk` + `@logbrew/react-native` subpath ESM/CJS imports, React Native 0.86.0 Metro bundle helper manifest generation, source-content/source-prefix stripping, query/hash stripping, and actual minified-frame resolution through installed `symbolicate-js`. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication, backend-symbolicated Vite/Next/React Native proof, native package-facing artifact tooling, React Native native symbolication, Unity native/OTel/rich URL timing gaps, Swift/Objective-C automatic instrumentation, Kotlin Android lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: Portable PDB payload validation gap reduced after reading current Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` PE-symbol paths, Sentry CLI `getsentry/sentry-cli@d9766db0b3c61cc6c0e445d8840c0765c42d6eea` `src/utils/dif_upload/mod.rs`, and Sentry symbolic `getsentry/symbolic@3581ad0d67a074e83ba10e612bce34a83e5916a8` Portable PDB parser paths recorded in `docs/competitor-research/source-maps-debug-symbols-2026-06-13.md`. LogBrew now parses dependency-free external Portable PDB `BSJB` metadata roots, reads only the `#Pdb` stream identity, cross-checks it against PE CodeView debug ID, blocks malformed or mismatched Portable PDBs, warns on unrecognized PDB formats, and keeps payload bytes, source paths, symbols, and source-link data out of manifests. Local evidence so far: focused TDD red/green, `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_native_release_artifact_manifest` ran 29 tests, full `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests` ran 142 tests, `bash scripts/real_user_native_release_artifact_smoke.sh`, and Python syntax checks passed. Remaining gaps: embedded Portable PDB extraction, classic Windows PDB payload parsing, backend upload/storage/lookup/symbolication, backend-symbolicated Vite/Next/React Native proof, native package-facing artifact tooling, React Native native symbolication, Unity native/OTel/rich URL timings, Swift/Objective-C automatic instrumentation, Kotlin lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: Embedded Portable PDB validation gap reduced after refreshing current Sentry symbolic `getsentry/symbolic@15e05ed4827c11ed410763de879757ef0246e4b8` `symbolic-debuginfo/src/pe.rs` and `symbolic-ppdb/src/format/{mod.rs,streams.rs,raw.rs}`; Sentry CLI and Datadog CI heads still match the previously recorded commits. Source pattern: PE debug-directory type 17 uses `MPDB`, declared uncompressed size, raw deflate payload, and Portable PDB `#Pdb` identity after decompression. LogBrew now accepts `dotnet_pdb` PE files with valid embedded Portable PDBs when sibling `.pdb` files are absent, caps embedded decompression, parses only the `#Pdb` identity, blocks malformed or mismatched embedded payloads, and keeps compressed bytes, source links, embedded sources, symbols, and payload contents out of manifests. Local evidence so far: focused red/green embedded tests, `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_native_release_artifact_manifest tests.test_native_release_artifact_embedded_pdb` ran 32 release-artifact tests, `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests` ran 145 tests, `bash scripts/real_user_native_release_artifact_smoke.sh` proves an embedded-only PE without a sibling `.pdb`, Python syntax checked 77 files, and ShellCheck 0.11.0 passed. Remaining gaps: classic Windows PDB payload parsing, backend upload/storage/lookup/symbolication, backend-symbolicated Vite/Next/React Native proof, native package-facing artifact tooling, Unity ZIP/archive/native symbolication, React Native native symbolication, Swift/Objective-C automatic instrumentation, Kotlin lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: Classic Windows PDB validation gap reduced after reading Sentry symbolic `getsentry/symbolic@15e05ed4827c11ed410763de879757ef0246e4b8` `symbolic-debuginfo/src/pdb/mod.rs` (`PdbObject::test`, `PdbObject::parse`, `PdbObject::debug_id`) and upstream Rust `pdb@0.8.0` `src/msf/mod.rs`, `src/pdb.rs`, `src/pdbi.rs`, and `src/dbi.rs` (`BigMSF::new`, `BigMSF::look_up_stream`, `PDB::pdb_information`, `PDB::debug_information`, `PDBInformation::parse`, `DebugInformation::age`). Pattern: recognize MSF 7.00 PDBs, read stream 1 for GUID/PDB age, prefer stream 3 DBI age for PE CodeView matching when present, and treat GUID/age as the debug identity. LogBrew now parses only bounded MSF stream directories plus PDB-info/DBI identity bytes for `dotnet_pdb`, cross-checks classic Windows PDB debug IDs against PE CodeView, blocks malformed or mismatched payloads, and keeps symbols, source streams, modules, file paths, and PDB bytes out of manifests; external/embedded Portable PDB validation remains current. Local evidence: focused TDD red/green classic tests, release-artifact focused tests ran 36 tests, full `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests` ran 149 tests, Python syntax checked 78 files, `bash scripts/real_user_native_release_artifact_smoke.sh`, ShellCheck 0.11.0, markdown links, backend reports, confidentiality scan, release metadata, generated-artifact hygiene, Ruby workflow YAML parse, and diff hygiene passed. Remaining gaps: backend upload/storage/lookup/symbolication, backend-symbolicated Vite/Next/React Native proof, native package-facing artifact tooling, Unity ZIP/archive/native symbolication, PDB/Breakpad edge-case hardening, React Native native symbolication, Swift/Objective-C automatic instrumentation, Kotlin lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: Unity ZIP archive validation gap reduced after re-reading Sentry Unity `getsentry/sentry-unity@a9bebf56a8808b866ff11330bf42030685701cf9` `src/Sentry.Unity.Editor/Android/DebugSymbolUpload.cs` and `src/Sentry.Unity.Editor/Native/BuildPostProcess.cs`, plus Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` `packages/base/src/commands/unity-symbols/upload.ts` and `interfaces.ts`. Pattern: competitors accept Unity symbols as build artifacts, copy/discover `.so` files plus IL2CPP mappings from build tooling, and upload with explicit build identity. LogBrew now accepts `unity_symbols=<symbols.zip>` in the native manifest dry-run path, validates safe ZIP entries, requires one safe `build_id`, validates extracted `.so` files through existing ELF rules, summarizes optional `LineNumberMappings.json` as metadata-only, reports paths as `archive.zip!entry`, blocks unsafe archive paths without echoing them, and still avoids backend upload/symbolication/support-ticket claims. Local evidence: TDD red run failed against the previous directory-only validator; final `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests` ran 151 tests, `python3 scripts/check_python_sources.py scripts tests` checked 79 files, `bash scripts/real_user_native_release_artifact_smoke.sh`, `bash scripts/real_user_native_release_artifact_upload_smoke.sh`, ShellCheck 0.11.0, markdown links, backend reports, confidentiality scan, release metadata, workflow YAML parse, generated-artifact hygiene, diff check, and thermo review passed. Backend support-ticket routes remain planned-only; SDK code must not call them or silently create tickets. Remaining gaps: backend release-artifact upload/storage/lookup/symbolication contract, backend-symbolicated Vite/Next/React Native/native proof, React Native native symbolication, Unity OTel/rich URL timings/native symbolication, Swift/Objective-C automatic instrumentation, Kotlin Android lifecycle/OkHttp/coroutines, Rust full OTel interop, and deeper server framework/outbound spans.
- 2026-06-18: Unity ZIP native upload proof gap reduced after refreshing current public Sentry Unity `getsentry/sentry-unity@a9bebf56a8808b866ff11330bf42030685701cf9` `DebugSymbolUpload.cs`/`BuildPostProcess.cs` and Datadog CI `DataDog/datadog-ci@625da2306abd9ac25ffbc1b4a810beed8cee5ef9` Unity upload plus shared upload/retry helpers. Pattern: competitors upload Unity symbols from explicit build tools and treat native `.so` and IL2CPP mapping payloads as artifact files, not runtime SDK telemetry. LogBrew now extends `scripts/real_user_native_release_artifact_upload_smoke.sh` to include a validated `unity_symbols` ZIP alongside dSYM, ProGuard/R8, and Android `.so` artifacts; the loopback fake intake verifies one basename-only `symbols.zip` multipart part, retry/auth/validation/dry-run/tamper paths, and no fake auth value, query text, or temp path leakage in reports/metadata. Thermo review extracted repeated Unity ZIP fixture construction into `tests/native_unity_fixture.py`. Local evidence: TDD red gate failed before the smoke included `unity_symbols`; focused native upload/Unity archive/gate tests ran 11 tests; full `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests` ran 153 tests; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/check_python_sources.py scripts tests` checked 80 files; native dry-run and upload smokes, ShellCheck 0.11.0, markdown links, backend reports, confidentiality scan, release metadata, workflow YAML parse, generated-artifact hygiene, diff check, automation TOML parse/prompt-size check, and thermo review passed. This remains loopback-only; backend upload/storage/lookup/symbolication routes are still required before public support claims.
- 2026-06-18: Unity request phase timing gap reduced after refreshing Sentry Unity `getsentry/sentry-unity@a9bebf56a8808b866ff11330bf42030685701cf9` startup/scene/awake tracing sources and Datadog Unity `DataDog/dd-sdk-unity@309e84de7c20af6ec47c2ff3e6c6f8d339937e93` plus package mirror `DataDog/unity-package@1dbbfe29d72028c430b800107a2cf3271a0aa163` resource tracking sources. Datadog is stronger for wrapped UnityWebRequest/resource timing, status, bytes, and error details; Sentry is stronger for startup/scene spans. LogBrew now adds dependency-free `UnityRequestTimings` for optional app-supplied queued/name-lookup/connect/TLS/send/wait/receive durations and response byte counts on `CaptureRequestSpan(...)` and `UnityRequestTracker.Capture(...)`, keeping route-only metadata, active trace correlation, and spoof-key overwrite while avoiding request wrapper ownership, full URLs, headers, payloads, content type, error messages, baggage, tracestate, raw propagation metadata, and `UnityEngine.Networking` dependency. Local evidence so far: Unity package tests ran 23 tests and installed request-tracker payload checker proves timing metadata. Remaining Unity gaps: OTel context ingestion, richer span events/exceptions beyond bounded fields, native crash/symbolication parity, and optional heavier framework-owned instrumentation for teams that explicitly want hidden/global hooks.
- 2026-06-18: Swift URLSession phase-timing gap reduced after reading Sentry Cocoa `getsentry/sentry-cocoa@1caa2120fe0b75e2e9d2b30ad51a63de6bc0e05c` `SentryNetworkTracker.m` network breadcrumb/body-size paths, Datadog iOS `DataDog/dd-sdk-ios@72544f98732c6e216e211fb6e1e799848f8f8c35` `NetworkInstrumentationFeature.task(_:didFinishCollecting:)`, `URLSessionTaskInterception.ResourceMetrics.init(taskMetrics:)`, `RUMMonitorProtocol+Internal.addResourceMetrics(...)`, and OpenTelemetry Swift `open-telemetry/opentelemetry-swift@291fe3fff413ae9277ac36aec9fd9b51c1caa7e0` URLSession span/header instrumentation. Datadog's useful pattern is delegate-collected `URLSessionTaskMetrics` split into fetch/redirect/name-lookup/connect/TLS/send/wait/receive and byte-count metadata; Sentry/OpenTelemetry are broader and more automatic. LogBrew now adds dependency-free `LogBrewURLSessionTimings` for explicit app-supplied timings or app-owned `URLSessionTaskMetrics`, merges them into `captureURLSessionSpan(...)` after caller metadata so spoofed timing keys are overwritten, and records only numeric phase durations/byte counts while avoiding URLSession patching, headers, payloads, full URLs/query/fragment, cookies, baggage, tracestate, and raw propagation metadata. Local evidence: focused TDD red failed for missing `LogBrewURLSessionTimings`/`timings:` API, final `swift test --package-path swift/logbrew-swift --scratch-path /tmp/logbrew-swift-timing-package-2` ran 35 tests in 8 suites, `bash scripts/check_swift_style.sh`, `bash scripts/check_swift_package.sh`, packaged trace-correlation checker, and `bash scripts/real_user_swift_smoke.sh` passed. Remaining Swift gaps: automatic URLSession/lifecycle instrumentation, live OTel Context/Span extraction, baggage/tracestate, rich span events/exceptions, native symbolication parity, and dedicated integration-package automation for teams that explicitly want heavier hooks.
